---
phase: quick-260812-56e-create-the-muchakucha-zwei-app-icon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/assets/images/icon.png
  - apps/client/app.json
autonomous: true
must_haves:
  truths:
    - "The Muchakucha Zwei app icon shows one centered, unbroken line changing from a loose coral tangle on the left into a clean teal checkmark on the right."
    - "The icon uses a warm ivory background, rounded caps, consistent stroke weight, generous padding, and remains recognizable at 48px."
    - "The icon contains no text, letters, gradient, texture, shadow, bevel, 3D treatment, or photorealistic detail."
    - "Expo resolves the persisted 1024x1024 PNG as the client application's icon."
  artifacts:
    - path: "apps/client/assets/images/icon.png"
      provides: "Final 1024x1024 Muchakucha Zwei application icon"
    - path: "apps/client/app.json"
      provides: "Expo icon configuration pointing to the project-local PNG"
      contains: '"icon": "./assets/images/icon.png"'
  key_links:
    - from: "apps/client/app.json"
      to: "apps/client/assets/images/icon.png"
      via: "expo.icon project-relative path"
      pattern: '"icon"\\s*:\\s*"\\./assets/images/icon\\.png"'
---

<objective>
Create and install the Muchakucha Zwei app icon as a production-ready project asset.

Purpose: Give the mobile-first application a distinctive, friendly identity that communicates movement from household chaos to orderly completion.
Output: A visually inspected 1024x1024 PNG at `apps/client/assets/images/icon.png`, referenced by the existing Expo configuration.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@apps/client/app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate, inspect, and install the Muchakucha Zwei icon</name>
  <files>apps/client/assets/images/icon.png, apps/client/app.json</files>
  <action>Use the built-in image generation tool to create a square 1024x1024 flat vector-style icon. The composition is a single visually continuous, unbroken line: it begins on the left as a loose, readable tangled scribble or knot in solid warm terracotta coral `#B94736`, then resolves toward the right into a clean smooth checkmark in solid deep teal `#277A72`. Join the two solid-color portions cleanly without a gradient. Keep one consistent, substantial stroke weight with rounded caps; center the complete mark on solid warm ivory `#FFF8F2` with generous safe-area padding so neither tangle nor checkmark approaches the edges. The result must be minimalist, soft, friendly, and warm rather than corporate, with no text or letters and no texture, shadow, bevel, dimensional lighting, or photorealistic detail.

Inspect the generated result with the image-viewing tool at full size. Create a temporary 48x48 downscaled preview outside the repository and inspect that preview as well. Confirm that the mark still reads as tangle-to-check, the checkmark is not visually lost, the line remains apparently continuous, the three specified colors dominate, and all excluded effects are absent. If any criterion fails, edit/regenerate with the built-in image generation tool and repeat both inspections; do not persist a near miss.

After inspection passes, create `apps/client/assets/images/` if needed and persist the final PNG exactly as `apps/client/assets/images/icon.png`. Preserve any unrelated user changes already present in `apps/client/app.json`; make only the scoped addition of Expo's top-level `icon` property with value `./assets/images/icon.png`. Do not add dependencies, source files, alternate icon variants, splash assets, or unrelated configuration. Remove the temporary 48px preview after inspection.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; Add-Type -AssemblyName System.Drawing; $p='apps/client/assets/images/icon.png'; if (-not (Test-Path -LiteralPath $p)) { throw 'icon missing' }; $img=[System.Drawing.Image]::FromFile((Resolve-Path $p)); try { if ($img.Width -ne 1024 -or $img.Height -ne 1024) { throw ('unexpected dimensions: ' + $img.Width + 'x' + $img.Height) } } finally { $img.Dispose() }; $cfg=Get-Content -Raw 'apps/client/app.json' | ConvertFrom-Json; if ($cfg.expo.icon -ne './assets/images/icon.png') { throw 'Expo icon path mismatch' }; Write-Output 'icon dimensions and Expo wiring verified'"</automated>
  </verify>
  <done>`apps/client/assets/images/icon.png` is a valid 1024x1024 PNG; full-size and 48px visual inspection confirm the specified ivory/coral/teal flat composition, continuous tangle-to-check transformation, generous padding, small-size legibility, and absence of text or effects; `apps/client/app.json` references the icon without disturbing its other settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated image output to repository asset | Tool-generated binary content becomes a shipped mobile application resource. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q56E-01 | Tampering | `apps/client/assets/images/icon.png` | low | mitigate | Open the persisted file, verify its decoded 1024x1024 dimensions, visually inspect both full-size and 48px renderings, and confirm Expo points to that exact project-local path. |
</threat_model>

<source_audit>

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | QUICK-260812-56E | Create the Muchakucha Zwei app icon from the supplied exact design brief and persist it in the Expo project | 01 | COVERED | Task 1 generates, iterates, inspects, persists, and wires the icon. |
| REQ | — | No ROADMAP requirement IDs assigned to this quick task | — | EXCLUDED | Quick task scope. |
| RESEARCH | — | No research phase requested | — | EXCLUDED | The design brief and existing Expo structure are sufficient. |
| CONTEXT | — | No separate CONTEXT.md decisions | — | EXCLUDED | The exact design brief is captured directly in Task 1. |
</source_audit>

<verification>
- Decode `apps/client/assets/images/icon.png` and prove it is exactly 1024x1024.
- Parse `apps/client/app.json` and prove `expo.icon` resolves to `./assets/images/icon.png`.
- Inspect the final asset at 1024px and a temporary 48px rendering for palette, composition, continuity, padding, readability, and forbidden visual effects.
- Run `git diff -- apps/client/app.json` and confirm the icon property is the only task-authored configuration change while pre-existing user edits remain intact.
</verification>

<success_criteria>
- The final project-local PNG matches the exact supplied design brief and remains legible at 48px.
- Expo is configured to use the PNG as the application icon.
- No unrelated files, dependencies, configuration, or existing user changes are altered.
</success_criteria>

<output>
Create `.planning/quick/260812-56e-create-the-muchakucha-zwei-app-icon-from/260812-56e-SUMMARY.md` when done.
</output>
