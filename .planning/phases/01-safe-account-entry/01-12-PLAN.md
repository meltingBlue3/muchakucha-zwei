---
phase: 01-safe-account-entry
plan: 12
type: execute
wave: 6
depends_on: [01-04, 01-07, 01-10]
files_modified: [apps/client/src/platform/session/pending-proof.ts, apps/client/src/platform/session/pending-proof.native.ts, apps/client/src/platform/session/pending-proof.web.ts, apps/client/src/platform/session/session-transport.ts, apps/client/src/platform/session/session-transport.native.ts, apps/client/src/platform/session/session-transport.web.ts, apps/client/src/features/auth/session-state.ts, apps/client/src/platform/session/__tests__/session-transport-test.ts]
autonomous: true
requirements: [AUTH-02, AUTH-03, SAFE-03, SAFE-04]
must_haves:
  truths:
    - "Before any registration or verification UI, typed PendingProofStore, SessionTransport, and authenticated session-state contracts exist."
    - "Native stores pending proof/refresh only in SecureStore; Web exposes no secret getter/setter and uses credentialed cookies only."
    - "An issued access/native-refresh pair can be accepted atomically into in-memory authenticated state and durable native transport."
  artifacts:
    - {path: apps/client/src/platform/session/pending-proof.ts, provides: typed pending-proof read/write/clear contract}
    - {path: apps/client/src/platform/session/session-transport.ts, provides: issued-session acceptance and clear/restore contract}
    - {path: apps/client/src/features/auth/session-state.ts, provides: authenticated/unauthenticated/offline/reauth state ownership}
  key_links:
    - {from: apps/client/src/platform/session/session-transport.native.ts, to: expo-secure-store, via: namespaced refresh and pending-proof keys}
    - {from: apps/client/src/platform/session/session-transport.web.ts, to: generated API requests, via: credentials include with no readable secret API}
---
<objective>Establish and test the D-06 continuation/session boundary before UI consumes it. Purpose: make registration write, verification read/clear, issued-session acceptance, and authenticated handoff one coherent typed pipeline. Output: platform adapters, session state, and green contract tests.</objective>
## Phase Goal

**As a** user, **I want to** register, verify my email, sign in, restore my session, reset my password, sign out, and update my display name in the real client, **so that** I can securely access and retain my account session.
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-CONTEXT.md
@.planning/phases/01-safe-account-entry/01-RESEARCH.md
@.planning/phases/01-safe-account-entry/01-PATTERNS.md
@.planning/phases/01-safe-account-entry/01-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify the complete continuation and session contract</name>
  <files>apps/client/src/platform/session/__tests__/session-transport-test.ts, apps/client/src/platform/session/pending-proof.ts, apps/client/src/platform/session/session-transport.ts, apps/client/src/features/auth/session-state.ts</files>
  <behavior>Registration proof write; verification proof read then clear on terminal outcome; native issued refresh persisted before authenticated state; access kept in memory; Web has credentialed-cookie calls and no secret accessor; clear is idempotent.</behavior>
  <action>Activate the session-transport suite with `IMPLEMENTATION_MISSING_SESSION_BOUNDARY`, then run the RED helper for this exact suite. Define PendingProofStore and SessionTransport signatures including `acceptIssuedSession({accessToken, refreshToken?})`, typed restore outcomes, and session-state transition methods. The test must reject AsyncStorage/localStorage and any Web secret getter.</action>
  <verify><automated>pwsh -NoProfile -File scripts/assert-red.ps1 -Suite client -TestPath src/platform/session/__tests__/session-transport-test.ts -Marker IMPLEMENTATION_MISSING_SESSION_BOUNDARY</automated></verify>
  <done>The boundary suite fails only because platform implementations are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement native and Web boundary adapters</name>
  <files>apps/client/src/platform/session/pending-proof.native.ts, apps/client/src/platform/session/pending-proof.web.ts, apps/client/src/platform/session/session-transport.native.ts, apps/client/src/platform/session/session-transport.web.ts, apps/client/src/features/auth/session-state.ts</files>
  <action>Implement namespaced SecureStore proof/refresh operations and memory-only access/session state on native. Implement Web proof/session adapters as credentialed transport with no proof/refresh getter or setter; HttpOnly ownership remains API-only. `acceptIssuedSession` stores native refresh before publishing authenticated access, accepts Web access without secret persistence, and supports rollback/clear on storage failure.</action>
  <verify><automated>pnpm --filter client test --runInBand session-transport && pnpm --filter client typecheck</automated></verify>
  <done>The typed platform boundary is green and ready for registration and verification consumers.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | Information Disclosure | client secrets | high | mitigate | Native SecureStore only; Web exposes no secret access; access remains memory-only. |
| T-12-02 | Tampering | partial session acceptance | high | mitigate | Persist native refresh before publishing authenticated state, with rollback on failure. |
</threat_model>
<verification>Focused session boundary tests and client typecheck pass.</verification>
<success_criteria>D-06 consumers have a complete, tested continuation/session API before any screen writes or reads proof material.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-12-SUMMARY.md`.</output>
