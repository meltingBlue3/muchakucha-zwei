---
phase: 01-safe-account-entry
plan: 14
type: execute
wave: 5
depends_on: [01-08]
files_modified: [apps/client/src/ui/theme.ts, apps/client/src/ui/primitives.tsx, apps/client/src/ui/__tests__/token-static-test.ts, apps/client/src/ui/__tests__/contrast-test.ts, apps/client/src/ui/__tests__/primitive-states-test.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]
must_haves:
  truths:
    - "Per D-14 through D-17, one typed owned component layer provides the approved warm, modern, restrained cross-platform visual language."
    - "All palette, spacing, typography, geometry, focus, loading, invalid, disabled, and password-reveal states pass measured tests."
  artifacts:
    - {path: apps/client/src/ui/theme.ts, provides: typed Muchakucha warm tokens}
    - {path: apps/client/src/ui/primitives.tsx, provides: complete UI-SPEC primitive surface}
  key_links:
    - {from: apps/client/src/ui/primitives.tsx, to: apps/client/src/ui/theme.ts, via: Restyle typed tokens only}
---
<objective>Implement the shared design system separately from feature screens. Purpose: keep the former 11-file registration/design plan within a five-file ownership boundary. Output: typed theme, owned accessible primitives, and green design-contract tests per D-14, D-15, D-16, and D-17.</objective>
## Phase Goal

**As a** user, **I want to** register, verify my email, sign in, restore my session, reset my password, sign out, and update my display name in the real client, **so that** I can securely access and retain my account session.
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-UI-SPEC.md
@.planning/phases/01-safe-account-entry/01-08-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate every design-system RED suite</name>
  <files>apps/client/src/ui/__tests__/token-static-test.ts, apps/client/src/ui/__tests__/contrast-test.ts, apps/client/src/ui/__tests__/primitive-states-test.tsx</files>
  <behavior>Typed tokens only; computed WCAG AA; all required primitives and states; 48×48 targets; 52px primary controls; medium radii; light hierarchy; no large illustration.</behavior>
  <action>Activate all three suites and invoke `assert-red.ps1` separately for each with marker `IMPLEMENTATION_MISSING_DESIGN_SYSTEM`. Discovery/config/import failures must fail the task.</action>
  <verify><automated>pwsh -NoProfile -File scripts/assert-red.ps1 -Suite client -TestPath src/ui/__tests__/token-static-test.ts -Marker IMPLEMENTATION_MISSING_DESIGN_SYSTEM && pwsh -NoProfile -File scripts/assert-red.ps1 -Suite client -TestPath src/ui/__tests__/contrast-test.ts -Marker IMPLEMENTATION_MISSING_DESIGN_SYSTEM && pwsh -NoProfile -File scripts/assert-red.ps1 -Suite client -TestPath src/ui/__tests__/primitive-states-test.tsx -Marker IMPLEMENTATION_MISSING_DESIGN_SYSTEM</automated></verify>
  <done>Each design suite independently fails only on its expected missing behavior marker.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement typed theme and owned primitives</name>
  <files>apps/client/src/ui/theme.ts, apps/client/src/ui/primitives.tsx</files>
  <action>Implement the exact UI-SPEC palette, spacing, typography, radii, focus, and elevation tokens in Restyle. Build Screen, Stack, Inline, Text, Heading, Button, IconButton, TextField, PasswordField, FormMessage, Banner, Spinner, BrandMark, AuthShell, LinkText, and StatusPanel with persistent labels, accessible state semantics, stable loading labels, password reveal without focus loss, individual Lucide imports, reduced-motion/forced-colors support, and no raw feature styling.</action>
  <verify><automated>pnpm --filter client test --runInBand token-static contrast primitive-states && pnpm --filter client typecheck</automated></verify>
  <done>The complete shared component contract is green and ready for auth/profile composition.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-01 | Denial of Service | inaccessible controls | medium | mitigate | Measured contrast, touch geometry, focus, semantics, and preference tests. |
</threat_model>
<verification>All design-system suites and client typecheck pass.</verification>
<success_criteria>The shared visual/accessibility contract is complete before feature-screen implementation.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-14-SUMMARY.md`.</output>
