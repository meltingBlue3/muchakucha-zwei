---
phase: 01-safe-account-entry
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified: [apps/client/package.json, apps/client/app.json, apps/client/tsconfig.json, apps/client/jest.config.js, apps/client/jest.setup.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, SAFE-03]
must_haves:
  truths:
    - "Expo component and platform-adapter tests are discoverable and run once without watch mode."
    - "SecureStore, network, reduced-motion, and forced-colors mocks preserve platform boundaries rather than leaking secrets."
  artifacts:
    - {path: apps/client/jest.config.js, provides: SDK 57 Jest Expo runner}
    - {path: apps/client/jest.setup.ts, provides: deterministic platform mocks}
  key_links:
    - {from: apps/client/jest.config.js, to: apps/client/jest.setup.ts, via: setupFilesAfterEnv}
---
<objective>Configure the Expo client test runner separately from API infrastructure. Purpose: keep runner setup within five files and make every later client RED gate trustworthy. Output: SDK-aligned client package/config, one-shot Jest Expo execution, and deterministic platform mocks.</objective>
## Phase Goal

**As a** user, **I want to** register, verify my email, sign in, restore my session, reset my password, sign out, and update my display name in the real client, **so that** I can securely access and retain my account session.
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-VALIDATION.md
@.planning/phases/01-safe-account-entry/01-RESEARCH.md
@.planning/phases/01-safe-account-entry/01-02-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Configure Expo component and adapter testing</name>
  <files>apps/client/package.json, apps/client/app.json, apps/client/tsconfig.json, apps/client/jest.config.js, apps/client/jest.setup.ts</files>
  <action>Configure Expo SDK 57, Expo Router, Jest Expo, RNTL, strict TypeScript, one-shot tests, and deterministic SecureStore/network/reduced-motion/forced-colors mocks. Define the Expo scheme and local association configuration without claiming production domains. Keep Web secret access absent.</action>
  <verify><automated>pnpm --filter client exec jest --version && pnpm --filter client exec tsc --noEmit</automated></verify>
  <done>The client runner and TypeScript configuration load without watch mode or undiscovered-test errors.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Repudiation | client test evidence | medium | mitigate | One-shot discovery and deterministic mocks prevent false runner success. |
| T-04-SC | Tampering | dependencies | high | mitigate | Use only the approved frozen lockfile from 01-02. |
</threat_model>
<verification>`pnpm --filter client exec jest --version && pnpm --filter client exec tsc --noEmit` succeeds.</verification>
<success_criteria>Client tests can be discovered independently of production screens.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-04-SUMMARY.md`.</output>
