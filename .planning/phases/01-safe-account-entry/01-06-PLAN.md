---
phase: 01-safe-account-entry
plan: 06
type: execute
wave: 5
depends_on: [01-04]
files_modified: [apps/api/prisma.config.ts, apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0001_auth_foundation/migration.sql, apps/api/src/infrastructure/prisma/prisma.module.ts, apps/api/src/infrastructure/prisma/prisma.service.ts, apps/api/test/setup-integration.ts, apps/api/test/reset-database.ts, apps/api/test/auth/register.int.test.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, SAFE-04]
must_haves:
  truths:
    - "The canonical migration enforces identity, hash-only token, session, expiry, and foreign-key invariants in PostgreSQL 18."
    - "Canonical email is trim plus Unicode normalization plus full lowercase while original delivery/display email remains preserved."
  artifacts:
    - {path: apps/api/prisma/migrations/0001_auth_foundation/migration.sql, provides: durable auth schema}
  key_links:
    - {from: apps/api/prisma/schema.prisma, to: migration.sql, via: reviewed Prisma Migrate history}
---
<objective>Establish the durable authentication model and migrated integration database. Purpose: settle irreversible identity/session invariants before API behavior. Output: Prisma 7 adapter, five-model schema, canonical migration, and deterministic DB harness.</objective>
## Phase Goal

**As a** user, **I want to** register, verify, sign in, restore, reset, sign out, and edit my nickname, **so that** I can securely retain and control my account session.
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-RESEARCH.md
@.planning/phases/01-safe-account-entry/SKELETON.md
@.planning/phases/01-safe-account-entry/01-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate persistence invariants in RED</name>
  <read_first><file>apps/api/test/auth/register.int.test.ts</file><file>.planning/phases/01-safe-account-entry/01-RESEARCH.md</file></read_first>
  <files>apps/api/test/auth/register.int.test.ts, apps/api/test/setup-integration.ts, apps/api/test/reset-database.ts</files>
  <behavior>Canonical equivalents collide; original delivery email is preserved; displayName is non-unique; token/proof/refresh values persist only as hashes; UTC/FK/check constraints reject invalid rows.</behavior>
  <action>Activate only the persistence cases in the Wave 0 registration suite and point the harness at a disposable migrated PostgreSQL 18 database. Use marker `IMPLEMENTATION_MISSING_AUTH_SCHEMA`; prove discovery before expecting that assertion failure.</action>
  <verify><automated>pwsh -NoProfile -File scripts/assert-red.ps1 -Suite api -TestPath test/auth/register.int.test.ts -Marker IMPLEMENTATION_MISSING_AUTH_SCHEMA</automated></verify>
  <acceptance_criteria>The suite is discovered and fails only because schema behavior is absent.</acceptance_criteria>
  <done>Persistence contract is meaningfully RED.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Migrate the auth foundation</name>
  <read_first><file>apps/api/test/auth/register.int.test.ts</file><file>.planning/phases/01-safe-account-entry/01-RESEARCH.md</file></read_first>
  <files>apps/api/prisma.config.ts, apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0001_auth_foundation/migration.sql, apps/api/src/infrastructure/prisma/prisma.module.ts, apps/api/src/infrastructure/prisma/prisma.service.ts</files>
  <action>Create User, AuthSession, RefreshToken, EmailVerificationToken, and PasswordResetToken using Prisma 7 `PrismaPg`, UUID foreign keys, `timestamptz(3)`, unique canonical email/token hashes/refresh lineage, conditional-state CHECK constraints, and hash-only pending-registration proof bound to verification state. Preserve original email separately, keep displayName non-unique, and apply the committed migration with `migrate deploy`, never `db push`.</action>
  <verify><automated>docker compose up -d --wait postgres mailpit && pnpm --filter api exec prisma migrate deploy && pnpm --filter api exec prisma migrate status && pnpm --filter api test --run test/auth/register.int.test.ts --testNamePattern persistence</automated></verify>
  <acceptance_criteria>Migration is applied with no pending changes and all persistence invariants pass against PostgreSQL 18.</acceptance_criteria>
  <done>The durable auth model is proven before API implementation.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | auth rows | high | mitigate | Real FK/unique/CHECK constraints and migrated DB tests. |
| T-06-02 | Information Disclosure | token columns | high | mitigate | Hash-only schema and direct persistence assertions. |
</threat_model>
<verification>Migration status and focused persistence cases pass.</verification>
<success_criteria>The canonical schema exists with no app-only integrity rule.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-06-SUMMARY.md`.</output>
