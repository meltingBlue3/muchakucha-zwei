---
phase: 01-safe-account-entry
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.base.json, compose.yaml, .env.test.example, playwright.config.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, SAFE-03, SAFE-04]
must_haves:
  truths:
    - "The approved dependency set installs as one pinned pnpm workspace and all root one-shot commands exist."
    - "PostgreSQL 18, Mailpit, exact local origins, and secret-free test configuration are deterministic."
  artifacts:
    - {path: package.json, provides: pinned workspace scripts and package manager}
    - {path: compose.yaml, provides: health-checked PostgreSQL 18 and Mailpit}
  key_links:
    - {from: package.json, to: pnpm-workspace.yaml, via: workspace scripts and package graph}
---

<objective>Install the approved stack and establish the root runtime contract. Purpose: give every later Wave 0 and feature plan one deterministic workspace. Output: pinned lockfile, strict TS base, Compose services, origins/env contract, and Playwright orchestration.</objective>

## Phase Goal

**As a** user, **I want to** register, verify, sign in, restore, reset, sign out, and edit my nickname, **so that** I can securely retain and control my account session.

<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-RESEARCH.md
@.planning/phases/01-safe-account-entry/01-VALIDATION.md
@.planning/phases/01-safe-account-entry/01-01-SUMMARY.md
@AGENTS.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Install the approved workspace</name>
  <read_first><file>.planning/phases/01-safe-account-entry/01-01-SUMMARY.md</file><file>.planning/phases/01-safe-account-entry/01-RESEARCH.md</file><file>AGENTS.md</file></read_first>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.base.json</files>
  <action>Install only the exact package pins approved in 01-01. Pin the pnpm 10 patch in `packageManager`; create strict shared TypeScript settings and root one-shot scripts `test:quick`, `test`, `test:integration`, `test:e2e:web`, `openapi:generate`, `openapi:check`, and `dev`. Do not install substitutions or preview packages.</action>
  <verify><automated>pnpm --version && pnpm install --frozen-lockfile && pnpm exec tsc --version</automated></verify>
  <acceptance_criteria>Fresh frozen installation succeeds with one lockfile and every root command is defined.</acceptance_criteria>
  <done>The approved package graph is reproducible.</done>
</task>
<task type="auto">
  <name>Task 2: Define deterministic local services and origins</name>
  <read_first><file>.planning/phases/01-safe-account-entry/01-RESEARCH.md</file><file>.planning/phases/01-safe-account-entry/01-VALIDATION.md</file></read_first>
  <files>compose.yaml, .env.test.example, playwright.config.ts</files>
  <action>Create PostgreSQL 18 and Mailpit services with health checks, isolated test database values, no committed secrets, and fail-fast `WEB_ORIGIN`, `API_ORIGIN`, and `EMAIL_LINK_ORIGIN` validation. Configure Playwright to orchestrate API and Expo Web without choosing production domains; production origins/provider stay Phase 6 release gates.</action>
  <verify><automated>docker compose config --quiet && pnpm exec playwright --version</automated></verify>
  <acceptance_criteria>Compose parses, required local origins are explicit, and Playwright resolves from the pinned workspace.</acceptance_criteria>
  <done>Local deployment inputs are deterministic and production-deferred choices are documented.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-SC | Tampering | package graph | high | mitigate | Prior blocking provenance approval plus frozen lockfile. |
| T-02-01 | Information Disclosure | test env | high | mitigate | Example-only values and fail-fast secret/origin validation. |
</threat_model>
<verification>`pnpm install --frozen-lockfile && docker compose config --quiet` succeeds.</verification>
<success_criteria>Workspace, services, and root commands are ready for explicit Wave 0 paths.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-02-SUMMARY.md`.</output>
