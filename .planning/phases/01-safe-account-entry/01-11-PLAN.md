---
phase: 01-safe-account-entry
plan: 11
type: execute
wave: 6
depends_on: [01-10]
files_modified: [apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/infrastructure/mail/mail.port.ts, apps/api/src/infrastructure/mail/smtp-mail.adapter.ts, apps/api/src/modules/auth/auth.module.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04, SAFE-03]
must_haves:
  truths:
    - "Nest/Fastify exposes a strict `/api/v1` boundary with exact-origin CORS, stable errors, redaction, throttling, and Swagger."
    - "Auth use cases depend on MailPort and deliver only after committed state; Mailpit/provider details remain infrastructure."
  artifacts:
    - {path: apps/api/src/main.ts, provides: versioned Fastify application boundary}
    - {path: apps/api/src/infrastructure/mail/mail.port.ts, provides: provider-neutral email contract}
  key_links:
    - {from: apps/api/src/modules/auth/auth.module.ts, to: apps/api/src/infrastructure/mail/mail.port.ts, via: Nest provider token}
---
<objective>Bootstrap the API and email boundary before registration behavior. Purpose: remove bootstrap/mail breadth from the registration TDD plan. Output: strict Nest/Fastify application, AuthModule shell, and provider-neutral SMTP/Mailpit adapter.</objective>
## Phase Goal

**As a** user, **I want to** register, verify my email, sign in, restore my session, reset my password, sign out, and update my display name in the real client, **so that** I can securely access and retain my account session.
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-RESEARCH.md
@.planning/phases/01-safe-account-entry/01-PATTERNS.md
@.planning/phases/01-safe-account-entry/01-10-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Bootstrap the versioned Fastify boundary</name>
  <files>apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/modules/auth/auth.module.ts</files>
  <action>Create Nest 11 on Fastify 5 under `/api/v1`; register cookie support before routes; apply global whitelist/transform validation, stable machine-readable errors, exact-origin credentialed CORS, correlation/redaction, environment validation, throttling, and Swagger. Import Prisma and AuthModule without implementing registration behavior.</action>
  <verify><automated>pnpm --filter api typecheck && pnpm --filter api test --run --passWithNoTests=false</automated></verify>
  <done>The API boots with versioning, validation, CORS, logging, and OpenAPI infrastructure.</done>
</task>
<task type="auto">
  <name>Task 2: Establish MailPort and SMTP adapter</name>
  <files>apps/api/src/infrastructure/mail/mail.port.ts, apps/api/src/infrastructure/mail/smtp-mail.adapter.ts, apps/api/src/modules/auth/auth.module.ts</files>
  <action>Define verification, reset, and password-changed messages on a domain-owned MailPort. Bind a Nodemailer SMTP adapter configured for Mailpit locally and fail-fast production SMTP inputs. Never log raw tokens or links, import Mailpit/provider details into AuthService, or perform delivery inside database transactions.</action>
  <verify><automated>docker compose up -d --wait mailpit && pnpm --filter api typecheck</automated></verify>
  <done>AuthModule can inject a provider-neutral mail boundary.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Information Disclosure | logs/mail | high | mitigate | Structured redaction and no secret-bearing log/message interpolation outside transport. |
| T-11-02 | Spoofing | browser origin | high | mitigate | Exact-origin credentialed CORS and POST-only mutation boundary. |
</threat_model>
<verification>API typecheck, boot tests, and Mailpit health succeed.</verification>
<success_criteria>Registration can be implemented against stable API and mail seams in five owned files.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-11-SUMMARY.md`.</output>
