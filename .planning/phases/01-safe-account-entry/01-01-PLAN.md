---
phase: 01-safe-account-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, SAFE-03, SAFE-04]
must_haves:
  truths:
    - "Dependency installation cannot begin until the user verifies the exact seam-SUS package pins, official ownership, and Expo SDK 57 compatibility."
    - "The approved stack remains Expo/React Native, NestJS/Fastify, Prisma/PostgreSQL, and the named test tools; no package substitution changes D-01 through D-17."
  artifacts:
    - path: ".planning/phases/01-safe-account-entry/01-RESEARCH.md"
      provides: "Package Legitimacy Audit and exact proposed pins"
  key_links:
    - from: ".planning/phases/01-safe-account-entry/01-RESEARCH.md"
      to: "01-02 dependency installation"
      via: "blocking human approval of every SUS package group"
---

<objective>
Gate every package-manager mutation behind the mandatory legitimacy and compatibility review.

Purpose: Prevent a too-new or misaligned Expo/Nest/Prisma patch from becoming the foundation of the walking skeleton while retaining the locked stack.
Output: A recorded blocking approval that authorizes Plan 01-02 to install the exact reviewed pins.
</objective>

## Phase Goal

**As a** user, **I want to** register, verify my email, sign in, restore my session, reset my password, sign out, and update my display name in the real client, **so that** I can securely access and retain my account session.

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-account-entry/01-RESEARCH.md
@.planning/phases/01-safe-account-entry/01-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve exact official dependency pins</name>
  <read_first>
    - .planning/phases/01-safe-account-entry/01-RESEARCH.md — Standard Stack and Package Legitimacy Audit
    - AGENTS.md — locked technology stack
  </read_first>
  <files>None — this is a non-mutating package legitimacy checkpoint.</files>
  <action>Before any install command runs, present the exact pins from the research audit as one review set. Verify npm package pages link to the official Expo, React, NestJS, Prisma, Fastify, Shopify, Lucide, TanStack, React Hook Form, Vitest, Playwright, and DefinitelyTyped repositories; verify Expo 57.0.9 resolves React 19.2.8, React Native 0.86.2, expo-router 57.0.9, expo-secure-store 57.0.1, and jest-expo 57.0.3; verify Nest core/common/platform-fastify/testing are aligned at 11.1.28; verify prisma, client, and adapter are aligned at 7.9.1; verify the argon2 package is the established node-argon2 implementation. Do not install, generate a lockfile, or accept a replacement package in this task.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/01-safe-account-entry/01-RESEARCH.md','utf8');for(const x of ['expo` | npm','@nestjs/common','prisma`, `@prisma/client`, `@prisma/adapter-pg','argon2` | npm','@playwright/test'])if(!s.includes(x))process.exit(1)"</automated>
    <human_check>Open the official npm pages for every SUS group, compare package ownership/repository and the exact version matrix above, then type `approved` only if the complete set is acceptable; otherwise name the package and approved compatible pin.</human_check>
  </verify>
  <acceptance_criteria>The executor records explicit human approval for the complete exact pin set before Plan 01-02; no package manager mutation has occurred.</acceptance_criteria>
  <done>The package legitimacy gate is approved or execution remains blocked with the disputed package identified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry metadata → workspace | Third-party package code would gain build/runtime execution after installation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | npm dependency set | high | mitigate | Blocking human review of official provenance, exact compatible pins, and aligned framework groups before any install. |
| T-01-01 | Spoofing | similarly named package | high | mitigate | Match every SUS entry to its official npm page and source repository; reject unlisted substitutes. |
| T-01-02 | Denial of Service | incompatible latest patches | medium | mitigate | Confirm Expo, Nest, and Prisma version matrices as aligned groups before generating the lockfile. |
</threat_model>

## Artifacts this phase produces

- No repository artifact; this plan produces the explicit human approval consumed by `01-02-PLAN.md`.

<verification>
Plan 01-02 must not begin until the checkpoint is approved. The research audit remains unchanged and no install/lockfile is created by this plan.
</verification>

<success_criteria>
- Every SUS package group is reviewed against its official registry/source identity.
- Exact Expo/Nest/Prisma compatibility groups are accepted or execution stops with a replacement pin decision.
- The checkpoint precedes all dependency installation.
</success_criteria>

<output>
Create `.planning/phases/01-safe-account-entry/01-01-SUMMARY.md` recording the approved pins and evidence when done.
</output>
