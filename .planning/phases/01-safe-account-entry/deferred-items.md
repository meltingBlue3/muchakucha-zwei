# Deferred Items

- Plan 01-09 sampling found that `pnpm test:quick` currently exits nonzero because the API unit project has no test files yet. This is pre-existing Wave 0 state outside Plan 01-09; API contract plans 01-05 and 01-06 own the first API tests. Both focused Playwright discovery commands for Plan 01-09 pass.
- Plan 01-21's optional registration-route regression reached the existing form but the real registration request returned the form's generic API-failure banner. The public-route bootstrap bypass worked and the required 01-21 component, browser-session, and typecheck gates pass; registration backend diagnosis remains outside this plan.
- Plan 01-24's complete API regression passed 84/85 assertions but the pre-existing concurrent email-verification test intermittently returned an undefined outcome for one request instead of the expected `used`/`verified_auto_login` pair. The focused logout, access-guard, ASVS evidence, typecheck, and OpenAPI gates all pass; verification concurrency diagnosis remains outside current-device logout scope.
