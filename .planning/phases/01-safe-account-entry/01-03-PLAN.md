---
phase: 01-safe-account-entry
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [scripts/assert-red.ps1, scripts/check-required-tests.ps1, scripts/check-openapi-drift.ps1, apps/api/package.json, apps/api/tsconfig.json, apps/api/vitest.config.ts, apps/api/test/setup-integration.ts, apps/api/test/reset-database.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, SAFE-03, SAFE-04]
must_haves:
  truths:
    - "The API runner discovers named tests without watch mode."
    - "RED verification rejects infrastructure failures and accepts only an expected missing-behavior marker."
  artifacts:
    - {path: scripts/assert-red.ps1, provides: fail-fast RED contract}
    - {path: apps/api/test/setup-integration.ts, provides: migrated disposable database harness}
  key_links:
    - {from: scripts/assert-red.ps1, to: API/client test commands, via: discovery then marker assertion}
---
<objective>Create API runner and fail-fast RED infrastructure. Purpose: make later API TDD evidence meaningful without mixing client configuration. Output: Vitest/integration helpers and RED/OpenAPI verification scripts.</objective>
## Phase Goal

**As a** user, **I want to** register, verify, sign in, restore, reset, sign out, and edit my nickname, **so that** I can securely retain and control my account session.
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-safe-account-entry/01-VALIDATION.md
@.planning/phases/01-safe-account-entry/01-PATTERNS.md
@.planning/phases/01-safe-account-entry/01-02-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Configure API integration testing and RED evidence</name>
  <read_first><file>.planning/phases/01-safe-account-entry/01-VALIDATION.md</file><file>package.json</file><file>compose.yaml</file></read_first>
  <files>scripts/assert-red.ps1, scripts/check-required-tests.ps1, scripts/check-openapi-drift.ps1, apps/api/package.json, apps/api/tsconfig.json, apps/api/vitest.config.ts, apps/api/test/setup-integration.ts, apps/api/test/reset-database.ts</files>
  <action>Configure Vitest one-shot projects and deterministic migrated PostgreSQL reset. Implement `assert-red.ps1` so it first lists/discovers the requested test, fails on missing runner/config/import/no-tests, runs the focused test, and succeeds only when output contains the caller-provided missing-behavior marker. Implement OpenAPI drift checking without error-suppressing fallbacks. Implement `check-required-tests.ps1` with the exact test paths from `01-VALIDATION.md`; it must fail on any required-file `.skip`, `.todo`, `test.todo`, `describe.skip`, or `IMPLEMENTATION_MISSING` marker and fail when an expected path is absent.</action>
  <verify><automated>pnpm --filter api exec vitest --version && pwsh -NoProfile -File scripts/assert-red.ps1 -SelfTest && pwsh -NoProfile -File scripts/check-required-tests.ps1 -SelfTest</automated></verify>
  <acceptance_criteria>Runner discovery succeeds; self-test proves infrastructure errors fail and expected assertion markers pass the RED gate.</acceptance_criteria>
  <done>API RED results cannot be mistaken for broken tooling.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Repudiation | RED evidence | high | mitigate | Discovery and expected-marker checks reject arbitrary nonzero exits. |
| T-03-SC | Tampering | dependencies | high | mitigate | Frozen approved lockfile only. |
</threat_model>
<verification>`pnpm --filter api exec vitest --version && pwsh -NoProfile -File scripts/assert-red.ps1 -SelfTest && pwsh -NoProfile -File scripts/check-required-tests.ps1 -SelfTest`.</verification>
<success_criteria>The API runner and fail-fast RED verification are operational.</success_criteria>
<output>Create `.planning/phases/01-safe-account-entry/01-03-SUMMARY.md`.</output>
