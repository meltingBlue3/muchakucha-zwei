---
phase: 02-household-member-collaboration
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/dto/update-household.dto.ts
  - apps/api/src/openapi/generate-openapi.ts
  - apps/api/test/households/households.int.test.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/client/src/features/households/household-api.ts
  - apps/client/src/features/households/household-settings.tsx
  - apps/client/app/(protected)/households/[id]/settings.tsx
  - e2e/households/rename-slice.spec.ts
autonomous: true
requirements: [HHLD-04, EXPR-02, SAFE-01]
must_haves:
  truths:
    - "Per D-04, an owner opens a real settings route whose rename form repeats 保存到：{家庭名称} and saves without an extra confirmation."
    - "Rename applies the exact trim+NFC 1-40 Unicode code-point rule used by creation and refreshes the authoritative header/result."
    - "Members, outsiders, stale roles, and cross-household route IDs cannot rename or learn inaccessible household data."
    - "Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to /households when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household."
  artifacts:
    - "apps/api/src/modules/households/dto/update-household.dto.ts"
    - "apps/client/app/(protected)/households/[id]/settings.tsx"
    - "e2e/households/rename-slice.spec.ts"
  key_links:
    - "/households/[id]/settings -> HouseholdContextNote -> generated updateHousehold -> fresh owner authorization -> authoritative refetch"
---

<objective>Deliver destination-explicit owner rename as its own complete settings slice per D-04.</objective>
## Scope Exception

**Accepted expanded scope: exactly 12 concrete files.** The unavoidable generated OpenAPI artifacts are `packages/api-client/openapi.json`, `packages/api-client/src/generated/models.ts`, and `packages/api-client/src/generated/client.ts`; the cross-platform test surfaces are `apps/api/test/households/households.int.test.ts` and `e2e/households/rename-slice.spec.ts`. Splitting this rename journey would create horizontal API/client layers and violate the phase's MVP vertical-slice rules. Execution is bounded to exactly two tasks, Plan 02-04 has exclusive Wave 4 ownership, and dependency sequencing ensures no overlapping concurrent plan or editor touches an owned file. Roster reads remain green from Plan 02-03 while this plan owns only rename behavior.

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-03-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish the dedicated rename RED contract</name>
  <files>e2e/households/rename-slice.spec.ts</files>
  <behavior>
    - Exactly one named test opens the real settings route and observes the current destination note.
    - Owner rename reaches only the missing behavior marker; member/outsider authorization fixtures are healthy first.
    - No extra confirmation appears for this ordinary save.
  </behavior>
  <action>Create exactly one Playwright test named `renames the explicit current household [RED:HOUSEHOLD_RENAME]` with marker `IMPLEMENTATION_MISSING_HOUSEHOLD_RENAME`. Add a static exactly-one-test guard before the existing RED helper and keep all other rename integration cases outside this file.</action>
  <verify><automated>$count = (rg -n "^\s*test\(" e2e/households/rename-slice.spec.ts | Measure-Object).Count; if ($count -ne 1) { throw "rename-slice.spec.ts must contain exactly one test" }; powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/rename-slice.spec.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_RENAME</automated></verify>
  <done>The exact destination-explicit rename journey is the only RED behavior in its file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement owner-authorized direct rename</name>
  <files>apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/dto/update-household.dto.ts, apps/api/src/openapi/generate-openapi.ts, apps/api/test/households/households.int.test.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, apps/client/src/features/households/household-api.ts, apps/client/src/features/households/household-settings.tsx, apps/client/app/(protected)/households/[id]/settings.tsx, e2e/households/rename-slice.spec.ts</files>
  <action>Use the classified `update-me.dto.ts`, guarded controller, household service, OpenAPI generator, and real integration analogs. Add `updateHousehold` with exact operationId/schema/security assertions, derive actor membership fresh inside the target household, require current owner, normalize the name identically to create, and return the authoritative household projection. Regenerate the three changed outputs and extend the narrow wrapper. Implement the thin settings route and form using owned primitives plus `HouseholdContextNote`; show “保存到：{家庭名称}”, submit directly, retain input on recoverable failure, replace header/form values only from the response, and distinguish a still-member permission change from membership loss. Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to `/households` when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household. Do not add a confirmation page to rename. Remove the marker only after focused integration and the dedicated browser test pass.</action>
  <verify><automated>pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts --testNamePattern "renames"; pnpm --filter client test:quick -- household-settings; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/rename-slice.spec.ts</automated></verify>
  <done>An owner renames the explicitly identified household directly, and unauthorized or invalid requests change nothing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| settings form -> household API | Route ID, cached role, and name are untrusted. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-10 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | Fresh owner function/item authorization executes at the service. |
| T-02-11 | Tampering | medium | mitigate | V8.3.1 | Stale cached roles and cross-household IDs cannot authorize update. |
| T-02-12 | Information Disclosure | high | mitigate | V15.3.1 | Unauthorized responses contain no household projection. |
| T-02-SC | Tampering | low | accept | N/A | No dependency or lockfile change. |
</threat_model>
<verification>Rename integration, asserted OpenAPI, wrapper/component/type, and real settings-route browser gates pass.</verification>
<success_criteria>The owner can rename the clearly identified destination without an unnecessary confirmation or isolation regression.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-04-SUMMARY.md` when done.</output>
