---
phase: 02-household-member-collaboration
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0003_household_invitations/migration.sql
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/dto/invitation.dto.ts
  - apps/api/src/infrastructure/mail/mail.port.ts
  - apps/api/src/infrastructure/mail/smtp-mail.adapter.ts
  - apps/api/test/households/invitations.int.test.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/{openapi.json,src/generated/models.ts,src/generated/client.ts,src/generated/index.ts}
autonomous: true
requirements: [HHLD-02, EXPR-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Per D-05/D-06, owner/admin can invoke the email-only member-invitation REST operation and every non-member identity receives the same accepted response."
    - "A repeated pending invitation rotates its hash and exact seven-day expiry before post-commit mail delivery."
    - "Raw invitation tokens exist only in the delivered URL, never persistence, response DTOs, or logs."
  artifacts:
    - "apps/api/prisma/migrations/0003_household_invitations/migration.sql"
    - "apps/api/src/modules/households/dto/invitation.dto.ts"
  key_links:
    - "settings form -> guarded create invitation -> committed hash/state -> MailPort raw-token URL"
---

<objective>Deliver the privacy-preserving invitation-send REST -> PostgreSQL -> mail increment per D-05/D-06, usable through `/api/v1` before the visible client journey.</objective>
<scope_rationale>This dependency-sequenced transport/domain increment is directly usable by an authenticated REST client and owns exactly 13 concrete files after brace expansion. The visible settings journey is Plan 02-05; list/status/resend/revoke remain Plan 02-07.</scope_rationale>
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-03-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate privacy-preserving invitation-send RED contracts</name>
  <files>apps/api/test/households/invitations.int.test.ts</files>
  <behavior>
    - D-05: request contains email only and server fixes new membership role to member.
    - D-06: registered, absent, and repeated targets share status/body/copy; only an existing household member gets the permitted explicit error.
    - Token persistence is a unique lowercase SHA-256 hash with exact seven-day expiry.
  </behavior>
  <action>Create one uniquely named Fastify/PostgreSQL RED case `sends a privacy-preserving member invitation [RED:INVITATION_SEND_API]` with marker `IMPLEMENTATION_MISSING_INVITATION_SEND_API`. Reach it only after current-household authorization and deterministic test mail transport are healthy.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite api -TestPath test/households/invitations.int.test.ts -TestName "sends a privacy-preserving member invitation [RED:INVITATION_SEND_API]" -MissingBehaviorMarker IMPLEMENTATION_MISSING_INVITATION_SEND_API</automated></verify>
  <done>The send-invitation slice has truthful, focused RED evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Migrate and implement invitation sending through the mail boundary</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0003_household_invitations/migration.sql, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/dto/invitation.dto.ts, apps/api/src/infrastructure/mail/mail.port.ts, apps/api/src/infrastructure/mail/smtp-mail.adapter.ts, apps/api/test/households/invitations.int.test.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/{openapi.json,src/generated/models.ts,src/generated/client.ts,src/generated/index.ts}</files>
  <action>Resolve classified `invitation.dto.ts` with the PATTERNS.md `register.dto.ts` role-match analog; also follow AccessTokenGuard/operation IDs from `auth.controller.ts`, opaque-token/Serializable mechanics from `auth.service.ts`, the typed mail port/adapter, generated assertions, and `me.int.test.ts`. Add Invitation persistence in `0003_household_invitations` with household/inviter FKs, canonical email, unique hash, lifecycle timestamps/checks, and one active pending invite per household/email. Apply Prisma Migrate and prove zero drift. Implement owner/admin-only send, not lifecycle listing: generate 32 random bytes, persist only SHA-256, rotate a predecessor transactionally, commit before mail, and return the same D-06 response for registered/absent/repeated recipients except the authorized existing-member case. Assert the exact send operationId and DTO schemas before generated output. Plan 02-07 owns lifecycle list/status/resend/revoke.</action>
  <verify><automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm --filter api prisma:generate; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/invitations.int.test.ts</automated></verify>
  <done>An authenticated owner/admin REST client can send a seven-day member invitation without disclosure or stored bearer token.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| manager client -> invite API | Email and household ID are untrusted; role is server-fixed. |
| API -> mail | Raw bearer material crosses only after durable commit. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-10 | Information Disclosure | high | mitigate | V15.3.1 | Registered/absent/repeated same-response tests prove only the required response subset is returned. |
| T-02-11 | Spoofing | high | mitigate | V11.4.1 (L1), V11.5.1 (L2 defense-in-depth) | Tests name approved SHA-256 use plus at least 128-bit CSPRNG invitation entropy without conflating levels. |
| T-02-12 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | Fresh owner/admin function and same-household item checks run at the trusted service; role is server-fixed. |
| T-02-13 | Tampering | high | mitigate | V2.3.3 (L2 defense-in-depth) | Serializable rotation and rollback assertions prove all-or-nothing behavior. |
| T-02-SC | Tampering | low | accept | N/A | Existing crypto/mail dependencies only; no install. |
</threat_model>
<verification>Migration/drift, privacy integration, mail, generated contract, client, and type gates pass.</verification>
<success_criteria>Invitation send is usable, generic, hash-only, role-fixed, and post-commit delivered.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-04-SUMMARY.md` when done.</output>
