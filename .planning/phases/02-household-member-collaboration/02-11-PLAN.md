---
phase: 02-household-member-collaboration
plan: 11
type: execute
wave: 11
depends_on: [02-10]
files_modified:
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/test/households/governance.int.test.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/client/src/features/households/member-governance.tsx
  - apps/client/src/features/households/__tests__/owner-leave-test.tsx
  - apps/client/app/(protected)/households/[id]/ownership/leave.tsx
  - e2e/households/owner-leave-slice.spec.ts
autonomous: true
requirements: [HHLD-08, HHLD-09, EXPR-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Per D-11, owner leave selects an existing successor and atomically moves owner pointer plus deletes former membership."
    - "The flow uses successor selection, consequence page, and safe-default final confirmation."
    - "Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to /households when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household."
  artifacts:
    - "apps/client/app/(protected)/households/[id]/ownership/leave.tsx"
    - "apps/client/src/features/households/__tests__/owner-leave-test.tsx"
  key_links:
    - "final leave confirmation -> locked owner pointer update -> former membership delete -> commit -> D-12 context recovery"
---
<objective>Deliver the D-11 owner-leave handoff as a complete atomic and recoverable slice.</objective>
<scope_rationale>This is one vertical owner-leave increment across atomic handoff/delete, generated contract, staged UI, D-12 recovery, and E2E proof. Plan 02-11 exclusively owns its Wave 11 edits; shared files are dependency-sequenced.</scope_rationale>
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-10-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate owner-leave RED contracts</name>
  <files>e2e/households/owner-leave-slice.spec.ts</files>
  <behavior>
    - D-11 transfer-and-delete commits wholly or not at all under failure/concurrency.
    - Safe-default three-stage leave flow makes zero mutation until final action.
    - Successful former owner enters accessChanged first; only an explicit action then opens the selector or equal D-01 handoff, never login or another household automatically.
  </behavior>
  <action>Create `owner-leave-slice.spec.ts` with exactly one test `owner hands off and leaves [RED:OWNER_LEAVE]` and marker `IMPLEMENTATION_MISSING_OWNER_LEAVE` after transfer/context prerequisites pass. Add the static exactly-one-test guard; keep concurrency/database and component confirmation cases in their classified files.</action>
  <verify><automated>$count = (rg -n "^\s*test\(" e2e/households/owner-leave-slice.spec.ts | Measure-Object).Count; if ($count -ne 1) { throw "owner-leave-slice.spec.ts must contain exactly one test" }; powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/owner-leave-slice.spec.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_OWNER_LEAVE</automated></verify>
  <done>The D-11/D-12 owner-leave contract is truthfully RED.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement atomic owner handoff-and-leave with recovery</name>
  <files>apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/test/households/governance.int.test.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, apps/client/src/features/households/member-governance.tsx, apps/client/src/features/households/__tests__/owner-leave-test.tsx, apps/client/app/(protected)/households/[id]/ownership/leave.tsx, e2e/households/owner-leave-slice.spec.ts</files>
  <action>Follow PATTERNS.md AccessTokenGuard/operation-ID analog from `auth.controller.ts`, Serializable mechanics from `auth.service.ts`, explicit no-direct-analog owner-invariant guidance, `me.int.test.ts` rollback DB assertions, `generate-openapi.ts` pre-write assertions, owned `FinalConfirmation`, and session-bootstrap D-12 routing analog. Add owner-only leave with `successorMembershipId`. In one Serializable transaction lock Household, verify actor is current owner and successor is a distinct same-household membership, conditionally move `ownerMembershipId` to successor, then delete the former membership; the deferred composite FK and non-null pointer validate at commit. Concurrent transfer/leave attempts serialize and compare current pointer; failures roll back pointer and membership together. Assert exact owner-leave operationId and DTO schemas before generated client output. Implement D-11 successor selection, consequence page, exact safe-default final dialog, pending locks, and no optimistic changes. After 204 apply D-12 exactly: membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to `/households` when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household and never presents login expiry.</action>
  <verify><automated>pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/governance.int.test.ts --testNamePattern "owner leaves"; pnpm --filter client test:quick -- owner-leave-test household-context-test; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/owner-leave-slice.spec.ts</automated></verify>
  <done>Owner leave atomically hands off ownership, removes the former membership, and recovers routing without login confusion.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| owner leave UI -> ownership API | Successor and cached owner state are untrusted. |
| commit -> client recovery | Access removal must not leave stale data or masquerade as session expiry. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-30 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | Fresh owner function and same-household successor-item checks execute at the trusted service. |
| T-02-31 | Tampering | high | mitigate | V2.3.3 (L2 defense-in-depth) | Locked pointer move plus delete, deferred FK, concurrency, and DB rollback prove atomic leave. |
| T-02-32 | Information Disclosure | high | mitigate | V14.3.1 | D-12 scoped-cache/persistence clearing and no-stale-content browser assertions cover terminated household access. |
| T-02-SC | Tampering | low | accept | N/A | Existing final-confirmation and context stack; no install. |
</threat_model>
<verification>Owner-leave integration, concurrency, component, context, browser, OpenAPI, and type gates pass.</verification>
<success_criteria>An owner can leave only through a complete successor handoff, and no race or failure yields zero owner or partial removal.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-11-SUMMARY.md` when done.</output>
