---
phase: 02-household-member-collaboration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/client/src/features/households/household-api.ts
  - apps/client/src/features/households/household-context.tsx
  - apps/client/src/features/auth/session-bootstrap.tsx
  - apps/client/app/(protected)/household-handoff.tsx
  - apps/client/app/(protected)/households/index.tsx
  - apps/client/src/ui/household-components.tsx
  - apps/client/src/ui/theme.ts
  - e2e/households/context-slice.spec.ts
autonomous: true
requirements: [HHLD-04, EXPR-02, SAFE-01]
must_haves:
  truths:
    - "A guarded listMyHouseholds endpoint, response DTO, stable operationId assertion, generated operation, typed household-api wrapper, and authorization tests exist before HouseholdProvider or /households consume it."
    - "Per D-02/D-03, authenticated startup lists all current memberships, restores only a valid device-local choice, and switches only after membership plus the first authoritative destination query succeed."
    - "Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to /households when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household."
    - "HouseholdCard is implemented in the owned UI layer and is consumed by both the D-01 handoff and /households selection route."
  artifacts:
    - "apps/api/src/modules/households/households.controller.ts#listMyHouseholds"
    - "apps/client/src/features/households/household-api.ts"
    - "apps/client/src/features/households/household-context.tsx"
    - "apps/client/src/ui/household-components.tsx#HouseholdCard"
    - "apps/client/app/(protected)/households/index.tsx"
    - "e2e/households/context-slice.spec.ts"
  key_links:
    - "AccessTokenGuard claims -> listMyHouseholds service membership lookup -> minimal list DTO -> asserted OpenAPI -> generated client -> household-api wrapper -> HouseholdProvider"
    - "SessionBootstrap -> HouseholdProvider resolution -> validated device preference -> /households selection state"
    - "membership loss/403 -> freeze -> scoped cache+persistence clear -> accessChanged explanation -> explicit user action -> selector or D-01"
    - "HouseholdCard -> D-01 handoff and /households selection"
---

<objective>
Deliver the complete list, restore, switch, and access-loss slice through a newly owned guarded API producer and the real `/households` route.

Purpose: Make household context server-authoritative and device-local while resolving D-12 through an explicit explanation state rather than silent fallback.
Output: List-my-households contract/client/wrapper, household state machine, shared cards/header/switcher, restored D-01 handoff, selector route, theme tokens, and one dedicated browser contract.
</objective>

## Scope Exception

**Accepted expanded scope: exactly 14 concrete files.** The unavoidable generated OpenAPI artifacts are `packages/api-client/openapi.json`, `packages/api-client/src/generated/models.ts`, and `packages/api-client/src/generated/client.ts`; the cross-platform state and journey surfaces are `apps/client/src/features/auth/session-bootstrap.tsx`, `apps/client/src/features/households/household-context.tsx`, and `e2e/households/context-slice.spec.ts`. Splitting this journey would create horizontal API/client layers and violate the phase's MVP vertical-slice rules. Execution is bounded to exactly two tasks, Plan 02-02 has exclusive Wave 2 ownership, and dependency sequencing ensures no overlapping concurrent plan or editor touches an owned file. The minimal list DTO remains controller-colocated and device-preference adapters remain internal typed seams to avoid nonessential file expansion.

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the dedicated context-slice RED contract</name>
  <files>e2e/households/context-slice.spec.ts</files>
  <behavior>
    - The file contains exactly one test named `lists restores switches and explains access loss [RED:HOUSEHOLD_CONTEXT]`.
    - The test signs in, creates two households through the green slice, verifies the list operation as the same actor and denial as another actor, then exercises restart, switch, and membership loss.
    - D-12 is exact: freeze actions, clear lost-household cache/persistence, show accessChanged first, and require a user action before selector or equal D-01 handoff; no automatic valid-household entry occurs.
  </behavior>
  <action>Create one Playwright test with exact marker `IMPLEMENTATION_MISSING_HOUSEHOLD_CONTEXT` and one `test(...)` declaration. Use its authenticated browser plus request context to cover the guarded list producer and UI consumer in the same real slice. Add the static one-test guard before existing `assert-red.ps1`; do not mix unrelated cases into this RED file.</action>
  <verify><automated>$count = (rg -n "^\s*test\(" e2e/households/context-slice.spec.ts | Measure-Object).Count; if ($count -ne 1) { throw "context-slice.spec.ts must contain exactly one test" }; powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/context-slice.spec.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_CONTEXT</automated></verify>
  <done>One exact test supplies RED evidence for the producer, wrapper, provider, selector, switch, and accessChanged behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce and consume the guarded household list vertically</name>
  <files>apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, apps/client/src/features/households/household-api.ts, apps/client/src/features/households/household-context.tsx, apps/client/src/features/auth/session-bootstrap.tsx, apps/client/app/(protected)/household-handoff.tsx, apps/client/app/(protected)/households/index.tsx, apps/client/src/ui/household-components.tsx, apps/client/src/ui/theme.ts, e2e/households/context-slice.spec.ts</files>
  <behavior>
    - The list response contains only the actor's household ID/name, textual role, member count, and owner membership identity required by context validation; client-supplied user/current-household IDs never authorize it.
    - Current household sorts first; remaining households sort by successful access UTC epoch milliseconds descending, NFC locale/name ascending, then stable household UUID code-point ascending.
    - A network switch failure retains the prior verified household; a membership loss always enters accessChanged first and never silently selects another household.
  </behavior>
  <action>Implement guarded GET `/api/v1/households` with operationId `listMyHouseholds`; export the smallest explicit Swagger DTOs from the controller, derive the actor from AccessTokenGuard claims, query Membership server-side, and return a minimal stable projection. Before generation, assert the path, operationId, bearer security, and DTO schemas in `generate-openapi.ts`; regenerate the three changed client outputs. Then implement `household-api.ts` as a narrow `Pick<ApiClient, 'listMyHouseholds'>` wrapper and map stable API failures before any provider use. In `household-context.tsx`, add typed device-preference seams: native uses the already-approved SecureStore as non-secret storage and Web uses device-local localStorage; persist current ID plus successful-access timestamps only after list validation and the first authoritative query, and never sync them. Implement `resolving | ready | noHousehold | offlineRetained | accessChanged`, household-prefixed query keys, the exact total comparator, and atomic switch publication. Extend SessionBootstrap so authenticated startup waits for this resolution without handoff/stale flashes. Add UI-SPEC theme tokens, then explicitly implement `AppShell`, `HouseholdHeader`, `HouseholdSwitcher`, and `HouseholdCard` in the owned component layer. Refactor both `/household-handoff` and `/households` to consume HouseholdCard; `/households` renders the authoritative list/current marker/device explanation/create action and performs switch without claiming `/households/[id]`. Per D-12, on membership loss freeze actions, clear only the lost household cache and stored selection, render the accessChanged explanation inside the owned `/households` route first, and expose exactly one subsequent user action: reveal selector state when memberships remain or navigate to D-01 when none remain. Never auto-select, auto-enter, or silently switch to another membership. Remove the marker only after the dedicated test passes.</action>
  <verify><automated>pnpm openapi:generate; pnpm openapi:check; pnpm --filter api typecheck; pnpm --filter client test:quick -- session-bootstrap-test; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/context-slice.spec.ts</automated></verify>
  <done>The guarded list producer, generated operation, wrapper, context provider, card consumers, selector, and exact D-12 explanation flow are green as one usable slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| device preference -> client context | Stored IDs/timestamps are hints and never authorization. |
| authenticated client -> list API | The server derives the subject and returns only its memberships. |
| bootstrap -> protected router | Household content cannot publish before membership validation. |

## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-04 | Spoofing | high | mitigate | V8.2.2, V8.3.1 | Cross-actor list assertions and restore tests require trusted server membership validation. |
| T-02-05 | Information Disclosure | high | mitigate | V14.3.1 | No-flash and D-12 tests prove lost household cache/persistence is cleared before explanation. |
| T-02-06 | Tampering | medium | mitigate | V8.3.1 | Corrupted device IDs/timestamps cannot create ready state or change server list membership. |
| T-02-SC | Tampering | low | accept | N/A | Existing approved storage/query/router stack only; no install. |
</threat_model>

<verification>One-test RED semantics, OpenAPI producer assertions, generated-wrapper typechecks, authenticated list isolation, restart/relogin, total ordering, atomic switch, both HouseholdCard consumers, and explicit D-12 browser behavior pass.</verification>
<success_criteria>A returning member sees and switches only their households, restores only a valid device choice, and always receives an explicit access-change explanation before choosing where to continue.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-02-SUMMARY.md` when done.</output>
