---
phase: 02-household-member-collaboration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/client/src/platform/household/current-household.ts
  - apps/client/src/platform/household/current-household.native.ts
  - apps/client/src/platform/household/current-household.web.ts
  - apps/client/src/features/households/household-context.tsx
  - apps/client/src/features/households/__tests__/household-context-test.tsx
  - apps/client/src/features/auth/session-bootstrap.tsx
  - apps/client/app/_layout.tsx
  - apps/client/app/(protected)/household-handoff.tsx
  - apps/client/app/(protected)/households/index.tsx
  - apps/client/src/ui/household-components.tsx
  - e2e/households/collaboration.spec.ts
autonomous: true
requirements: [HHLD-04, EXPR-02, SAFE-01]
must_haves:
  truths:
    - "Per D-02/D-03, login, relogin, and restart restore the last valid household for that device without flashing the Phase 1 handoff."
    - "The current household is fixed first; remaining households use persisted UTC epoch-millisecond access time descending, UI-SPEC locale/name ascending, then stable household UUID ascending for a total order."
    - "A stale selection freezes old actions, clears only that household cache, and routes to access-changed or D-01 when no households remain."
  artifacts:
    - "apps/client/src/features/households/household-context.tsx"
    - "apps/client/src/features/auth/session-bootstrap.tsx"
    - "apps/client/app/_layout.tsx"
    - "apps/client/app/(protected)/households/index.tsx"
  key_links:
    - "SessionBootstrap -> HouseholdProvider resolution -> validated device selection -> protected route"
    - "successful first household query -> current household first -> remaining access DESC, locale/name ASC, household UUID ASC total ordering"
---

<objective>Deliver authenticated startup, restore, switch, and stale-membership recovery as a usable device-local household context slice per D-02, D-03, and D-12.</objective>
<scope_rationale>The files form one vertical authenticated-startup increment across platform storage, provider, root routing, owned switcher UI, and its browser proof. Plan 02-02 exclusively owns this Wave 2 change; overlap with Plan 02-01 is dependency-sequenced.</scope_rationale>

<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate startup restore and switch RED contracts</name>
  <files>apps/client/src/features/households/__tests__/household-context-test.tsx, e2e/households/collaboration.spec.ts</files>
  <behavior>
    - D-03: restart and relogin restore a valid device-local choice; an invalid or lost membership never renders stale content.
    - D-02: switching validates membership and the first new-household query before publishing the new header/data together.
    - D-12: a stale 403 leads to access-changed, another valid household, or D-01 dual entry without a login-expired message.
  </behavior>
  <action>Write one uniquely named Playwright RED case `lists restores and switches households [RED:HOUSEHOLD_STARTUP]` plus focused component cases for restart, relogin, invalid persisted selection, two independent browser contexts, switch rollback, stale membership, and the `/households` selection route. Use exact marker `IMPLEMENTATION_MISSING_HOUSEHOLD_STARTUP` after real Phase 1 session restoration and Plan 02-01 routing succeed.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/collaboration.spec.ts -TestName "lists restores and switches households [RED:HOUSEHOLD_STARTUP]" -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_STARTUP</automated></verify>
  <done>The startup/switch contracts are discovered and only the exact absent-household-startup marker is accepted as RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire HouseholdProvider into authenticated startup and routing</name>
  <files>apps/client/src/platform/household/current-household.ts, apps/client/src/platform/household/current-household.native.ts, apps/client/src/platform/household/current-household.web.ts, apps/client/src/features/households/household-context.tsx, apps/client/src/features/auth/session-bootstrap.tsx, apps/client/app/_layout.tsx, apps/client/app/(protected)/household-handoff.tsx, apps/client/app/(protected)/households/index.tsx, apps/client/src/ui/household-components.tsx, apps/client/src/features/households/__tests__/household-context-test.tsx, e2e/households/collaboration.spec.ts</files>
  <action>Following PATTERNS.md, use `session-transport.{ts,native.ts,web.ts}` as the platform-storage analog, `session-bootstrap.tsx` as the explicit state-machine/routing analog, `primitives.tsx` as the owned UI analog, and the thin `profile.tsx` route pattern for `/households`. Create a non-secret `CurrentHouseholdStore` record containing `currentHouseholdId` and `successfulAccessAtByHousehold`, persisted as UTC epoch milliseconds; keep it separate from refresh/session secret transport on native and Web. Update a timestamp only after membership validation and the first authoritative query succeed. Implement the UI-SPEC comparator with current household fixed first; remaining households use access timestamp descending, locale-aware normalized name ascending, then household UUID/code-point ascending. Implement `HouseholdProvider` states and household-prefixed query keys. Mount it after authentication and route no-household users to D-01 handoff, while stale membership routes explicitly to `/households` when alternatives exist. Implement the thin `/households` route as the complete membership selection screen with current marker, device-restore explanation, create link to `/households/new`, and authoritative switch action. During switch publish header/content atomically; network failure retains the prior selection, while stale membership freezes actions, clears only lost-household state, and follows D-12. Add owned `AppShell`, `HouseholdHeader`, and `HouseholdSwitcher` behavior required by D-02, consuming the theme tokens established in 02-01.</action>
  <verify><automated>pnpm --filter client test:quick -- household-context-test session-bootstrap-test; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/collaboration.spec.ts --grep "restores and switches|stale membership"</automated></verify>
  <done>Restart/relogin restoration, recency ordering, atomic switching, and stale-access routing work through the actual authenticated root.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| device preference -> client context | Stored household IDs and timestamps are hints, never authorization. |
| authenticated bootstrap -> protected router | No protected household route may render before membership validation. |

## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-04 | Spoofing | high | mitigate | V8.2.2, V8.3.1 | Invalid-selection and relogin tests require fresh server-side item authorization before ready state. |
| T-02-05 | Information Disclosure | high | mitigate | V14.3.1 | Switch/no-flash and scoped-cache tests prove authenticated household data clears when access terminates. |
| T-02-06 | Tampering | medium | mitigate | V8.3.1, V2.2.2 | State-machine tests treat device selection as a hint and require trusted-service validation before publication. |
| T-02-SC | Tampering | low | accept | N/A | No dependency installation; approved storage/query/router stack only. |
</threat_model>

<verification>Focused component, session bootstrap, typecheck, and restart/relogin Playwright gates pass.</verification>
<success_criteria>A returning member reaches the last valid device household without handoff flashes, and stale access recovers explicitly.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-02-SUMMARY.md` when done.</output>
