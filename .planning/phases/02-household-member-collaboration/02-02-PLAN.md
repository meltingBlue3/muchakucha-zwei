---
phase: 02-household-member-collaboration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0002_household_core/migration.sql
  - apps/api/src/modules/households/household-policy.ts
  - apps/api/src/modules/households/dto/create-household.dto.ts
  - apps/api/src/modules/households/dto/household.dto.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.module.ts
  - apps/api/src/app.module.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - packages/api-client/src/generated/index.ts
  - apps/client/src/features/households/household-api.ts
  - apps/client/src/features/households/household-entry.tsx
  - apps/client/app/(protected)/household-handoff.tsx
  - apps/client/app/(protected)/households/new.tsx
requirements: [HHLD-01, HHLD-04, HHLD-05, EXPR-02, SAFE-01, SAFE-02]
autonomous: true
must_haves:
  truths:
    - "An authenticated user creates a 1–40-character household and becomes its sole owner atomically."
    - "The no-household screen offers equal create and accept-invitation entries, and creation lands in the new household with its name visible."
    - "Only a member can list or read a household and its member projection."
  artifacts:
    - "apps/api/prisma/migrations/0002_household_core/migration.sql"
    - "apps/api/src/modules/households/households.service.ts"
    - "apps/client/src/features/households/household-entry.tsx"
  key_links:
    - "CreateHouseholdDto -> HouseholdsService.create -> serializable Household+Membership write"
    - "generated createHousehold/listHouseholds/getHouseholdMembers -> household-entry route"
---

<objective>
Deliver the first real UI → API → PostgreSQL household slice: create a household, become owner, and see the authoritative household/member state.

Purpose: Replace the Phase 1 handoff with the first usable collaboration capability while establishing the server-owned isolation boundary.
Output: Migrated Household/Membership schema, guarded REST/OpenAPI operations, generated client, and creation UI.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/01-safe-account-entry/01-10-SUMMARY.md
@.planning/phases/01-safe-account-entry/01-19-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement atomic household creation and the no-household entry slice</name>
  <files>apps/api/prisma/schema.prisma, apps/api/src/modules/households/household-policy.ts, apps/api/src/modules/households/dto/create-household.dto.ts, apps/api/src/modules/households/dto/household.dto.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.module.ts, apps/api/src/app.module.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, packages/api-client/src/generated/index.ts, apps/client/src/features/households/household-api.ts, apps/client/src/features/households/household-entry.tsx, apps/client/app/(protected)/household-handoff.tsx, apps/client/app/(protected)/households/new.tsx</files>
  <read_first>
    - apps/api/prisma/schema.prisma
    - apps/api/src/modules/auth/auth.controller.ts
    - apps/api/src/modules/auth/auth.service.ts
    - apps/api/src/modules/users/users.module.ts
    - apps/api/src/app.module.ts
    - apps/api/src/openapi/generate-openapi.ts
    - packages/api-client/src/generated/client.ts
    - apps/client/src/features/profile/profile-form.tsx
    - apps/client/app/(protected)/household-handoff.tsx
    - apps/client/src/ui/primitives.tsx
    - .planning/phases/02-household-member-collaboration/02-CONTEXT.md
    - .planning/phases/02-household-member-collaboration/02-PATTERNS.md
    - .planning/phases/02-household-member-collaboration/02-UI-SPEC.md
  </read_first>
  <behavior>
    - HHLD-01/D-01: POST `/api/v1/households` trims a 1–40 Unicode-character name and atomically creates Household plus owner Membership from `request.auth.sub`.
    - HHLD-04/05/SAFE-01: GET `/api/v1/households` and GET `/api/v1/households/:householdId/members` return explicit DTO projections only for verified memberships.
    - EXPR-02/D-02/D-04: the created household name is visible on landing and household-bound forms render `保存到：{家庭名称}`.
  </behavior>
  <action>Extend Prisma `User` with memberships and add `Household`, `Membership`, and `HouseholdRole` using UUID FKs, `timestamptz(3)`, unique `(householdId,userId)`, indexed user membership, and explicit cascade/restrict semantics. Add database-ready owner invariant support and a 1–40 nonblank Unicode name policy. Register `HouseholdsModule` with the existing configured `AuthModule`; expose `createHousehold`, `listHouseholds`, and `getHouseholdMembers` operation IDs under `/api/v1/households`. `HouseholdsService.create` must use a serializable transaction and return only DTOs. Generate the client rather than hand-editing generated output. Replace the existing handoff screen per D-01 with equal `创建家庭` and `接受邀请` cards, implement the name form with owned primitives, stable pending feedback, and authoritative post-create navigation. No package changes and no Phase 3 content.</action>
  <verify>
    <automated>MISSING — Task 2 is the required blocking Prisma Migrate application gate and owns all behavioral verification after schema edits.</automated>
  </verify>
  <acceptance_criteria>
    - Source defines `Household`, `Membership`, `HouseholdRole`, `CreateHouseholdDto`, `HouseholdDto`, `MembershipDto`, `HouseholdsService`, `HouseholdsController`, and `HouseholdsModule`.
    - Controller actor identity comes only from `AccessTokenGuard`; no DTO accepts actor user ID or role authority.
    - The no-household route exposes the exact equal-level actions from D-01 and the creation form validates 1–40 characters.
  </acceptance_criteria>
  <done>The complete creation UI/API/schema slice exists and is ready for the mandatory migration gate.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Create review apply and validate the household-core Prisma migration</name>
  <files>apps/api/prisma/migrations/0002_household_core/migration.sql, apps/api/prisma/schema.prisma, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, packages/api-client/src/generated/index.ts</files>
  <read_first>
    - apps/api/prisma/schema.prisma
    - apps/api/prisma/migrations/0002_household_core/migration.sql (create; target file)
    - apps/api/prisma/migrations/0001_auth_foundation/migration.sql
    - apps/api/prisma.config.ts
    - apps/api/test/setup-integration.ts
    - compose.yaml
    - apps/api/package.json
    - apps/api/test/households/households.int.test.ts
  </read_first>
  <action>Start the repository PostgreSQL 18 service with `docker compose up -d --wait postgres`; set the disposable loopback test `DATABASE_URL`; create `0002_household_core` through `pnpm --filter api exec prisma migrate dev --name household_core`, review and augment its SQL with named CHECK/UNIQUE/FK constraints Prisma cannot express, then apply with `prisma migrate deploy`. Require exactly one owner after the create transaction and reject blank/overlong names at PostgreSQL. Run `prisma migrate status`, a schema diff exit-code check, Prisma generation, OpenAPI generation/drift, the focused household integration suite, client tests, and the browser happy path. Do not use `prisma db push`.</action>
  <verify>
    <automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm --filter api prisma:generate; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts; pnpm --filter client test:quick; pnpm exec playwright test e2e/households/collaboration.spec.ts --grep "creates a household"</automated>
  </verify>
  <acceptance_criteria>
    - `prisma migrate status` reports the test database current with committed `0002_household_core` history.
    - `prisma migrate diff ... --exit-code` returns zero; no `db push` command appears in execution evidence.
    - Creation integration tests prove one Household and one owner Membership commit together, invalid names fail, and outsider reads return 403 without data.
    - The focused Playwright creation journey passes and the header shows the created name.
  </acceptance_criteria>
  <done>The household-core migration is committed, applied, drift-free, and the real creation journey is green.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Household`, `Membership`, `HouseholdRole` — Prisma domain symbols in `apps/api/prisma/schema.prisma`.
- `HouseholdsModule`, `HouseholdsController`, `HouseholdsService` — guarded household boundary in `apps/api/src/modules/households/`.
- `createHousehold`, `listHouseholds`, `getHouseholdMembers` — generated operations in `packages/api-client/src/generated/client.ts`.
- `HouseholdEntry` — dual-entry and creation composition in `apps/client/src/features/households/household-entry.tsx`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated client → household API | Names and route household IDs are untrusted; subject is guard-derived. |
| service → PostgreSQL | Household and first owner membership must commit as one unit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Elevation of Privilege | create/list/member endpoints | high | mitigate | Reuse AccessTokenGuard and resolve membership by guard subject plus route ID inside each service method. |
| T-02-05 | Tampering | household creation | high | mitigate | Serializable Household+owner Membership transaction plus DB constraints and rollback test. |
| T-02-06 | Information Disclosure | member projection | high | mitigate | Explicit DTO projection after membership authorization; cross-household integration test. |
| T-02-SC | Tampering | package boundary | low | accept | No install task; approved lockfile remains unchanged. |
</threat_model>

<verification>Migration history, drift, OpenAPI, integration, client, and focused browser gates all pass in that order.</verification>
<success_criteria>A new authenticated user creates a durable household, becomes owner, and reaches an isolated household context through the real client.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-02-SUMMARY.md` when done.</output>
