---
phase: 02-household-member-collaboration
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - apps/api/src/modules/households/dto/invitation.dto.ts
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/openapi/generate-openapi.ts
  - apps/api/test/households/invitations.int.test.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - packages/api-client/src/generated/index.ts
  - apps/client/src/features/auth/session-bootstrap.tsx
  - apps/client/src/features/households/invitation-flow.tsx
  - apps/client/src/features/households/__tests__/invitation-flow-test.tsx
  - apps/client/app/invite/[token].tsx
requirements: [HHLD-03, HHLD-04, HHLD-05, SAFE-01, SAFE-02]
autonomous: true
must_haves:
  truths:
    - "A valid invite preview reveals only household name and inviter before auth, returns after login/registration, and never auto-accepts."
    - "Only an authenticated account whose canonical email matches can explicitly accept once."
    - "Successful acceptance atomically consumes the invite, creates one member relation, selects the household on this device, and routes into it."
  artifacts:
    - "apps/client/src/features/households/invitation-flow.tsx"
    - "apps/client/app/invite/[token].tsx"
    - "apps/api/test/households/invitations.int.test.ts"
  key_links:
    - "sanitized invite continuation -> safe intended route -> authenticated preview -> acceptInvitation transaction"
---

<objective>
Complete the invitation join loop with privacy-safe preview, authentication return, explicit acceptance, and atomic membership creation.

Purpose: Fulfill D-07/D-08 without token leakage, email confusion, auto-join, or partial membership state.
Output: Preview/accept API operations, generated client, invitation state machine, and deep-link route.
</objective>

<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-04-SUMMARY.md
@.planning/phases/01-safe-account-entry/01-17-SUMMARY.md
@.planning/phases/01-safe-account-entry/01-21-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement privacy-safe invitation preview and atomic explicit acceptance</name>
  <files>apps/api/src/modules/households/dto/invitation.dto.ts, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/openapi/generate-openapi.ts, apps/api/test/households/invitations.int.test.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, packages/api-client/src/generated/index.ts</files>
  <read_first>
    - apps/api/src/modules/households/dto/invitation.dto.ts
    - apps/api/src/modules/households/households.controller.ts
    - apps/api/src/modules/households/households.service.ts
    - apps/api/test/households/invitations.int.test.ts
    - apps/api/src/modules/auth/auth.service.ts
    - apps/api/src/modules/auth/access-token.guard.ts
    - apps/api/src/openapi/generate-openapi.ts
    - packages/api-client/src/generated/client.ts
    - .planning/phases/02-household-member-collaboration/02-CONTEXT.md
    - .planning/phases/02-household-member-collaboration/02-PATTERNS.md
  </read_first>
  <behavior>
    - D-07: public GET `/invitations/:token/preview` returns household name, inviter display name, and exact expiry only for a valid pending token; terminal/unknown tokens use a generic response.
    - D-08/SAFE-02: guarded POST `/invitations/:token/accept` canonical-email checks and conditionally claims the invite plus inserts member Membership in one serializable transaction.
    - Concurrent accept requests create exactly one membership; used/expired/revoked links never establish another relationship.
  </behavior>
  <action>Add explicit `InvitationPreviewDto` and `AcceptInvitationResponseDto`. Hash the raw route token before lookup; never log or echo it. Public preview returns only UI-approved fields for valid pending state and a stable generic not-found boundary otherwise. Acceptance derives user ID and canonical email server-side, suppresses invite details on mismatch, conditionally updates the pending row, inserts fixed member membership, and returns the authoritative household inside a serializable transaction with bounded conflict mapping. Add stable codes for invalid/expired/used/revoked/email-mismatch without exposing internal IDs. Regenerate the client and activate single-use/concurrency/rollback tests.</action>
  <verify><automated>pnpm openapi:generate &amp;&amp; pnpm openapi:check &amp;&amp; pnpm --filter api test:integration -- --run test/households/invitations.int.test.ts</automated></verify>
  <acceptance_criteria>
    - Matching valid accept returns one membership and accepted timestamp; concurrent requests cannot create duplicates.
    - Mismatch response contains no household, inviter, recipient, token, or internal identifier.
    - Forced membership insert/claim failure leaves both invitation and membership unchanged.
    - Terminal and unknown preview responses follow the generic disclosure boundary defined by the UI contract.
  </acceptance_criteria>
  <done>Preview and concurrent-safe acceptance API tests pass without token or recipient-detail disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver sanitized auth-return invitation UI and current-household handoff</name>
  <files>apps/client/src/features/auth/session-bootstrap.tsx, apps/client/src/features/households/invitation-flow.tsx, apps/client/src/features/households/__tests__/invitation-flow-test.tsx, apps/client/app/invite/[token].tsx</files>
  <read_first>
    - apps/client/src/features/auth/session-bootstrap.tsx
    - apps/client/src/features/auth/verification-flow.tsx
    - apps/client/src/features/auth/__tests__/verification-flow-test.tsx
    - apps/client/src/features/households/household-context.tsx
    - apps/client/src/features/households/invitation-flow.tsx (create; target file)
    - apps/client/src/features/households/__tests__/invitation-flow-test.tsx (create; target file)
    - apps/client/app/auth/verify-email.tsx
    - apps/client/app/invite/[token].tsx (create; target file)
    - .planning/phases/02-household-member-collaboration/02-CONTEXT.md
    - .planning/phases/02-household-member-collaboration/02-UI-SPEC.md
  </read_first>
  <behavior>
    - D-07: unauthenticated preview displays household/inviter and login/register continuations; auth returns to the same sanitized invitation page and requires an explicit press.
    - D-08: mismatch renders only the exact mismatch copy and switch-account action; no further invitation detail remains in accessibility tree.
    - Success persists target household on this device, refreshes memberships, enters ready state, announces success, and navigates to target household.
  </behavior>
  <action>Capture the token in a route-owned ref, sanitize Web history and native route parameters before rendering, and dispose the sanitizer on unmount following verification-flow patterns. Extend safe intended routing with a typed invitation continuation that cannot become an arbitrary redirect. Implement `InvitationFlow` states `preview | unauthenticated | accepting | accepted | expired | used | invalid | emailMismatch | recoverableError`; login/registration return to preview and never call accept automatically. On successful explicit accept, persist the authoritative household ID before publishing ready context. On persistence or transaction failure, keep pre-action UI and refetch authority.</action>
  <verify><automated>pnpm --filter client test:quick -- invitation-flow-test &amp;&amp; pnpm --filter client typecheck &amp;&amp; pnpm exec playwright test e2e/households/collaboration.spec.ts --grep "accepts an invitation"</automated></verify>
  <acceptance_criteria>
    - Tests prove token never renders and Web history/native params are sanitized before completion.
    - Authentication returns to preview and `acceptInvitation` call count remains zero until the explicit acceptance action.
    - Email mismatch accessibility tree contains only mismatch explanation and switch-account action among invitation-specific details.
    - Browser join path ends in the target household with member role and device-local selection.
  </acceptance_criteria>
  <done>The sanitized authentication-return and explicit acceptance journey passes end to end.</done>
</task>

</tasks>

## Artifacts this phase produces

- `previewInvitation`, `acceptInvitation` — REST/OpenAPI/generated client operations.
- `InvitationFlow` and its typed state machine — `apps/client/src/features/households/invitation-flow.tsx`.
- `/invite/[token]` — sanitized universal deep-link route.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| deep link → public preview | Bearer token is untrusted and secret-bearing. |
| authenticated accept → DB | Account identity must match invitation and claim exactly once. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-14 | Information Disclosure | invitation preview/mismatch | high | mitigate | Valid-field allowlist, generic invalid response, mismatch detail suppression, no token rendering/logging. |
| T-02-15 | Spoofing | invitation recipient | high | mitigate | Compare canonical invite email to authenticated user record, never caller DTO. |
| T-02-16 | Tampering | concurrent acceptance | high | mitigate | Serializable conditional claim plus unique membership and rollback assertions. |
| T-02-17 | Repudiation | explicit acceptance | medium | mitigate | Acceptance occurs only on POST after explicit UI action and stores acceptedAt/acceptedBy relation. |
| T-02-SC | Tampering | package boundary | low | accept | Uses existing router, crypto, and client stack; no install task. |
</threat_model>

<verification>Focused API concurrency tests, client state tests, typecheck, OpenAPI drift, and browser join journey pass.</verification>
<success_criteria>A matching authenticated user can explicitly accept a valid link exactly once and enter the target household without token or detail leakage.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-05-SUMMARY.md` when done.</output>
