---
phase: 02-household-member-collaboration
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0003_household_invitations/migration.sql
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/infrastructure/mail/mail.port.ts
  - apps/api/src/infrastructure/mail/smtp-mail.adapter.ts
  - apps/api/src/infrastructure/mail/smtp-mail.adapter.test.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/client/src/features/households/household-api.ts
  - apps/client/src/features/households/household-settings.tsx
  - e2e/households/invitation-send-slice.spec.ts
autonomous: true
requirements: [HHLD-02, EXPR-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Per D-05, a signed-in owner/admin can use the real current-household settings form to send an email-only invitation whose eventual role is server-fixed MEMBER."
    - "Per D-06, registered, absent, and repeated non-member recipients receive identical API/status/UI success; only an already-current member may receive the authorized explicit error."
    - "Invitation persistence, exact seven-day rotation, generated contract/wrapper, settings form, committed database state, and post-commit mail delivery ship in this same first invitation increment."
    - "Raw invitation tokens exist only in the delivered URL, never persistence, response DTOs, UI state, or logs."
    - "The SMTP adapter contract maps the invitation recipient, invitation URL containing the raw token, inviter display name, household display name, and exact expiry into the delivered message without losing or swapping fields."
    - "Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to /households when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household."
  artifacts:
    - "apps/api/prisma/migrations/0003_household_invitations/migration.sql"
    - "apps/api/src/infrastructure/mail/smtp-mail.adapter.test.ts"
    - "apps/client/src/features/households/household-api.ts"
    - "apps/client/src/features/households/household-settings.tsx"
    - "e2e/households/invitation-send-slice.spec.ts"
  key_links:
    - "real /households/[id]/settings form -> household-api wrapper -> generated sendHouseholdInvitation -> guarded service -> committed hash/state -> MailPort URL"
---

<objective>
Deliver invitation sending as one genuinely usable UI -> API -> PostgreSQL -> mail slice per D-05/D-06.

Purpose: Ensure the first invitation increment is usable by a signed-in manager from the real settings UI rather than split horizontally across plans.
Output: Invitation migration/domain, guarded asserted contract and generated wrapper, mail extension, current-household form, and one dedicated full-boundary browser test.
</objective>

## Scope Exception

**Accepted expanded scope: exactly 14 concrete files.** The unavoidable generated OpenAPI artifacts are `packages/api-client/openapi.json`, `packages/api-client/src/generated/models.ts`, and `packages/api-client/src/generated/client.ts`; the cross-platform test surfaces are `apps/api/src/infrastructure/mail/smtp-mail.adapter.test.ts` and `e2e/households/invitation-send-slice.spec.ts`, with the latter covering browser, authenticated API, PostgreSQL, and deterministic mailbox boundaries. Splitting this send journey would create horizontal API/client layers and violate the phase's MVP vertical-slice rules. Execution is bounded to exactly two tasks, Plan 02-05 has exclusive Wave 5 ownership, and dependency sequencing ensures no overlapping concurrent plan or editor touches an owned file. To stay below the 15-file blocker, send-only Swagger DTOs are exported from `households.controller.ts` rather than owned in a separate `invitation.dto.ts`; no contract or journey behavior is reduced. Invitation listing, resend, and revoke remain in Plan 02-07.

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-04-SUMMARY.md
</context>

<read_first>
@apps/api/src/infrastructure/mail/smtp-mail.adapter.test.ts
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish the dedicated end-to-end invitation-send RED contract</name>
  <files>e2e/households/invitation-send-slice.spec.ts</files>
  <behavior>
    - Exactly one named test navigates through the signed-in real settings route and sends one invitation.
    - The same test uses authenticated request contexts for owner/admin/member/outsider and registered/absent/repeat/current-member cases, then inspects deterministic mailbox and database state.
    - Healthy auth, current household, PostgreSQL, migration, SMTP, route, and selector fixtures precede the one exact missing behavior marker.
  </behavior>
  <action>Create exactly one Playwright test named `sends a privacy-preserving invitation from settings [RED:INVITATION_SEND]` with marker `IMPLEMENTATION_MISSING_INVITATION_SEND`. Add the static one-test guard before existing `assert-red.ps1`. The test must traverse the actual settings UI, use real API/mail services, and leave lifecycle listing/resend/revoke assertions out.</action>
  <verify><automated>$count = (rg -n "^\s*test\(" e2e/households/invitation-send-slice.spec.ts | Measure-Object).Count; if ($count -ne 1) { throw "invitation-send-slice.spec.ts must contain exactly one test" }; powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/invitation-send-slice.spec.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_INVITATION_SEND</automated></verify>
  <done>The first invitation increment has one exact full-boundary RED contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the real settings-to-mail invitation journey green</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0003_household_invitations/migration.sql, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/infrastructure/mail/mail.port.ts, apps/api/src/infrastructure/mail/smtp-mail.adapter.ts, apps/api/src/infrastructure/mail/smtp-mail.adapter.test.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, apps/client/src/features/households/household-api.ts, apps/client/src/features/households/household-settings.tsx, e2e/households/invitation-send-slice.spec.ts</files>
  <behavior>
    - Invitation hash is unique lowercase SHA-256; raw 32-byte token is delivered only after commit; expiry is exactly seven days.
    - Repeating a pending household/canonical-email invitation transactionally invalidates/rotates the predecessor before mail; no account-existence difference is observable.
    - Owner/admin see one contextual email field and generic five-second accessible success; member cannot see or invoke the action.
    - The SMTP adapter test asserts recipient mapping, invitation URL/token placement, inviter display, household display, and expiry mapping at the adapter contract layer.
  </behavior>
  <action>Apply `0003_household_invitations` after `0002_household_core` with Prisma Migrate, never schema push. Add Invitation with household/inviter FKs, canonical email, unique hash, lifecycle timestamps/checks, exact seven-day expiry, and at most one active pending invitation per household/email. Follow the PATTERNS guarded controller, opaque-token/Serializable service, MailPort/SMTP, and OpenAPI assertions: server-fix role MEMBER; export the send-only request/response Swagger DTOs from `households.controller.ts`; fresh owner/admin authorization; 32 random bytes; SHA-256-only persistence; rotate any pending predecessor transactionally; commit before mail; expose identical D-06 status/body for registered, absent, and repeated non-members; permit only the authorized already-member error. Extend MailPort and SmtpMailAdapter with the typed household invitation message. Extend `smtp-mail.adapter.test.ts` first to capture the transport payload and assert the exact recipient, invitation URL containing the raw token, inviter display name, household display name, and expiry mapping in both accessible text and escaped HTML where applicable; keep existing verification/reset/password-change assertions green. Assert send operationId, bearer security, and controller-exported DTO schemas before regenerating the three changed outputs. Extend the typed household-api wrapper. In the already-real `/households/[id]/settings` feature, add one email field, `HouseholdContextNote`, fixed member copy, owner/admin-only action, stable pending label, duplicate lock, cleared field on success, at-least-five-second polite status, recoverable failure, and no registration-state copy. If fresh authorization reports membership loss, apply D-12 exactly: freeze household actions, clear the lost household cache and device persistence, render the explicit accessChanged explanation first, and only let an explicit user action continue to `/households` when memberships remain or to the equal D-01 create/accept handoff when none remain; never automatically select or enter another household. Exercise PostgreSQL and mailbox state through the dedicated test and remove the marker only when the whole journey passes.</action>
  <verify><automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres mailpit; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm --filter api prisma:generate; pnpm --filter api test:quick -- smtp-mail.adapter.test; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api typecheck; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/invitation-send-slice.spec.ts</automated></verify>
  <done>A signed-in owner/admin sends a privacy-preserving invitation from the real settings UI through committed persistence and post-commit mail, and the adapter contract proves recipient, URL/token, inviter, household, and expiry mapping.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| settings form -> invite API | Email, cached role, and household route ID are untrusted. |
| API -> PostgreSQL -> mail | Raw bearer material crosses only into post-commit delivery. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-13 | Information Disclosure | high | mitigate | V15.3.1 | Registered/absent/repeat response and UI evidence is identical. |
| T-02-14 | Spoofing | high | mitigate | V11.4.1 (L1), V11.5.1 (L2 defense-in-depth) | CSPRNG entropy and SHA-256 hash-only persistence are inspected. |
| T-02-15 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | Owner/admin/member/outsider server cases execute through trusted authorization. |
| T-02-16 | Tampering | high | mitigate | V2.3.3 (L2 defense-in-depth) | Rotation, commit-before-mail, and rollback database assertions prove atomicity. |
| T-02-SC | Tampering | low | accept | N/A | Existing crypto/mail/client dependencies only; no install. |
</threat_model>
<verification>Migration sequence/drift, SMTP adapter contract mapping, asserted OpenAPI generation, strict API/client typechecks, real settings UI, authorization/privacy matrix, database hash/expiry/rotation, and mailbox delivery all pass.</verification>
<success_criteria>D-05/D-06 invitation sending is immediately usable from the real settings UI and remains generic, role-fixed, hash-only, transactional, and post-commit delivered, with recipient, URL/token, inviter, household, and expiry mapping proven at the SMTP adapter boundary.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-05-SUMMARY.md` when done.</output>
