---
context: phase
phase: 02-household-member-collaboration
status: paused
last_updated: 2026-08-03T07:55:53.006Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified in this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Post-merge executor test breakage | Executors cannot run tests in worktree (no node_modules). Every wave merge requires running `pnpm test:quick` and fixing: async `render()` must be awaited, RNTL queries are methods on the awaited result (not screen/standalone), duplicate text elements need `getAllByText`. | advisory | Always run `pnpm --if-present --recursive test:quick` after each wave merge before moving to next wave. Fix and commit as `fix(XX-XX): reason`. |
| Worktree `.claude/` dir blocks cleanup | The harness copies `.claude/settings.local.json` into the worktree. `git worktree remove` blocks on untracked files. | advisory | Always `rm -rf worktree_path/.claude` before `git merge` and `git worktree remove`. |

<current_state>
Phase 02 execution paused between Waves 6 and 7. All 6 completed waves merged to master with passing quick tests.

**Orchestrator:** The execute-phase orchestrator was about to dispatch Wave 7 (02-07 invitation lifecycle). The dispatch pattern is identical to Waves 1-6.
</current_state>

<completed_work>

Completed Waves (9/13):
- **Wave 1 (02-01)**: Household creation slice — Prisma migration 0002, households module, D-01 handoff, `/households/new` route, e2e spec. 15 files.
- **Wave 2 (02-02)**: List/restore/switch/access-loss — GET /households, household-context (5-state machine), platform seams (native/web), `/households` index, AppShell/HouseholdHeader/HouseholdSwitcher/HouseholdCard. 20 files.
- **Wave 3 (02-03)**: Member roster — GET /households/:id, total-order member sorting, MemberRow/RoleBadge, `/households/[id]` route. 14 files.
- **Wave 4 (02-04)**: Owner rename — PATCH /households/:id, update-household DTO, `/households/[id]/settings` route. 12 files.
- **Wave 5 (02-05)**: Invitation send — Prisma migration 0003, POST /households/:id/invitations, SMTP mail adapter, commit-before-mail, household-settings form. 15 files.
- **Wave 6 (02-06)**: Invitation accept — GET /invite/:token preview, POST /invite/:token accept, InvitationFlow component (7 states), sessionStorage token persistence, 8 client tests. 14 files.
- **Wave 7 (02-07)**: Invitation lifecycle — status display, resend with rotation, confirmed revoke. InvitationRow/ConfirmationPage components. 10 files.
- **Wave 8 (02-08)**: Role governance — D-09 permission matrix, D-10 demotion confirmation, household-policy.ts, canGovern/useMemberGovernance. 10 files.
- **Wave 9 (02-09)**: Member removal — D-09 removal policy (removalFailure), D-10 ConfirmationPage consequence, D-12 access-loss recovery, DELETE endpoint, remove.tsx route. 13 files.
- **Wave 10 (02-10)**: Ownership transfer — transferFailure policy, compare-and-set pointer transaction, FinalConfirmation D-10 component, POST /ownership/transfer, transfer.tsx route. 15 files.
- **Wave 11 (02-11)**: Owner leave — D-11 leaveFailure policy, atomic pointer-transfer-and-membership-delete transaction, FinalConfirmation reuse, POST /ownership/leave, leave.tsx route with D-12 recovery. 13 files.

All quick tests pass: API 4/4, Client 137/139 (15 suites, 2 known skips).
</completed_work>

<remaining_work>

- **Wave 10 (02-10)**: Ownership transfer — owner-pointer invariant, D-10 confirmation. `/ownership/transfer` route.
- **Wave 11 (02-11)**: Owner leave — D-11 atomic handoff. `/ownership/leave` route.
- **Wave 12 (02-12)**: Evidence closure — responsive/a11y/migration/ASVS audits.
- **Wave 13 (02-13)**: ◷ Human Android acceptance — non-autonomous checkpoint plan.
</remaining_work>

<decisions_made>
- Set `worktree.baseRef: "head"` in `.claude/settings.local.json` — repo has no remote; worktrees fork from live HEAD.
- Executor model: opus, verifier model: sonnet.
- Skip post-merge integration tests (Docker PostgreSQL 18 healthcheck flaky) — gate each wave on quick tests only.
- Skip ROADMAP/STATE tracking updates (post-merge integration tests not passing) — update at phase completion.
</decisions_made>

<blockers>
- Docker PostgreSQL 18 healthcheck fails in Windows environment — full integration tests and Playwright e2e cannot run. Quick tests (unit only) pass. This is pre-existing (Phase 1 had same issue).
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-household-member-collaboration/02-07-PLAN.md` — next plan to execute
1. `.planning/STATE.md` — current project state
1. `.planning/HANDOFF.json` — structured machine-readable state

## Critical Anti-Patterns (do NOT repeat these)

## Infrastructure State
- Docker available (29.1.3) but PostgreSQL 18 container healthcheck is flaky
- pnpm workspace monorepo; quick tests run as `pnpm --if-present --recursive test:quick`
- Worktree isolation: `harness-worktree` with `baseRef:head`
- `.claude/settings.local.json` exists with `{"worktree":{"baseRef":"head"}}`

## Pre-Execution Critique Required

<context>
Wave dispatch pattern (repeat for each wave):
1. Base-check guard: `gsd_run query worktree.base-check --pick shouldDegrade` (expect false)
2. Capture: `EXPECTED_BASE=$(git rev-parse HEAD)`
3. Fresh manifest: `mktemp`, write `{orchestrator_root, worktrees:[]}`
4. Emit `[checkpoint]` heartbeat
5. Spawn `Agent(subagent_type="gsd-executor", isolation="worktree", model="opus", run_in_background=false)` with worktree_branch_check embedding EXPECTED_BASE
6. After return: `rm -rf worktree_path/.claude`, `git merge agent-branch`, `git worktree remove`, `git branch -D`, `git worktree prune`
7. Run `pnpm --if-present --recursive test:quick`
8. Fix any test failures, commit as `fix(XX-XX): ...`
9. Emit `[checkpoint] phase 02 wave N/13 complete, N/13 plans done`

The agent prompt template is well-proven from Waves 1-6 and should be reused with only the EXPECTED_BASE and plan file number changed.
</context>

<next_action>
Run `/gsd-execute-phase 2` to resume. Phase discovery will detect 6 existing SUMMARY.md files and skip to Wave 7 (02-07). Continue with the same dispatch pattern through Waves 7-12, then present the Wave 13 Android acceptance checkpoint to the user.
</next_action>
