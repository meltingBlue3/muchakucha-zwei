---
phase: 02-household-member-collaboration
plan: 10
type: execute
wave: 10
depends_on: [02-09]
files_modified:
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/test/households/governance.int.test.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/{openapi.json,src/generated/models.ts,src/generated/client.ts,src/generated/index.ts}
  - apps/client/src/ui/household-components.tsx
  - apps/client/src/features/households/member-governance.tsx
  - apps/client/src/features/households/__tests__/ownership-transfer-test.tsx
  - apps/client/app/(protected)/households/[id]/ownership/transfer.tsx
autonomous: true
requirements: [HHLD-08, HHLD-09, EXPR-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Only current owner can transfer the household owner pointer to an existing same-household member."
    - "Per D-10, transfer uses successor selection, consequence page, and safe-default final confirmation."
    - "Concurrent or failed transfers leave the complete pre-operation owner graph unchanged."
  artifacts:
    - "apps/client/src/ui/household-components.tsx#FinalConfirmation"
    - "apps/client/app/(protected)/households/[id]/ownership/transfer.tsx"
  key_links:
    - "successor selection -> final confirmation -> locked Household.ownerMembershipId compare-and-set -> authoritative refetch"
---
<objective>Deliver owner-only ownership transfer with the database owner-pointer invariant and D-10 confirmation depth.</objective>
<scope_rationale>This is one vertical transfer increment across invariant-preserving operation, generated contract, staged owned UI, and concurrency proof. Plan 02-10 exclusively owns its Wave 10 edits; shared files are dependency-sequenced.</scope_rationale>
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-09-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate ownership-transfer RED contracts</name>
  <files>apps/api/test/households/governance.int.test.ts, apps/client/src/features/households/__tests__/ownership-transfer-test.tsx</files>
  <behavior>
    - Owner-only transfer to a different current same-household membership.
    - Concurrent transfer compare-and-set permits one winner and preserves exactly one pointer.
    - D-10 three-stage flow, safe-default focus, and zero mutation from safe/cancel actions.
  </behavior>
  <action>Add focused API tests and one uniquely named component case `transfers ownership safely [RED:OWNERSHIP_TRANSFER]` with marker `IMPLEMENTATION_MISSING_OWNERSHIP_TRANSFER` after owner-pointer checks pass.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite client -TestPath apps/client/src/features/households/__tests__/ownership-transfer-test.tsx -TestName "transfers ownership safely [RED:OWNERSHIP_TRANSFER]" -MissingBehaviorMarker IMPLEMENTATION_MISSING_OWNERSHIP_TRANSFER</automated></verify>
  <done>The transfer contract is exact, focused, and truthfully RED.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement pointer-based ownership transfer and final confirmation</name>
  <files>apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/test/households/governance.int.test.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/{openapi.json,src/generated/models.ts,src/generated/client.ts,src/generated/index.ts}, apps/client/src/ui/household-components.tsx, apps/client/src/features/households/member-governance.tsx, apps/client/src/features/households/__tests__/ownership-transfer-test.tsx, apps/client/app/(protected)/households/[id]/ownership/transfer.tsx</files>
  <action>Follow PATTERNS.md AccessTokenGuard/operation-ID analog from `auth.controller.ts`, Serializable mechanics from `auth.service.ts`, explicit no-direct-analog owner-invariant guidance, `me.int.test.ts` concurrency/DB assertions, `generate-openapi.ts` pre-write assertions, and owned `FinalConfirmation`. Add owner-only transfer accepting `successorMembershipId`. In a Serializable transaction lock the Household row, verify actor membership equals current `ownerMembershipId`, verify a distinct successor in the same household, set the former owner's base role deterministically to `MEMBER`, then conditionally update the single household pointer from actor ID to successor ID. Assert the former-owner `MEMBER` role in success, rollback, and concurrent-loser tests and in the returned DTO. The non-null composite FK enforces same-household membership at commit; no promote-then-demote dual-owner ordering exists. Concurrent requests serialize on the household row and compare-and-set; loser maps to stable conflict with no partial writes. Assert exact transfer operationId and DTO schemas before generated client output. Implement `FinalConfirmation` mobile modal/Web dialog with focus trap, Esc, safe-default focus/restoration, safe-first DOM order, exact D-10 copy, pending locks, and authoritative refetch.</action>
  <verify><automated>pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/governance.int.test.ts --testNamePattern "transfers ownership"; pnpm --filter client test:quick -- ownership-transfer-test; pnpm --filter client typecheck</automated></verify>
  <done>Transfer atomically moves the one owner pointer and the safe-default staged UI is green.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| owner UI -> ownership API | Successor ID and cached owner state are untrusted. |
| transaction -> owner pointer | Competing transfers must serialize and preserve one same-household target. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-27 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | Fresh current-owner function and same-household successor-item checks execute at the trusted service. |
| T-02-28 | Tampering | high | mitigate | V2.3.3 (L2 defense-in-depth) | Row lock, compare-and-set, composite FK, former-owner MEMBER role, and rollback assertions prove atomic transfer. |
| T-02-29 | Repudiation | medium | mitigate | V3.5.3 | Explicit POST after staged confirmation and zero safe-action calls prove mutation does not use a safe method. |
| T-02-SC | Tampering | low | accept | N/A | Owned dialog; no install. |
</threat_model>
<verification>Ownership integration, concurrency DB, final-confirmation component, OpenAPI, and type gates pass.</verification>
<success_criteria>Ownership transfers to one existing member without dual-owner ordering, zero-owner windows, or partial UI/database state.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-10-SUMMARY.md` when done.</output>
