---
phase: 02-household-member-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0002_household_core/migration.sql
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.module.ts
  - apps/api/src/modules/households/dto/create-household.dto.ts
  - apps/api/src/app.module.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/client/app/(protected)/household-handoff.tsx
  - apps/client/app/(protected)/households/new.tsx
  - e2e/households/create-slice.spec.ts
autonomous: true
requirements: [HHLD-01, HHLD-09, EXPR-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Per D-01, an authenticated user with no memberships sees equal create and accept-invitation actions and can open /households/new."
    - "Creating a 1-40 NFC-code-point household atomically creates its creator membership, stores that membership in non-null Household.ownerMembershipId, and returns the authoritative household/member projection."
    - "The owned /households/new route stays in place after success and renders the authoritative created household plus owner role; it does not claim or navigate to an unowned household destination route."
  artifacts:
    - "apps/api/prisma/migrations/0002_household_core/migration.sql"
    - "apps/api/src/modules/households/households.controller.ts"
    - "apps/client/app/(protected)/household-handoff.tsx"
    - "apps/client/app/(protected)/households/new.tsx"
    - "e2e/households/create-slice.spec.ts"
  key_links:
    - "D-01 handoff -> /households/new -> generated createHousehold -> Serializable Household+Membership write -> authoritative result rendered on /households/new"
    - "Household.ownerMembershipId -> deferred same-household composite FK -> Membership(id, householdId)"
---

<objective>
Deliver the first complete Phase 2 user slice: create a household from the real D-01 handoff, become its pointer-designated owner, and see the committed result inside the route this plan owns.

Purpose: Replace the Phase 1 placeholder with a narrow UI -> API -> PostgreSQL journey without claiming `/households` or `/households/[id]` before those routes exist.
Output: Core migration, guarded create contract, generated client operation, D-01 handoff, `/households/new`, and one dedicated RED-to-green browser contract.
</objective>

<scope_rationale>Exactly 14 concrete files are owned. The generated index is not listed because its constant re-export content does not change; `openapi.json`, `models.ts`, and `client.ts` are the actual changed generator outputs. The route intentionally owns its compact form/result presentation to keep this first vertical slice below the hard file cap; later shared household presentation moves into the PATTERNS-owned component layer.</scope_rationale>

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/01-safe-account-entry/01-19-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the dedicated creation-slice RED contract</name>
  <files>e2e/households/create-slice.spec.ts</files>
  <behavior>
    - The file contains exactly one test named `creates and displays the authoritative household [RED:HOUSEHOLD_CREATE]`.
    - Healthy Phase 1 authentication reaches the D-01 handoff, opens `/households/new`, submits a valid name, and fails only at the named missing creation behavior.
    - The marker occurs exactly once; test discovery, authentication, database, migration, network, module/config, and selector failures remain blocking.
  </behavior>
  <action>Create the dedicated Playwright file with exactly one `test(...)` declaration and exact marker `IMPLEMENTATION_MISSING_HOUSEHOLD_CREATE`. Add a static one-test guard before calling the existing fail-closed `scripts/assert-red.ps1`; do not enhance that shared helper or place unrelated tests in this file. The test must use the real signed-in handoff and `/households/new` route, then assert the authoritative created name and owner role after submission.</action>
  <verify><automated>$count = (rg -n "^\s*test\(" e2e/households/create-slice.spec.ts | Measure-Object).Count; if ($count -ne 1) { throw "create-slice.spec.ts must contain exactly one test" }; powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/create-slice.spec.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_CREATE</automated></verify>
  <done>One discoverable test supplies exact, dedicated, infrastructure-clean RED evidence for the creation slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make household creation green through the owned result route</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0002_household_core/migration.sql, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.module.ts, apps/api/src/modules/households/dto/create-household.dto.ts, apps/api/src/app.module.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, apps/client/app/(protected)/household-handoff.tsx, apps/client/app/(protected)/households/new.tsx, e2e/households/create-slice.spec.ts</files>
  <behavior>
    - D-01/HHLD-01: equal handoff actions lead to the real create route and invitation entry without preselecting intent.
    - HHLD-09/SAFE-02: creation yields one household, one creator membership, and one non-null same-household owner pointer in one transaction.
    - Exact trimmed NFC household names from 1 through 40 Unicode code points succeed; empty or 41-code-point values fail consistently in DTO, OpenAPI, service, and database checks.
  </behavior>
  <action>Follow the PATTERNS classifications: extend Prisma with Household and Membership plus `ADMIN | MEMBER` base roles; make non-null `ownerMembershipId` authoritative through a deferred same-household composite FK and unique membership constraints; create and apply `0002_household_core` with Prisma Migrate, never schema push. Use the existing Auth controller/service/module analogs to add guarded `createHousehold`, deriving the creator solely from verified access claims and performing the household, membership, and owner-pointer writes at Serializable isolation. Normalize trim+NFC and enforce exact 1-40 Unicode code points in DTO, service, SQL CHECK, and OpenAPI metadata. Add explicit create operationId and schema assertions before generation; regenerate only the three outputs whose content changes. Per D-01, replace the placeholder handoff with equal create/accept actions. Implement the compact form directly in the owned `/households/new` route using existing typed primitives; after commit, remain on that route and render the returned household name, creator membership, textual owner role, and exact success copy. Do not navigate to `/households` or `/households/[id]`, which are not owned here. Remove the marker only after the one-test file passes.</action>
  <verify><automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm --filter api prisma:generate; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api typecheck; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/create-slice.spec.ts</automated></verify>
  <done>The real handoff creates a durable household and displays the authoritative owner projection inside `/households/new`, with no unowned-route claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| authenticated client -> household API | Name input is untrusted; creator identity comes only from verified access claims. |
| service -> PostgreSQL | Household, first membership, and owner pointer must commit together. |

## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-01 | Elevation of Privilege | high | mitigate | V8.2.1, V8.3.1 | Browser/API assertions prove subject identity is guard-derived and no actor ID is accepted. |
| T-02-02 | Tampering | high | mitigate | V2.3.3 (L2 defense-in-depth) | Migration constraints and rollback checks prove household, membership, and owner pointer remain atomic. |
| T-02-03 | Information Disclosure | medium | mitigate | V15.3.1 | Create response exposes only the household and creator membership projection needed by the owned result route. |
| T-02-SC | Tampering | low | accept | N/A | No package installation or lockfile change. |
</threat_model>

<verification>Dedicated one-test RED semantics, migration status/drift, asserted OpenAPI generation, strict typechecks, and the real browser create journey pass.</verification>
<success_criteria>A signed-in no-household user creates a valid household, becomes its sole pointer-designated owner, and sees the committed result without navigating to an unowned route.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-01-SUMMARY.md` when done.</output>
