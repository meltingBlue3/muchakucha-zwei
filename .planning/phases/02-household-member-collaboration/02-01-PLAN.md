---
phase: 02-household-member-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/assert-red.ps1
  - scripts/assert-red.Tests.ps1
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0002_household_core/migration.sql
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.module.ts
  - apps/api/src/modules/households/dto/create-household.dto.ts
  - apps/api/src/app.module.ts
  - apps/api/src/openapi/generate-openapi.ts
  - apps/api/test/households/households.int.test.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/{models,client,index}.ts
  - apps/client/app/(protected)/household-handoff.tsx
  - apps/client/app/(protected)/households/new.tsx
  - apps/client/src/features/households/household-api.ts
  - apps/client/src/features/households/household-entry.tsx
  - apps/client/src/ui/theme.ts
  - apps/client/src/ui/__tests__/token-static-test.ts
  - e2e/households/collaboration.spec.ts
autonomous: true
requirements: [HHLD-01, HHLD-04, HHLD-05, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Per D-01, an authenticated user with no household sees equal create and accept-invitation entries and can create a household."
    - "The creator becomes the database-designated owner in the same transaction and immediately sees the new household and member projection."
    - "A non-member cannot read the household or its members."
  artifacts:
    - "scripts/assert-red.ps1"
    - "scripts/assert-red.Tests.ps1"
    - "apps/api/prisma/migrations/0002_household_core/migration.sql"
    - "apps/api/src/modules/households/households.service.ts"
    - "apps/client/app/(protected)/household-handoff.tsx"
    - "apps/client/app/(protected)/households/new.tsx"
  key_links:
    - "handoff create form -> generated createHousehold -> serializable Household+Membership write -> member projection"
    - "Household.ownerMembershipId -> deferred same-household FK -> Membership(id, householdId)"
---

<objective>
Deliver the first usable UI -> API -> PostgreSQL slice: create a household, become its owner, and enter its isolated member view per D-01.

Purpose: Replace the Phase 1 handoff with a real household capability while establishing the feasible exactly-one-owner representation.
Output: Core migration, guarded household module, generated contract, focused tests, and working handoff creation UI.
</objective>

<scope_rationale>This broad file set is one vertical create-household increment: schema, guarded operation, generated contract, and the only user entry UI must become usable together. Plan 02-01 exclusively owns these files in Wave 1; every later shared-file change is dependency-sequenced.</scope_rationale>

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/01-safe-account-entry/01-19-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make RED verification exact and fail closed</name>
  <files>scripts/assert-red.ps1, scripts/assert-red.Tests.ps1</files>
  <behavior>
    - A backward-compatible `-TestName` selector executes exactly one named Vitest, Jest, or Playwright test inside the requested file.
    - RED succeeds only when exactly that selected test fails with exactly one occurrence of the requested missing-behavior marker and no other failed test or error.
    - Discovery, database, network, authentication, fixture, module/config, and locator/selector failures are always rejected even if marker text is also present.
  </behavior>
  <action>Enhance `assert-red.ps1` with optional mandatory-for-Phase-2 `-TestName` selection while retaining old callers that omit it. Map the selector to Vitest `--testNamePattern`, Jest `--testNamePattern`, and Playwright `--grep` using exact anchored/escaped matching; prove discovery identifies exactly one test, execute only that test, parse output to require one failed test and the requested marker exactly once, and reject any additional failure/error. Expand the infrastructure signature denylist to cover connection refused/timeouts, PostgreSQL/database/migration errors, unauthorized/forbidden/session setup, fixture/bootstrap failures, and missing/ambiguous locator or selector signatures. Move self-testing out of generated temporary tests into committed `scripts/assert-red.Tests.ps1`; use deterministic fake-runner transcripts/process seams to prove the valid single-marker case and negative cases for marker-plus-unrelated assertion, DB, network, auth, locator, missing test, ambiguous selector, duplicate marker, and unexpected pass. Keep `-SelfTest` backward compatible by delegating to the committed suite.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/assert-red.Tests.ps1</automated></verify>
  <done>The helper accepts one selected missing behavior and rejects every mixed, ambiguous, infrastructure, or unrelated failure case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the household creation API under an exact RED contract</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0002_household_core/migration.sql, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.module.ts, apps/api/src/modules/households/dto/create-household.dto.ts, apps/api/src/app.module.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/{models,client,index}.ts, apps/api/test/households/households.int.test.ts</files>
  <behavior>
    - D-01/HHLD-01: the no-household route exposes equal create/accept actions and creation yields one household plus one owner membership.
    - HHLD-05/SAFE-01: the creator can read the explicit member projection and an outsider receives the stable inaccessible response.
    - HHLD-09: the household row identifies exactly one same-household owner membership.
  </behavior>
  <action>First create the uniquely named integration case `creates a household through the new-household route [RED:HOUSEHOLD_CREATE]` with marker `IMPLEMENTATION_MISSING_HOUSEHOLD_CREATE`, and after healthy auth/database/migration fixtures execute `powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite api -TestPath test/households/households.int.test.ts -TestName "creates a household through the new-household route [RED:HOUSEHOLD_CREATE]" -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_CREATE`. Then implement `Household`, `Membership`, and base roles `ADMIN | MEMBER`, using `register.dto.ts` as the classified `create-household.dto.ts` analog, `auth.controller.ts` for guard/operation IDs, `auth.service.ts` for Serializable transactions, and `me.int.test.ts` for isolation. Make `ownerMembershipId` non-null and authoritative with a deferred same-household composite FK and unique membership constraints. Normalize names with NFC/trim and enforce 1-40 code points in SQL/DTO/OpenAPI. Generate the client from asserted live OpenAPI and apply `0002_household_core` with Prisma Migrate, never schema push. Remove the marker only after the focused test and full integration file pass.</action>
  <verify><automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts</automated></verify>
  <done>The exact test transitions RED to green and the versioned API can create one durable household with one pointer-designated owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver the routed household creation UI on the green API</name>
  <files>apps/client/app/(protected)/household-handoff.tsx, apps/client/app/(protected)/households/new.tsx, apps/client/src/features/households/household-api.ts, apps/client/src/features/households/household-entry.tsx, apps/client/src/ui/theme.ts, apps/client/src/ui/__tests__/token-static-test.ts, e2e/households/collaboration.spec.ts</files>
  <action>Resolve classified `household-api.ts` with `src/api/api-client.ts` and `household-entry.tsx` with `profile-form.tsx`; use thin route and owned primitive analogs. Before household components consume new values, extend `theme.ts` with `colors.overlayScrim: rgba(45,39,37,0.60)` as typed `overlay.scrim` and layout tokens `householdMaxWidth: 960`, `householdSwitcherWidth: 360`, `householdSwitcherMaxHeight: 480`, and `householdSettingsNavWidth: 280`; correct caption typography/text variant from 500/Medium to 400/Regular so household UI uses only 400/600. Static tests assert exact tokens and reject raw feature literals. Per D-01/UI-SPEC, keep equal create/accept cards on `/household-handoff`; create navigates to `/households/new` and never expands inline. The thin route renders `HouseholdEntry`, calls typed `household-api.ts`, and enters the authoritative household after commit. Add route E2E proof for handoff -> new -> created household.</action>
  <verify><automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm --filter api prisma:generate; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts; pnpm --filter client test:quick; pnpm --filter client typecheck</automated></verify>
  <done>Creation is green through the real handoff UI, generated API, committed migration, PostgreSQL constraints, and isolated member projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| authenticated client -> household API | Name and route identifiers are untrusted; identity comes from the verified access token. |
| service -> PostgreSQL | Household, first membership, and owner pointer must commit together. |

## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-01 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | `households.int.test.ts` names function-permission, same-household data-permission, and trusted-service AccessTokenGuard assertions. |
| T-02-02 | Tampering | high | mitigate | V2.3.3 | Migration FK checks and rollback assertions prove the owner pointer and membership commit together; V2.3.3 is L2 defense-in-depth and is not mislabeled L1. |
| T-02-03 | Information Disclosure | high | mitigate | V8.2.2, V15.3.1 | Outsider/cross-household tests assert item authorization plus an explicit minimal DTO projection. |
| T-02-SC | Tampering | low | accept | N/A | No package installation or lockfile change, so package-legitimacy controls are not applicable. |
</threat_model>

<verification>Migration status/drift, focused integration, generated contract, client tests, and typecheck all pass.</verification>
<success_criteria>A signed-in user can create a durable household, become its database-designated owner, and see its isolated member state.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-01-SUMMARY.md` when done.</output>
