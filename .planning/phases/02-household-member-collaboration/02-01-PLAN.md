---
phase: 02-household-member-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/0002_household_core/migration.sql
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.module.ts
  - apps/api/src/app.module.ts
  - apps/api/src/openapi/generate-openapi.ts
  - apps/api/test/households/households.int.test.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/{models,client,index}.ts
  - apps/client/app/(protected)/household-handoff.tsx
autonomous: true
requirements: [HHLD-01, HHLD-04, HHLD-05, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "Per D-01, an authenticated user with no household sees equal create and accept-invitation entries and can create a household."
    - "The creator becomes the database-designated owner in the same transaction and immediately sees the new household and member projection."
    - "A non-member cannot read the household or its members."
  artifacts:
    - "apps/api/prisma/migrations/0002_household_core/migration.sql"
    - "apps/api/src/modules/households/households.service.ts"
    - "apps/client/app/(protected)/household-handoff.tsx"
  key_links:
    - "handoff create form -> generated createHousehold -> serializable Household+Membership write -> member projection"
    - "Household.ownerMembershipId -> deferred same-household FK -> Membership(id, householdId)"
---

<objective>
Deliver the first usable UI -> API -> PostgreSQL slice: create a household, become its owner, and enter its isolated member view per D-01.

Purpose: Replace the Phase 1 handoff with a real household capability while establishing the feasible exactly-one-owner representation.
Output: Core migration, guarded household module, generated contract, focused tests, and working handoff creation UI.
</objective>

<scope_rationale>This broad file set is one vertical create-household increment: schema, guarded operation, generated contract, and the only user entry UI must become usable together. Plan 02-01 exclusively owns these files in Wave 1; every later shared-file change is dependency-sequenced.</scope_rationale>

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/01-safe-account-entry/01-19-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate the focused creation RED contract</name>
  <files>apps/api/test/households/households.int.test.ts, apps/client/app/(protected)/household-handoff.tsx</files>
  <behavior>
    - D-01/HHLD-01: the no-household route exposes equal create/accept actions and creation yields one household plus one owner membership.
    - HHLD-05/SAFE-01: the creator can read the explicit member projection and an outsider receives the stable inaccessible response.
    - HHLD-09: the household row identifies exactly one same-household owner membership.
  </behavior>
  <action>Create the focused booted-Fastify/PostgreSQL integration cases and a colocated handoff component contract. Use exact marker `IMPLEMENTATION_MISSING_HOUSEHOLD_CREATE`; reach the absent Phase 2 boundary only after existing auth, Docker, migration-runner, and rendering fixtures succeed. The RED helper must reject discovery, fixture, database, authentication, and selector failures.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite api -TestPath test/households/households.int.test.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_CREATE</automated></verify>
  <done>The focused creation contract is discovered and the fail-closed helper accepts only the exact missing-behavior marker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and migrate the household creation slice</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/0002_household_core/migration.sql, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.module.ts, apps/api/src/app.module.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/openapi.json, packages/api-client/src/generated/{models,client,index}.ts, apps/client/app/(protected)/household-handoff.tsx, apps/api/test/households/households.int.test.ts</files>
  <action>Implement `Household`, `Membership`, and base roles `ADMIN | MEMBER`, following PATTERNS.md analogs `auth.controller.ts` for `AccessTokenGuard`/versioned operation IDs, `auth.service.ts` for Serializable transactions, `register.dto.ts` for DTO validation, `me.int.test.ts` for real Fastify/PostgreSQL isolation tests, and `primitives.tsx` for owned UI. Make `Household.ownerMembershipId` non-null and authoritative for owner status; add a deferred, initially-deferred composite FK `(owner_membership_id,id) -> Membership(id,household_id)` plus uniqueness on `Membership(id,householdId)`. Creation pre-generates both UUIDs, inserts the household pointer and owner membership in one Serializable transaction, and the DTO derives `owner` when membership ID equals the household pointer. This is the concrete HHLD-09 mechanism: there is always one non-null owner pointer, and the FK guarantees that it targets a current membership in the same household. Normalize names with Unicode NFC plus trim and enforce 1-40 Unicode code points using PostgreSQL `char_length`, equivalent DTO/client validation, OpenAPI minLength/maxLength, and boundary tests; the exact validation copy is `请输入 1–40 个字符的家庭名称。`. Add unique `(householdId,userId)`, real FKs/indexes, guarded create/list/member endpoints, explicit DTO projections, and stable errors. In `generate-openapi.ts`, assert exact household operationIds and request/response DTO schema bounds before generated files are written, then generate the client from live OpenAPI. Create and apply `0002_household_core` with Prisma Migrate against PostgreSQL 18; never use schema push. Replace the Phase 1 handoff per D-01 with equal owned-primitive cards and an inline name form; on success enter the authoritative household/member view without claiming later content.</action>
  <verify><automated>$env:DATABASE_URL='postgresql://muchakucha_test:muchakucha_test_only@127.0.0.1:55432/muchakucha_test'; docker compose up -d --wait postgres; pnpm --filter api exec prisma migrate deploy; pnpm --filter api exec prisma migrate status; pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code; pnpm --filter api prisma:generate; pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts; pnpm --filter client test:quick; pnpm --filter client typecheck</automated></verify>
  <done>Creation is green through the real handoff UI, generated API, committed migration, PostgreSQL constraints, and isolated member projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| authenticated client -> household API | Name and route identifiers are untrusted; identity comes from the verified access token. |
| service -> PostgreSQL | Household, first membership, and owner pointer must commit together. |

## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS L1 | Planned evidence |
|---|---|---|---|---|---|
| T-02-01 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | `households.int.test.ts` names function-permission, same-household data-permission, and trusted-service AccessTokenGuard assertions. |
| T-02-02 | Tampering | high | mitigate | V2.3.3 | Migration FK checks and rollback assertions prove the owner pointer and membership commit together; V2.3.3 is L2 defense-in-depth and is not mislabeled L1. |
| T-02-03 | Information Disclosure | high | mitigate | V8.2.2, V15.3.1 | Outsider/cross-household tests assert item authorization plus an explicit minimal DTO projection. |
| T-02-SC | Tampering | low | accept | N/A | No package installation or lockfile change, so package-legitimacy controls are not applicable. |
</threat_model>

<verification>Migration status/drift, focused integration, generated contract, client tests, and typecheck all pass.</verification>
<success_criteria>A signed-in user can create a durable household, become its database-designated owner, and see its isolated member state.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-01-SUMMARY.md` when done.</output>
