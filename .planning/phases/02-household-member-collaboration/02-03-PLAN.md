---
phase: 02-household-member-collaboration
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/dto/membership.dto.ts
  - apps/api/src/openapi/generate-openapi.ts
  - apps/api/test/households/households.int.test.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/client/src/features/households/household-api.ts
  - apps/client/src/features/households/household-settings.tsx
  - apps/client/app/(protected)/households/index.tsx
  - apps/client/app/(protected)/households/[id]/index.tsx
  - apps/client/src/ui/household-components.tsx
  - e2e/households/roster-slice.spec.ts
autonomous: true
requirements: [HHLD-05, EXPR-02, SAFE-01]
must_haves:
  truths:
    - "A current member can enter the newly owned /households/[id] destination and see the authoritative household and complete roster."
    - "Members are totally ordered owner/admin/member, current user first within role, NFC nickname ascending, canonical email ascending, then stable membership/user ID."
    - "Cross-household route IDs expose no household or membership data; an inconsistent empty/ownerless projection freezes governance."
    - "Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to /households when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household."
  artifacts:
    - "apps/api/src/modules/households/dto/membership.dto.ts"
    - "apps/client/src/features/households/household-settings.tsx"
    - "apps/client/app/(protected)/households/[id]/index.tsx"
    - "e2e/households/roster-slice.spec.ts"
  key_links:
    - "/households HouseholdCard action -> /households/[id] -> guarded getHousehold roster -> generated client -> MemberRow/RoleBadge"
---

<objective>Deliver the first owned household destination: a guarded, totally ordered member roster reached from `/households`.</objective>
## Scope Exception

**Accepted expanded scope: exactly 14 concrete files.** The unavoidable generated OpenAPI artifacts are `packages/api-client/openapi.json`, `packages/api-client/src/generated/models.ts`, and `packages/api-client/src/generated/client.ts`; the cross-platform test surfaces are `apps/api/test/households/households.int.test.ts` and `e2e/households/roster-slice.spec.ts`. Splitting this roster destination would create horizontal API/client layers and violate the phase's MVP vertical-slice rules. Execution is bounded to exactly two tasks, Plan 02-03 has exclusive Wave 3 ownership, and dependency sequencing ensures no overlapping concurrent plan or editor touches an owned file. Rename remains in Plan 02-04 so this exception covers only the roster destination.

## Phase Goal

**As a** 已登录的成员, **I want to** 创建或通过邀请加入协作组，在多个组之间切换，并在权限范围内管理成员, **so that** 我可以和信任的人在明确、安全且彼此隔离的组上下文中协作.

<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate roster destination and total-order contracts</name>
  <files>e2e/households/roster-slice.spec.ts</files>
  <behavior>
    - Guarded member and outsider/cross-household cases prove item-level isolation.
    - Owner/admin/member order and every current-user/nickname/email/stable-ID tie-break are deterministic.
    - The household selector reaches `/households/[id]`, whose persistent header and member overview are authoritative.
  </behavior>
  <action>Create exactly one Playwright test named `shows the isolated totally ordered roster [RED:HOUSEHOLD_ROSTER]` with marker `IMPLEMENTATION_MISSING_HOUSEHOLD_ROSTER`. Its setup must prove auth, database, list, selector, and destination routing are healthy first. Add a static exactly-one-test guard and keep integration/tie cases outside this dedicated RED file.</action>
  <verify><automated>$count = (rg -n "^\s*test\(" e2e/households/roster-slice.spec.ts | Measure-Object).Count; if ($count -ne 1) { throw "roster-slice.spec.ts must contain exactly one test" }; powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite e2e -TestPath e2e/households/roster-slice.spec.ts -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_ROSTER</automated></verify>
  <done>The roster destination is truthfully RED after its list/context prerequisites are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the isolated roster API and owned destination</name>
  <files>apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/dto/membership.dto.ts, apps/api/src/openapi/generate-openapi.ts, apps/api/test/households/households.int.test.ts, packages/api-client/openapi.json, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, apps/client/src/features/households/household-api.ts, apps/client/src/features/households/household-settings.tsx, apps/client/app/(protected)/households/index.tsx, apps/client/app/(protected)/households/[id]/index.tsx, apps/client/src/ui/household-components.tsx, e2e/households/roster-slice.spec.ts</files>
  <action>Use the PATTERNS DTO/controller/service/OpenAPI/integration analogs to add guarded `getHousehold`: resolve actor membership from access claims plus route household ID, derive owner from `ownerMembershipId`, return only explicit household/member DTO fields, and deny outsiders without a partial projection. Define one server total order: OWNER/ADMIN/MEMBER, current actor first within role, NFC-normalized nickname locale ascending, canonical email ascending, membership ID then user ID code-point ascending. Assert operationId/schema/security before regenerating the three changed outputs and extend the narrow household API wrapper. Implement owned `MemberRow` and `RoleBadge`; build the thin `/households/[id]` route over `household-settings.tsx` with AppShell/HouseholdHeader, member overview, skeleton/error/inconsistent-owner state, and no Phase 3 content. Update `/households` and HouseholdCard only now to expose the concrete “查看成员” destination. Never re-sort the server projection with a different client comparator. Per D-12, membership loss freezes household actions, clears the lost household cache and device persistence, renders the explicit accessChanged explanation first, and only an explicit user action continues to `/households` when memberships remain or to the equal D-01 create/accept handoff when none remain; it never automatically selects or enters another household.</action>
  <verify><automated>pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts; pnpm --filter client test:quick -- household-settings; pnpm --filter client typecheck; pnpm exec playwright test e2e/households/roster-slice.spec.ts</automated></verify>
  <done>A member reaches a real isolated household destination and sees every member/role in one total order.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| route/cache -> household API | Route ID and cached membership/role are untrusted. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-07 | Elevation of Privilege | high | mitigate | V8.2.2, V8.3.1 | Fresh same-household item authorization is enforced at the service. |
| T-02-08 | Information Disclosure | high | mitigate | V8.2.2, V15.3.1 | Cross-household tests prove no roster item or excess field is returned. |
| T-02-09 | Tampering | medium | mitigate | V8.3.1 | Client route/cache data cannot change server ordering or authorization. |
| T-02-SC | Tampering | low | accept | N/A | Existing generated and UI stack only. |
</threat_model>
<verification>Focused roster integration, OpenAPI drift, component, route, and type gates pass.</verification>
<success_criteria>Every current member can enter an owned destination and view an isolated, authoritative, totally ordered roster.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-03-SUMMARY.md` when done.</output>
