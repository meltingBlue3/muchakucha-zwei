---
phase: 02-household-member-collaboration
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/dto/update-household.dto.ts
  - apps/api/src/modules/households/dto/membership.dto.ts
  - apps/api/src/openapi/generate-openapi.ts
  - apps/api/test/households/households.int.test.ts
  - packages/api-client/openapi.json
  - packages/api-client/src/generated/{models,client,index}.ts
  - apps/client/src/features/households/household-settings.tsx
  - apps/client/app/(protected)/households/[id]/index.tsx
  - apps/client/app/(protected)/households/[id]/settings.tsx
  - apps/client/src/ui/household-components.tsx
autonomous: true
requirements: [HHLD-04, HHLD-05, EXPR-02, SAFE-01]
must_haves:
  truths:
    - "A current member sees all members in the total order owner/admin/member, current user first within role, normalized nickname ascending, email ascending, then stable membership/user ID."
    - "Per D-04, a household rename form states its destination and saves without an extra confirmation."
    - "Cross-household route or membership IDs cannot reveal member data or authorize rename."
  artifacts:
    - "apps/client/src/features/households/household-settings.tsx"
    - "apps/client/src/ui/household-components.tsx"
  key_links:
    - "HouseholdHeader/current ID -> guarded member projection and updateHousehold -> authoritative refetch"
---

<objective>Deliver member visibility and destination-explicit household rename as one isolated household settings slice per D-02 and D-04.</objective>
<scope_rationale>This is one vertical settings increment: protected read/update contract, generated client, owned roster/form UI, and routes ship together. Plan 02-03 exclusively owns these Wave 3 edits; shared files are dependency-sequenced.</scope_rationale>
<execution_context>@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-member-collaboration/02-CONTEXT.md
@.planning/phases/02-household-member-collaboration/02-PATTERNS.md
@.planning/phases/02-household-member-collaboration/02-UI-SPEC.md
@.planning/phases/02-household-member-collaboration/02-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Activate member-view and rename RED contracts</name>
  <files>apps/api/test/households/households.int.test.ts, apps/client/src/features/households/household-settings.tsx</files>
  <behavior>
    - HHLD-05: member rows are owner/admin/member ordered; within a role current user is first, then normalized display nickname ascending, email ascending, and stable membership/user ID.
    - Duplicate nicknames and equal-role rows use email and stable ID tie-breaks identically on server and client.
    - D-04: rename displays `保存到：{家庭名称}` and commits directly.
    - SAFE-01: outsider and cross-household target tests return no member or household payload.
  </behavior>
  <action>Add one uniquely named API RED case `orders members and renames the current household [RED:HOUSEHOLD_SETTINGS]` plus component cases for duplicate normalized nicknames, equal roles, email ties, and stable-ID ties. Use exact marker `IMPLEMENTATION_MISSING_HOUSEHOLD_SETTINGS` only after Plans 02-01/02-02 are green.</action>
  <verify><automated>powershell -ExecutionPolicy Bypass -File scripts/assert-red.ps1 -Suite api -TestPath test/households/households.int.test.ts -TestName "orders members and renames the current household [RED:HOUSEHOLD_SETTINGS]" -MissingBehaviorMarker IMPLEMENTATION_MISSING_HOUSEHOLD_SETTINGS</automated></verify>
  <done>The settings contracts truthfully identify only the missing member/rename behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the guarded and totally ordered settings contract</name>
  <files>apps/api/src/modules/households/households.controller.ts, apps/api/src/modules/households/households.service.ts, apps/api/src/modules/households/dto/update-household.dto.ts, apps/api/src/modules/households/dto/membership.dto.ts, apps/api/src/openapi/generate-openapi.ts, apps/api/test/households/households.int.test.ts, packages/api-client/openapi.json, packages/api-client/src/generated/{models,client,index}.ts</files>
  <action>Use `update-me.dto.ts` for classified `update-household.dto.ts` and `register.dto.ts` for classified `membership.dto.ts`, plus documented guard/service/OpenAPI/integration analogs. Add explicit member/household DTOs and `updateHousehold`; derive owner from `ownerMembershipId`, recheck actor membership, and permit rename only to owner. Define server ordering: OWNER/ADMIN/MEMBER, current user first within role, NFC-normalized nickname locale-ascending, canonical email ascending, then membership ID/user ID code-point ascending. Test duplicate nicknames, equal roles, email ties, final IDs, isolation, and the 1-40 code-point rename rule. Generate the asserted contract.</action>
  <verify><automated>pnpm openapi:generate; pnpm openapi:check; pnpm --filter api test:integration -- --run test/households/households.int.test.ts</automated></verify>
  <done>The REST contract returns a minimal isolated roster in the exact total order and supports owner-only rename.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver the ordered roster and destination-explicit rename UI</name>
  <files>apps/client/src/features/households/household-settings.tsx, apps/client/app/(protected)/households/[id]/index.tsx, apps/client/app/(protected)/households/[id]/settings.tsx, apps/client/src/ui/household-components.tsx</files>
  <action>Using `primitives.tsx`/`profile-form.tsx`, implement owned `MemberRow`, `RoleBadge`, `HouseholdContextNote`, member route, and settings form. Preserve the server total order exactly on the client, including duplicate-nickname/email/ID tie cases; never re-sort with a different comparator. Provide D-02/D-04 persistent header, destination note, direct save, authoritative refetch, skeleton/error/empty states, and access-changed handling. Zero-owner/inconsistent data freezes governance and shows the UI-SPEC recovery copy.</action>
  <verify><automated>pnpm --filter client test:quick -- household-settings; pnpm --filter client typecheck</automated></verify>
  <done>Members and textual roles are visible in exact order and rename remains destination-explicit.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| route/cache -> household API | Household ID and cached role can be attacker-controlled or stale. |
## ASVS 5.0.0 / STRIDE Register
| Threat ID | STRIDE | Severity | Disposition | ASVS control (official level) | Planned evidence |
|---|---|---|---|---|---|
| T-02-07 | Elevation of Privilege | high | mitigate | V8.2.1, V8.2.2, V8.3.1 | Rename tests name owner function permission, household item permission, and trusted-service checks. |
| T-02-08 | Information Disclosure | high | mitigate | V8.2.2, V15.3.1 | Cross-household projection cases assert no unauthorized item or excess fields. |
| T-02-09 | Tampering | medium | mitigate | V8.3.1 | Stale-role tests prove client state cannot grant mutation and authoritative refetch controls UI state. |
| T-02-SC | Tampering | low | accept | N/A | Generated contract uses existing approved toolchain only; no install occurs. |
</threat_model>

<verification>OpenAPI drift, household integration, client component, and type gates pass.</verification>
<success_criteria>A member can inspect the current household roster, and its owner can rename the clearly identified destination.</success_criteria>
<output>Create `.planning/phases/02-household-member-collaboration/02-03-SUMMARY.md` when done.</output>
