---
phase: 07-recurring-events-tasks
plan: 05
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - apps/client/src/features/recurrence/recurrence-summary.tsx
  - apps/client/src/features/recurrence/recurrence-picker.tsx
  - apps/client/src/features/events/event-form.tsx
  - apps/client/src/features/tasks/task-form.tsx
  - apps/client/src/features/recurrence/__tests__/recurrence-summary-test.tsx
  - apps/client/src/features/recurrence/__tests__/recurrence-picker-test.tsx
autonomous: true
requirements: [RECR-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "新建事件/任务表单打开时频率固定为『不重复』，星期几、结束条件与摘要三块全部未挂载，不重复的用户看到的表单与 Phase 3/4 完全一致，没有多一次点击或多一屏滚动"
    - "『每周』频率下 byWeekday 永不为空：切换到每周时默认勾选开始日期所在的星期几；取消最后一个已选项为 no-op（chip 保持选中）并以 polite live region 播报『至少需要选择一天。』"
    - "结束条件选『截止日期』但未填写时阻止提交，DateField 下方渲染 FormMessage『请选择重复的截止日期。』并把焦点移到该字段；截止日期早于开始日期同样阻止提交并提示『截止日期必须晚于开始日期。』"
    - "结束条件的『截止日期』与『重复次数』互斥：切换 chip 时清空另一个字段的值，界面上任一时刻只有一个输入可见 (D-06)"
    - "重复次数输入允许 1–1000，超范围阻止提交并提示『重复次数需要在 1 到 1000 之间。』"
    - "频率与结束条件用 radiogroup / radio 语义，星期几用 checkbox 语义，三者均带 accessibilityState，选中态不依赖颜色"
    - "重复摘要是纯格式化结果：单日『每周三重复』，多日『每周二、四、六重复』（顿号分隔、周日→周六排序），每月『每月 12 日重复』，每年『每年 8 月 12 日重复』；追加结束条件时为『…，到 2027-08-12 为止』或『…，共 10 次』"
    - "频率为『每月』且开始日期的日 ≥ 29 时，摘要下方渲染一行 caption/inkMuted『有些月份没有这一天，会自动改到当月最后一天。』；开始日期的日 ≤ 28 时不渲染 (D-09)"
    - "规则时区取自设备（Intl.DateTimeFormat().resolvedOptions().timeZone），本阶段不提供时区选择控件；仅当规则时区与当前设备时区不同时追加一行『按 {IANA 时区} 的日期重复。』(D-10)"
    - "表单提交中重复区块内所有 chip 与输入 disabled 且保留已输入值，主按钮内显示 Spinner 且标签为『保存中…』"
    - "服务端 VALIDATION_FAILED 的 details[].field 映射到重复区块内的内联 FormMessage，焦点移到首个出错字段"
    - "编辑既有实例时重复区块与其余字段同批渲染，不会先显示『不重复』再跳成实际规则"
    - "任务表单在 status === 'cancelled' 时额外渲染一个选中态的只读『已取消』chip 与文本动作『恢复这一次』；正常任务的状态区保持现有三个 chip 不变"
    - "features/recurrence/** 下没有裸色值、裸间距、裸圆角、裸字号或裸阴影，全部走 useTheme<Theme>()"
    - statement: "重复摘要 + 月末钳位说明 + 时区注记三行叠加时（最长组合：每年 + 截止日期 + 长 IANA 时区名）不挤压相邻表单字段；摘要最多两行、超出以省略号截断但 accessibilityLabel 保留完整摘要"
      verification: backstop
  artifacts:
    - path: "apps/client/src/features/recurrence/recurrence-summary.tsx"
      provides: "formatRecurrenceSummary 纯函数 + RecurrenceSummary 展示组件（teal 信息块）"
      exports: ["formatRecurrenceSummary", "RecurrenceSummary"]
    - path: "apps/client/src/features/recurrence/recurrence-picker.tsx"
      provides: "受控的 RecurrencePicker：频率单选 + 星期几多选 + 结束条件；null 表示不重复"
      exports: ["RecurrencePicker", "RecurrenceInput"]
    - path: "apps/client/src/features/recurrence/__tests__/recurrence-picker-test.tsx"
      provides: "默认不重复、星期几永不为空、结束条件互斥、a11y 角色的组件断言"
  key_links:
    - from: "apps/client/src/features/tasks/task-form.tsx"
      to: "apps/client/src/features/recurrence/recurrence-picker.tsx"
      via: "截止日期与描述之间插入一个受控的 RecurrencePicker 区块"
      pattern: "RecurrencePicker"
    - from: "apps/client/src/features/recurrence/recurrence-picker.tsx"
      to: "packages/api-client/src/generated/models.ts"
      via: "RecurrenceInput 由生成的 RecurrenceDto 派生，客户端不自定义规则形状"
      pattern: "RecurrenceDto"
  prohibitions:
    - "MUST NOT 在界面上出现 RRULE / FREQ / BYDAY / interval / occurrenceDate 等技术词或内部字段名 —— 用户永远不需要用日历规范的词汇思考自己的家务安排"
    - "MUST NOT 让用户以为某个月的重复被静默跳过：每月锚点 ≥29 时必须在保存之前就把钳位规则用中文说清楚，而不是等到发生项生成之后才让用户自己发现"
    - "MUST NOT 在客户端推算任何未来发生日期 —— 摘要只是对已选规则的格式化，日期序列的唯一权威是服务端"
---

<objective>
让用户第一次能在应用里**亲手**设置重复规则：在事件表单与任务表单里插入一个受控的 `RecurrencePicker`，用中文说人话地表达「每周二、四、六重复，共 10 次」，并在保存前就把月末钳位与时区这两件容易误解的事讲清楚。

完成之后，Phase 7 的用户故事第一次端到端成立 —— 用户不再需要手动重建同一件事。

Purpose: UI-SPEC 已经把这个控件的结构、语义、文案与可访问性全部定死（`## RecurrencePicker Contract`），本计划的工作是忠实落地，而不是重新设计。
Output: 两个可复用组件 + 两个表单的插入点 + 覆盖空选拦截、互斥、a11y 角色与摘要全组合的组件测试。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-CONTEXT.md
@.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md
@.planning/phases/07-recurring-events-tasks/07-PATTERNS.md
@.planning/phases/07-recurring-events-tasks/07-03-SUMMARY.md
</context>

<!-- planner-discipline-allow: RRULE -->
<!-- planner-discipline-allow: BYDAY -->
<!-- planner-discipline-allow: FREQ= -->
<!-- planner-discipline-allow: react-hook-form -->

## Artifacts this plan produces

- `apps/client/src/features/recurrence/recurrence-summary.tsx` → `formatRecurrenceSummary`、`RecurrenceSummary`
- `apps/client/src/features/recurrence/recurrence-picker.tsx` → `RecurrencePicker`、类型 `RecurrenceInput`
- `apps/client/src/features/recurrence/__tests__/recurrence-summary-test.tsx`、`.../recurrence-picker-test.tsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecurrenceSummary —— 纯中文摘要格式化 + teal 信息块</name>
  <reversibility rating="reversible">纯展示层格式化函数，无持久化影响。</reversibility>
  <files>apps/client/src/features/recurrence/recurrence-summary.tsx, apps/client/src/features/recurrence/__tests__/recurrence-summary-test.tsx</files>
  <read_first>
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## RecurrencePicker Contract` → `### RecurrenceSummary（信息块）` 全节 —— 摘要文案表、月末钳位提示、时区注记三项的精确措辞
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Copywriting Contract` —— 所有文案的唯一事实源，逐字使用
    - `apps/client/src/features/events/calendar-utils.ts` 全文 —— 纯格式化模块的写法与 `DAY_NAMES` 的周日→周六排序（星期编号必须与之一致）
    - `apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx` 第 112-124 行 —— `tealSoft` 信息胶囊的既有样式写法
    - `apps/client/src/features/tasks/task-form.tsx` 第 59、112-133 行 —— `useTheme<Theme>()` + 令牌化样式对象的写法（禁止裸值）
    - `apps/client/src/features/profile/__tests__/profile-form-test.tsx` —— 客户端测试文件写法（注意 glob 是 `-test.tsx` 后缀，不是 `.test.tsx`）
  </read_first>
  <behavior>
先写 `recurrence-summary-test.tsx`，再写实现。`formatRecurrenceSummary(rule, deviceTimeZone)` 返回 `{ summary: string; clampNote: string | null; timeZoneNote: string | null }`，断言矩阵：

- 每天 → `每天重复`
- 每周单日（`byWeekday: [3]`）→ `每周三重复`
- 每周多日（`byWeekday: [2,4,6]`）→ `每周二、四、六重复`；顿号分隔；传入乱序 `[6,2,4]` 时输出顺序仍为周日→周六
- 每月（`startsOn: '2026-08-12'`）→ `每月 12 日重复`
- 每年（`startsOn: '2026-08-12'`）→ `每年 8 月 12 日重复`
- 追加 `endsOn: '2027-08-12'` → 摘要以 `，到 2027-08-12 为止` 结尾
- 追加 `count: 10` → 摘要以 `，共 10 次` 结尾
- 每月 + `startsOn` 的日为 29 / 30 / 31 → `clampNote` 等于 `有些月份没有这一天，会自动改到当月最后一天。`
- 每月 + `startsOn` 的日为 12 或 28 → `clampNote` 为 `null`
- 每天 / 每周 / 每年 + 任意日 → `clampNote` 恒为 `null`（钳位提示只属于每月）
- 规则时区等于设备时区 → `timeZoneNote` 为 `null`
- 规则时区为 `Asia/Tokyo` 而设备为 `Asia/Shanghai` → `timeZoneNote` 等于 `按 Asia/Tokyo 的日期重复。`
- 输出字符串中不出现 `RRULE`、`FREQ`、`BYDAY`、`interval`、`occurrenceDate` 任何一个

组件断言：`<RecurrenceSummary>` 渲染 `summary`；`clampNote` / `timeZoneNote` 非空时各多渲染一行；三行都在时三行都可读。
  </behavior>
  <action>
新建 `apps/client/src/features/recurrence/recurrence-summary.tsx`，导出一个纯函数与一个展示组件。

`formatRecurrenceSummary` 只做**格式化**，不做任何日期推算。禁止在客户端计算未来发生日期 —— 日期序列的唯一权威是服务端；界面显示什么，服务端就必须已经生成了什么。星期名映射与排序与 `calendar-utils.ts` 的 `DAY_NAMES`（周日→周六）保持一致，否则日历表头与摘要会给出互相矛盾的星期顺序。

全部文案逐字取自 UI-SPEC 的 `## Copywriting Contract` 表，不做同义改写。输出中不得出现日历规范的技术词或内部字段名 —— 用户永远不需要用这些词思考自己的家务安排。

`RecurrenceSummary` 组件：容器 `backgroundColor` 用 `theme.colors.tealSoft`、`borderRadius` 用 `theme.borderRadii.sm`、`padding` 用 `theme.spacing[3]`；主文字 `variant="caption"` + `color="teal"`；`clampNote` / `timeZoneNote` 各自一行 `variant="caption"` + `color="inkMuted"`。摘要 `numberOfLines={2}` 截断，但 `accessibilityLabel` 传完整摘要字符串（截断只影响视觉，不影响屏幕阅读器）。

样式一律走 `useTheme<Theme>()`，不得出现裸色值、裸间距、裸圆角、裸字号或裸阴影。
  </action>
  <verify>
    <automated>cd apps/client && pnpm test -- recurrence-summary && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/client && pnpm test -- recurrence-summary` 全绿。
    - `grep -c '每周二、四、六重复' apps/client/src/features/recurrence/__tests__/recurrence-summary-test.tsx` ≥ 1 且 `grep -c '有些月份没有这一天' apps/client/src/features/recurrence/__tests__/recurrence-summary-test.tsx` ≥ 1（多日摘要与钳位提示两组断言真实存在）。
    - `! grep -REn 'RRULE|BYDAY|FREQ=' apps/client/src/features/recurrence/recurrence-summary.tsx`
    - `! grep -REn '#[0-9A-Fa-f]{3,8}|rgba?\(' apps/client/src/features/recurrence/recurrence-summary.tsx`（无裸色值）
    - `cd apps/client && pnpm typecheck` 零错误。
  </acceptance_criteria>
  <done>四种频率 × 三种结束条件的全组合摘要文案正确；月末钳位提示只在每月且锚点日 ≥29 时出现；时区注记只在规则时区与设备时区不同时出现；三者都以令牌化样式渲染。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecurrencePicker —— 频率单选、星期几多选、结束条件与 a11y 语义</name>
  <reversibility rating="reversible">纯表单控件，受控组件形状由生成的 `RecurrenceDto` 派生。</reversibility>
  <precondition>`packages/api-client` 已导出 `RecurrenceDto` 类型（07-03 交付），`cd apps/client && pnpm typecheck` 可通过。</precondition>
  <files>apps/client/src/features/recurrence/recurrence-picker.tsx, apps/client/src/features/recurrence/__tests__/recurrence-picker-test.tsx</files>
  <read_first>
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## RecurrencePicker Contract` 全节 —— 结构树、频率控件、星期几多选、结束条件四小节；以及 `## Accessibility Contract`、`## Spacing Scale` 的例外条款（星期几 chip 宽可降至 44px 但高必须 48px 且带 hitSlop）
    - `apps/client/src/features/tasks/task-form.tsx` 第 10-21 行（常量元组选项列表）、第 112-133 行（`inputStyle` / `chipStyle` / `chipTextColor`）、第 154-193 行（**单选** chip：只有 `accessibilityLabel`，是较弱先例，不得照抄其 a11y 部分）、第 213-239 行（**多选** chip：`accessibilityRole="checkbox"` + `accessibilityState`，可直接照抄结构）
    - `apps/client/src/ui/date-field.tsx` 全文 —— `DateField` 的 props 与 `mode="date"` 用法
    - `apps/client/src/ui/primitives.tsx` —— `Stack` / `Text` / `FormMessage` 的可用变体
    - `packages/api-client/src/generated/models.ts` 中的 `RecurrenceDto`（07-03 产出）
  </read_first>
  <behavior>
先写 `recurrence-picker-test.tsx`，再写实现。断言：

- **默认不重复**：初次渲染时 `不重复` chip 的 `accessibilityState.checked` 为 true，且查询不到星期几 chip、结束条件 chip 与摘要块（三者全部未挂载，不是隐藏）
- **a11y 语义**：频率容器 `accessibilityRole="radiogroup"` 且 `accessibilityLabel="重复频率"`；5 个频率 chip 各为 `accessibilityRole="radio"` 且带 `accessibilityState.checked`；结束条件容器 `accessibilityLabel="重复结束条件"` 且三个 chip 为 `radio`；星期几 7 个 chip 为 `accessibilityRole="checkbox"` 且带 `accessibilityState.checked`
- **切到每周默认勾选开始日**：`startsOn` 为某个周三时切到 `每周`，星期三 chip 已 checked，且恰好只有它 checked
- **至少保留一天**：只剩一个已选星期时再点它 → 该 chip 仍 checked（no-op），且渲染了 `至少需要选择一天。` 的 live region 文本
- **结束条件互斥**：选 `截止日期` 填入值后切到 `重复次数` → 日期输入不再挂载且 `endsOn` 值被清空；再切回 `截止日期` → 次数输入不再挂载且 `count` 值被清空
- **次数默认与范围**：切到 `重复次数` 时输入框默认值为 `10`；输入 `0` 或 `1001` 时 `onValidityChange`（或等价的错误回调）报出 `重复次数需要在 1 到 1000 之间。`
- **截止日期未填拦截**：选 `截止日期` 但留空 → 渲染 `请选择重复的截止日期。`
- **截止日期早于开始拦截** → 渲染 `截止日期必须晚于开始日期。`
- **回到不重复**：从 `每周` 切回 `不重复` → `onChange` 收到 `null`，星期几/结束条件/摘要全部卸载
- **提交中禁用**：`disabled` 为 true 时全部 chip 与输入的 `accessibilityState.disabled` 为 true，但已选值不变
- **无 interval 输入**：任意频率下都查询不到「每 N 个」之类的间隔输入框（D-05 明确本阶段界面只支持 interval=1）
- **无技术词**：渲染树的全部文本中不出现 `RRULE`、`FREQ`、`BYDAY`、`interval`、`occurrenceDate`
  </behavior>
  <action>
新建 `apps/client/src/features/recurrence/recurrence-picker.tsx`，导出受控组件 `RecurrencePicker` 与类型 `RecurrenceInput`（由生成的 `RecurrenceDto` 派生，客户端不自定义规则形状）。Props：`value: RecurrenceInput | null`（`null` 表示不重复）、`onChange(next: RecurrenceInput | null)`、`startDate: string`（表单里的开始/截止日期，用于默认星期几与钳位提示）、`disabled?: boolean`、`errors?: Record<string, string>`（服务端 `details[].field` 映射进来的内联错误）。

结构严格按 UI-SPEC 的结构树：`Stack gap={1}` → `Text variant="label"` 值为 `重复` → 频率 chips → （每周时）星期几 chips → （非不重复时）`Text variant="label"` 值为 `结束` → 结束条件 chips → 对应输入 → `<RecurrenceSummary>`。选择「不重复」时后四块**全部卸载**（直接挂载/卸载，不做高度动画 —— 滚动表单里的高度动画会产生跳动）。

**频率控件**：5 个 chip（`不重复` / `每天` / `每周` / `每月` / `每年`），复用 `task-form.tsx` 现有的 `chipStyle` / `chipTextColor` 视觉。可访问性必须**高于**现有状态/优先级 chip 的实现：外层容器 `accessibilityRole="radiogroup"` + `accessibilityLabel="重复频率"`，每个 chip `accessibilityRole="radio"` + `accessibilityState={{ checked }}`。现有那两组 chip 只带 `accessibilityLabel`，是较弱先例，不得照抄其 a11y 部分。界面**不提供**周期间隔输入框（D-05）。

**星期几多选**：7 个 chip，标签 `日 一 二 三 四 五 六`，`accessibilityLabel` 为 `星期日` … `星期六`，排序固定周日→周六（与日历表头一致）。结构照抄 `task-form.tsx:213-239` 的负责人多选（`accessibilityRole="checkbox"` + `accessibilityState={{ checked }}`）。切到「每周」时默认勾选 `startDate` 所在的星期几，永不出现空选。取消最后一个已选项为 no-op（chip 保持选中），并通过 `accessibilityLiveRegion="polite"` 播报 `至少需要选择一天。`。宽度可降至 44px 以在 320px 内一行放 7 个，但**高度必须保持 48px** 并用 `hitSlop={theme.spacing[1]}` 补足横向可点区。

**结束条件**：3 个 chip（`永不结束` / `截止日期` / `重复次数`），`accessibilityRole="radio"`，容器 `accessibilityLabel="重复结束条件"`，默认 `永不结束`。选 `截止日期` 展开 `<DateField mode="date" label="截止日期" placeholder="YYYY-MM-DD" />`；选 `重复次数` 展开 `TextInput keyboardType="number-pad"`（复用 `inputStyle`），默认值 `10`，右侧静态后缀 `次`。两者互斥：切换时清空另一个字段的值，任一时刻只有一个输入可见（D-06）。

**校验与错误**：三条拦截文案逐字取自 UI-SPEC 的 `## Copywriting Contract`（`请选择重复的截止日期。` / `截止日期必须晚于开始日期。` / `重复次数需要在 1 到 1000 之间。`），用 `FormMessage` 渲染在对应字段下方并把焦点移到该字段。`errors` prop 里以 `recurrence.` 开头的键映射到对应字段的内联 `FormMessage`。

**时区**：规则时区取 `Intl.DateTimeFormat().resolvedOptions().timeZone`，本阶段**不提供**时区选择控件（D-10）。取值失败时按 UI-SPEC 渲染 `无法识别当前设备的时区。请检查系统时区设置后重试。`。

**表单状态**：沿用两个表单现有的 `useState` + `updateField` 模式。仓库依赖里那个表单库虽已安装，但事件表单与任务表单都没有导入它 —— 本阶段同样不引入。

样式全部走 `useTheme<Theme>()`；不得引入任何新依赖（segmented control、bottom sheet、日期库、RRULE 解析器一律禁止 —— UI-SPEC `## Registry Safety` 的新依赖禁令）。
  </action>
  <verify>
    <automated>cd apps/client && pnpm test -- recurrence-picker && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/client && pnpm test -- recurrence-picker` 全绿。
    - `grep -c 'radiogroup' apps/client/src/features/recurrence/recurrence-picker.tsx` ≥ 1 且 `grep -c 'accessibilityRole="radio"' apps/client/src/features/recurrence/recurrence-picker.tsx` ≥ 1 且 `grep -c 'accessibilityRole="checkbox"' apps/client/src/features/recurrence/recurrence-picker.tsx` ≥ 1。
    - `grep -c 'accessibilityLiveRegion' apps/client/src/features/recurrence/recurrence-picker.tsx` ≥ 1（空选拦截有播报）。
    - `grep -c '至少需要选择一天。' apps/client/src/features/recurrence/__tests__/recurrence-picker-test.tsx` ≥ 1。
    - `! grep -REn '#[0-9A-Fa-f]{3,8}|rgba?\(' apps/client/src/features/recurrence/recurrence-picker.tsx`（无裸色值）
    - `! grep -n 'react-hook-form' apps/client/src/features/recurrence/recurrence-picker.tsx`
    - `git diff --stat -- apps/client/package.json pnpm-lock.yaml` 为空（UI-SPEC 的新依赖禁令）。
    - `cd apps/client && pnpm typecheck` 零错误。
  </acceptance_criteria>
  <done>控件默认不重复；每周下星期几永不为空且空选被拦截并播报；结束条件互斥且各自有范围校验；频率/结束条件为 radio 语义、星期几为 checkbox 语义且都带 accessibilityState；无间隔输入框、无技术词、无新依赖。</done>
</task>

<task type="auto">
  <name>Task 3: 把 RecurrencePicker 接入事件表单与任务表单（含任务的已取消状态区）</name>
  <reversibility rating="reversible">表单接线，改动局限在两个组件内部。</reversibility>
  <files>apps/client/src/features/events/event-form.tsx, apps/client/src/features/tasks/task-form.tsx</files>
  <read_first>
    - `apps/client/src/features/tasks/task-form.tsx` 全文 —— 第 10-21 行状态常量、第 43-83 行 `useState` + `updateField`、第 152-193 行状态/优先级区块、第 244-251 行 `DateField`、第 254 行起的描述块（插入点在这两者之间）、以及提交按钮的 disabled/Spinner 处理
    - `apps/client/src/features/events/event-form.tsx` 全文 —— 与 task-form 同构；插入点在「结束」与「地点」之间
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Information Architecture and Screen Inventory` 表 —— 两个插入点的精确位置与「不改变现有字段顺序」的约束
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `### 已取消的单次实例（D-07）` —— 任务编辑表单里状态区的已取消分支与 `恢复这一次` 动作
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Loading, Empty, Success, and Error Behavior` —— 编辑既有实例时重复区块必须与其余字段同批渲染
    - `packages/api-client/src/generated/models.ts` 中的 `CreateEventDto` / `CreateTaskDto`（07-03 已加 `recurrence?`）
  </read_first>
  <action>
**(1) 插入点。** `task-form.tsx`：在 `DateField`（截止日期）与描述块之间插入 `<RecurrencePicker>`。`event-form.tsx`：在「结束」与「地点」之间插入。**不改变任何现有字段的顺序**，不改变现有 `submitLabel`（沿用 `保存` / `保存中…`）。

**(2) 表单状态。** 在两个表单既有的 `useState<...>` 表单对象里加一个 `recurrence: RecurrenceInput | null` 字段，初值为 `null`（新建）或由服务端返回的 `recurrence` 映射而来（编辑）。用既有的 `updateField('recurrence', next)` 回调接线。编辑既有实例时，重复区块必须与其余字段**同批渲染** —— 不得先渲染成「不重复」再跳成实际规则，那会让用户误以为规则丢了。数据到达前整个表单沿用既有的全屏加载指示器。

**(3) 提交。** 提交时若 `form.recurrence !== null` 就把它作为可选的 `recurrence` 字段放进 `CreateEventDto` / `CreateTaskDto`；为 `null` 时**完全不传该字段**（`exactOptionalPropertyTypes: true` 下用条件展开 `...(form.recurrence === null ? {} : { recurrence: form.recurrence })`）。不重复的用户提交的请求体与本阶段之前逐字节相同。

**(4) 提交中。** 把表单既有的 `isSubmitting` 传给 `<RecurrencePicker disabled>`：重复区块内所有 chip 与输入禁用且**保留已输入值**，主按钮内显示 `Spinner` 并保持稳定的标签宽度，禁止重复提交。

**(5) 服务端校验错误映射。** 捕获到 `VALIDATION_FAILED` 时把 `error.details` 中 `field` 以 `recurrence.` 开头的项组装成 `Record<string, string>` 传给 `<RecurrencePicker errors>`，并把焦点移到首个出错字段。其余 `field` 沿用表单现有的错误处理。可恢复失败时用 `Banner` 保留用户输入与当前上下文，**不清空**重复区块的任何已填值。

**(6) 任务表单的已取消状态区（D-07 客户端扇出点之一）。** 仅当 `form.status === 'cancelled'` 时，在状态区额外渲染一个选中态的**只读** `已取消` chip，并在其后提供文本动作 `恢复这一次`（点击后把 status 置回 `pending`）；点击三个正常状态 chip 中的任意一个同样视为恢复。**正常任务的状态区保持现有三个 chip 完全不变** —— 不重复、未取消的任务看到的表单不能有任何变化。
  </action>
  <verify>
    <automated>cd apps/client && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/client && pnpm test` 全绿（既有 profile / households / tasks 组件测试无回归）。
    - `cd apps/client && pnpm typecheck` 零错误。
    - `grep -c 'RecurrencePicker' apps/client/src/features/tasks/task-form.tsx` ≥ 1 且 `grep -c 'RecurrencePicker' apps/client/src/features/events/event-form.tsx` ≥ 1。
    - `grep -c '恢复这一次' apps/client/src/features/tasks/task-form.tsx` ≥ 1。
    - `git diff --stat -- apps/client/package.json pnpm-lock.yaml` 为空。
  </acceptance_criteria>
  <done>两个表单在指定插入点渲染重复区块且现有字段顺序不变；不设重复的提交请求体与本阶段之前一致；提交中重复区块禁用且保值；服务端重复字段校验错误落到内联位置；任务表单在已取消时提供只读 chip 与恢复动作，正常任务状态区不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → `RecurrenceInput` → API 请求体 | 客户端校验只是体验，服务端 DTO 与数据库 CHECK 才是权威 |
| 设备时区 → 规则 `timezone` | 由 `Intl` 读取，可能返回意外值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Denial of Service | 重复次数输入 | high | mitigate | 客户端把次数限制在 1–1000 并阻止越界提交；这只是第一道，服务端 `@Max(1000)`、数据库 `recurrence_rules_count_range_ck` 与生成器硬上限仍然是权威控制 |
| T-07-05 | Denial of Service | 设备时区读取 | medium | mitigate | `Intl.DateTimeFormat().resolvedOptions().timeZone` 取值失败或为空时渲染 `无法识别当前设备的时区。请检查系统时区设置后重试。` 并阻止提交，不把空值或垃圾值发给服务端 |
| T-07-13 | Tampering | 新增前端依赖 | high | mitigate | UI-SPEC `## Registry Safety` 明令本阶段所有控件只用 RN 的 `Pressable`/`View`/`TextInput`/`Modal` 与既有 primitives 实现；验收门要求 `apps/client/package.json` 与 `pnpm-lock.yaml` 的 diff 为空。若判定必须新增依赖，停止并走阻塞式人工审批（Phase 1 先例） |
| T-07-14 | Information Disclosure | 内联错误文案 | low | accept | 服务端错误信封只回传 `field` / `codes` / 面向用户的 message，不含内部标识；沿用既有映射方式，不额外处理 |
| T-07-SC | Tampering | npm 安装 | high | mitigate | 同 T-07-13：本计划零新增包，依赖清单 diff 必须为空 |
</threat_model>

<verification>
- `cd apps/client && pnpm test` 全绿
- `cd apps/client && pnpm typecheck` 零错误
- `apps/client/package.json` 与 `pnpm-lock.yaml` 无变动

<human-check>
在 320px 与 390px 宽度、100% 与 200% 系统字号下打开新建任务表单，选择「每年」+「截止日期」并把设备时区设为与规则不同的长名时区（如 `America/Argentina/Buenos_Aires`），确认摘要 + 月末钳位说明 + 时区注记三行叠加时不挤压相邻表单字段、摘要最多两行且截断后仍可通过屏幕阅读器听到完整内容。（对应 must_haves 中标记为 backstop 的那条）
</human-check>
</verification>

<success_criteria>
- 用户可以在事件与任务表单里设置四种预设频率、多选星期几与两种结束条件，并看到中文摘要。
- 不设重复的用户体验与 Phase 3/4 完全一致。
- 月末钳位与时区差异在保存**之前**就被明确告知。
- 三组控件的可访问性语义达到 UI-SPEC 要求（radiogroup/radio/checkbox + accessibilityState），且不依赖颜色传达选中态。
- 零新增前端依赖。
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-05-SUMMARY.md` when done
</output>
