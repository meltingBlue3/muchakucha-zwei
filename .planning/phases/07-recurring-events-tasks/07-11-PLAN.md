---
phase: 07-recurring-events-tasks
plan: 11
type: execute
wave: 8
depends_on: [07-09]
files_modified:
  - apps/api/src/modules/tasks/tasks.service.ts
  - apps/api/src/modules/tasks/tasks.controller.ts
  - apps/api/src/modules/events/events.service.ts
  - apps/api/src/modules/events/events.controller.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/src/generated/client.ts
  - packages/api-client/openapi.json
  - apps/api/test/recurrence/recurring-filter.int.test.ts
autonomous: true
requirements: [RECR-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /households/:householdId/tasks 与 GET /households/:householdId/events 都接受一个 recurring 查询参数：值为 true 时只返回 recurrenceRuleId 非空的实例，值为 false 时只返回 recurrenceRuleId 为空的实例，不传时行为与本计划之前逐字节一致 (D-15)"
    - "recurring 参数被当作固定取值的字符串枚举处理并映射到一个写死的 where 片段，任何其他取值都按不传处理；参数值绝不参与任何字符串拼接"
    - "两个 list 的 where 累加器由 Record<string, unknown> + as any 改成 Prisma.TaskWhereInput / Prisma.EventWhereInput，findMany 与 count 两处的 as any 断言随之消失（IN-03）"
    - "EventsService.list 的位置参数签名重构成与 TasksService.list 同形状的 ListFilters 对象，唯一调用方 EventsController 同步更新；startDate/endDate 的既有跨时区补偿逻辑逐字不变"
    - "packages/api-client 的 listTasks / listEvents 新增一个可选的 recurring 参数，位置在 signal 之前，所有既有调用方无需改动即可继续编译"
    - "新增的查询参数经由 generate-openapi.ts 的模板字符串与断言块产出，不是手改 packages/api-client/src/generated/* 得到的；pnpm openapi:check 对提交树零漂移"
    - "非家庭成员带 recurring 参数请求列表仍然得到 404 HOUSEHOLD_NOT_FOUND，家庭存在性不因新参数泄漏 (SAFE-01)"
  artifacts:
    - path: "apps/api/src/modules/events/events.service.ts"
      provides: "与任务侧同形状的 ListFilters 对象签名与类型化 where"
      contains: "ListFilters"
    - path: "apps/api/test/recurrence/recurring-filter.int.test.ts"
      provides: "recurring=true/false/缺省 三态在任务与事件两侧的集成断言，以及非成员 404"
      min_lines: 150
  key_links:
    - from: "apps/api/src/openapi/generate-openapi.ts"
      to: "packages/api-client/src/generated/client.ts"
      via: "clientSource 模板字符串是 listTasks/listEvents 签名的唯一真源，改完后由 pnpm openapi:generate 写出"
      pattern: "recurring"
---

<objective>
给任务与事件两个列表接口加上 recurring 查询参数，并在改这两行 where 的同时把它们从 `as any` 断言换成 Prisma 生成的类型。

Purpose: D-15 明确要求服务端提供这个筛选参数（客户端的 chip 是另一半，走客户端筛选）。同一处代码正是 IN-03 那两处 `as any` 的所在地 —— 给一个 `Record<string, unknown>` 加字段永远编译通过，这恰好是 IN-03 命名的风险，趁着动它把类型补上成本最低。
Output: 两个 list 的 recurring 支持、类型化 where、事件侧 list 的对象化签名、手写 OpenAPI 模板与断言的同步更新，以及一份三态集成回归。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-RESEARCH-ADDENDUM.md
@.planning/phases/07-recurring-events-tasks/07-09-SUMMARY.md
</context>

<artifacts_this_plan_produces>
新增符号：`ListFilters.recurring`（任务侧扩展）、`EventsService` 的 `ListFilters` 接口、`packages/api-client` 的 `listTasks(..., recurring?, signal?)` 与 `listEvents(..., recurring?, signal?)` 参数。
新增文件：`apps/api/test/recurrence/recurring-filter.int.test.ts`。
无新增 Prisma 迁移，无新增依赖，无破坏性契约变化（新参数一律可选）。
</artifacts_this_plan_produces>

<tasks>

<task type="tracer">
  <name>Task 1: 任务侧 recurring 参数端到端（服务 → 控制器 → OpenAPI → 生成客户端 → 集成断言）</name>
  <files>apps/api/src/modules/tasks/tasks.service.ts, apps/api/src/modules/tasks/tasks.controller.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/src/generated/client.ts, packages/api-client/openapi.json, apps/api/test/recurrence/recurring-filter.int.test.ts</files>
  <read_first>
    - `apps/api/src/modules/tasks/tasks.service.ts` 第 56-60 行（ListFilters）与 238-270 行（list 与两处 `as any`）
    - `apps/api/src/modules/tasks/tasks.controller.ts` 第 52-67 行（三个既有 `@ApiQuery` + `@Query` 参数）
    - `apps/api/src/openapi/generate-openapi.ts` 第 874-894 行（`listTasks` 的 clientSource 模板）与 1364-1376 行（series 路径的断言块，新断言照它的形状写）
    - `07-RESEARCH-ADDENDUM.md` §5 全节与 Pitfall 1（生成客户端的假象）
    - `apps/api/test/recurrence/materializer.int.test.ts` 第 29-91 行（fixture helper，复制到新测试文件）
  </read_first>
  <precondition>本地 PostgreSQL 测试库可连通且迁移已应用；`pnpm openapi:generate` 需要能启动 Nest 应用上下文。</precondition>
  <action>
1. `ListFilters` 接口加一个 `recurring?: string | undefined;`，并在字段上写注释说明它只接受 `'true'` 与 `'false'` 两个字面量、其余取值按不传处理。
2. `list()` 内的 `const where: Record<string, unknown> = { householdId };` 改成 `const where: Prisma.TaskWhereInput = { householdId };`（`Prisma` 类型从 `../../generated/prisma/client.js` 引入，形状照 `recurrence.service.ts` 第 2 行的 `import type { Prisma }`）。三个既有 `if` 分支照旧，末尾追加：`if (filters.recurring === 'true') where.recurrenceRuleId = { not: null };` 与 `else if (filters.recurring === 'false') where.recurrenceRuleId = null;`。绝不把 `filters.recurring` 拼进任何字符串或原始 SQL。
3. 删除 `findMany` 与 `count` 两处的 `as any`，直接传 `where`。若类型化后暴露出既有字段的类型不匹配，修正字段而不是恢复断言。
4. `tasks.controller.ts` 的 `list` 照 `assigneeId` 那一行的写法追加 `@ApiQuery({ name: 'recurring', required: false })` 与 `@Query('recurring') recurring?: string`，并把它传进 `this.tasksService.list(...)` 的 filters 对象。
5. `generate-openapi.ts` 的 `clientSource` 中 `listTasks` 的签名在 `signal?: AbortSignal` **之前**插入 `recurring?: boolean`，并在拼 URL 时按需追加查询串：只有 `recurring !== undefined` 才追加 `?recurring=${recurring ? 'true' : 'false'}`（注意 `listTasks` 现有实现如何拼接既有的 status/priority/assigneeId 查询串，沿用同一种拼法，不要另起一套）。在文件末尾的断言块中，紧邻 series 路径断言追加一条：任务与事件的 list 操作的 `parameters` 中都必须存在 name 为 `recurring` 的查询参数，缺失时 `throw new Error('OpenAPI recurring list filter parameters are missing or unstable.')`。**不要直接编辑 `packages/api-client/src/generated/*`** —— 它们由 `pnpm openapi:generate` 从这个文件的模板字符串写出，手改会被覆盖且 `openapi:check` 会失败。
6. 运行 `pnpm openapi:generate` 再运行 `pnpm openapi:check`，把两个生成产物与 `packages/api-client/openapi.json` 的改动一并提交。
7. 新建 `apps/api/test/recurrence/recurring-filter.int.test.ts`，fixture helper 从 `materializer.int.test.ts` 第 29-91 行复制（沿用每文件各自复制的既有约定）。任务侧覆盖四条：同一家庭内同时存在一条周期任务与一条普通任务时，`?recurring=true` 只返回周期那条、`?recurring=false` 只返回普通那条、不传参数时两条都返回且 `total` 与不传参数前一致；非家庭成员带 `?recurring=true` 请求得到 404 且响应体 code 为 `HOUSEHOLD_NOT_FOUND`。
  </action>
  <verify>
    <automated>pnpm openapi:check &amp;&amp; cd apps/api &amp;&amp; pnpm typecheck &amp;&amp; pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" apps/api/src/modules/tasks/tasks.service.ts` 为 0。
    - `grep -c "recurring" apps/api/src/modules/tasks/tasks.controller.ts` ≥ 2。
    - `grep -c "recurring" apps/api/src/openapi/generate-openapi.ts` ≥ 3（模板签名、URL 拼接、断言块）。
    - `pnpm openapi:check` 通过且 `git status --porcelain packages/api-client` 在 `pnpm openapi:generate` 后无未提交漂移。
    - 任务侧四条集成用例全部通过。
    - `cd apps/api && pnpm typecheck` 无错误。
  </acceptance_criteria>
  <done>任务列表接受 recurring 参数并按固定枚举映射到 where，两处 `as any` 消失，契约由 generate-openapi.ts 的模板与断言把守，四条集成断言覆盖三态与非成员边界。</done>
</task>

<task type="auto">
  <name>Task 2: 事件侧 recurring 参数与 list 签名对象化</name>
  <files>apps/api/src/modules/events/events.service.ts, apps/api/src/modules/events/events.controller.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/src/generated/client.ts, packages/api-client/openapi.json, apps/api/test/recurrence/recurring-filter.int.test.ts</files>
  <read_first>
    - `apps/api/src/modules/events/events.service.ts` 第 246-300 行（list 全文，含 startDate/endDate 的跨时区补偿注释）
    - `apps/api/src/modules/events/events.controller.ts` 第 56-68 行（唯一调用方）
    - Task 1 在 `tasks.service.ts` / `generate-openapi.ts` 中留下的形状（事件侧逐项对齐）
  </read_first>
  <precondition>本地 PostgreSQL 测试库可连通且迁移已应用。</precondition>
  <action>
1. 在 `events.service.ts` 中新增与任务侧同形状的 `interface ListFilters { startDate?: string | undefined; endDate?: string | undefined; recurring?: string | undefined; }`，把 `list(actorId, householdId, startDate?, endDate?)` 改成 `list(actorId, householdId, filters: ListFilters = {})`。方法体内 `startDate`/`endDate` 的读取改为从 `filters` 取值，**其余一字不改** —— 特别是那两段解释为什么要把 `startDate` 往前挪一天、把 `endDate` 往后挪一天的注释与逻辑，逐字保留。
2. `const where: Record<string, unknown> = { householdId, cancelledAt: null };` 改成 `Prisma.EventWhereInput`，删除 `findMany` 与 `count` 两处 `as any`。追加与任务侧完全同形的 recurring 分支。
3. `events.controller.ts` 的 `list` 追加 `@ApiQuery({ name: 'recurring', required: false })` 与 `@Query('recurring') recurring?: string`，调用改为传 filters 对象。⚠ 该控制器仍在用 `@Param() params: HouseholdIdParam` 这种交叉类型参数 DTO —— 本计划**不**动它（超出范围），但也**不要**把这种写法复制到任何新代码里；新参数只走 `@Query`。
4. `generate-openapi.ts` 的 `clientSource` 中 `listEvents` 按 Task 1 对 `listTasks` 的同样做法插入 `recurring?: boolean`（位置在 `signal` 之前）并拼接查询串。Task 1 已加的断言块条目此时应当对事件侧也生效，确认它同时检查两个 list 操作。
5. 重新运行 `pnpm openapi:generate` 与 `pnpm openapi:check`。
6. 在 `recurring-filter.int.test.ts` 追加事件侧的对应四条：`?recurring=true` / `?recurring=false` / 不传 的三态，以及非成员 404。额外加一条回归：同时带 `startDate`/`endDate` 与 `recurring=true` 时，日期范围过滤与重复过滤同时生效（证明签名重构没有丢掉日期分支）。
  </action>
  <verify>
    <automated>pnpm openapi:check &amp;&amp; cd apps/api &amp;&amp; pnpm typecheck &amp;&amp; pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" apps/api/src/modules/events/events.service.ts` 为 0。
    - `grep -c "interface ListFilters" apps/api/src/modules/events/events.service.ts` 为 1，且 `events.controller.ts` 中对 `this.eventsService.list(` 的调用传入的是一个对象字面量而不是三个位置参数。
    - `grep -c "HouseholdIdParam" apps/api/src/modules/events/events.controller.ts` 与本任务开始前相同（未扩散也未顺手重构）。
    - `pnpm openapi:check` 通过；`pnpm openapi:generate` 后工作树对 `packages/api-client` 无额外漂移。
    - 事件侧五条集成用例（三态 + 404 + 日期与重复叠加）全部通过。
    - `pnpm --filter api test:integration` 全绿，`apps/api/test/events` 与 `apps/api/test/tasks` 下的既有用例未回归。
  </acceptance_criteria>
  <done>事件列表与任务列表在 recurring 参数上行为一致、签名同形、where 均已类型化，日期范围过滤在签名重构后仍然正确。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → `GET .../tasks` / `GET .../events` 查询串 | 未受信输入；新参数直接参与数据库查询条件的选择 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-28 | Tampering | `recurring` 查询参数被当作可注入的过滤表达式 | high | mitigate | 只比较 `'true'` / `'false'` 两个字面量并映射到写死的 `where` 片段；参数值不参与任何拼接、不进入原始 SQL；集成用例覆盖三态 |
| T-07-29 | Information disclosure | 新参数绕开家庭成员校验，泄漏他家的周期实例 | high | mitigate | `list()` 的 `resolveActorRole` → 404 `HOUSEHOLD_NOT_FOUND` 前置守卫不变；两侧各有一条非成员 404 集成断言 |
| T-07-30 | Tampering | `where` 仍是 `Record<string, unknown>` + `as any`，新字段拼错也能编译并静默返回错误集合 | medium | mitigate | 改用 `Prisma.TaskWhereInput` / `Prisma.EventWhereInput`，删除两侧共四处断言（IN-03） |
| T-07-31 | Denial of Service | `recurring=false` 在大家庭上返回全部一次性实例 | low | accept | 与不传参数时的返回规模同量级，既有列表接口本就无分页；不引入新的放大面 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增任何依赖，仅重跑既有的 `pnpm openapi:generate` 脚本；无安装步骤故无供应链检查点 |
</threat_model>

<verification>
- `pnpm openapi:check` 通过
- `cd apps/api && pnpm typecheck` 无错误
- `pnpm --filter api test:integration` 全绿
- `grep -rn "as any" apps/api/src/modules/tasks/tasks.service.ts apps/api/src/modules/events/events.service.ts` 无输出
- `git status --porcelain packages/api-client` 在生成后无未提交漂移
</verification>

<success_criteria>
- 两个列表接口的 recurring 三态行为一致且可断言
- 两侧 where 全部类型化，四处 `as any` 归零
- 生成客户端的新参数来自 generate-openapi.ts 模板而非手改产物
- 既有调用方零改动即可编译，已发布客户端契约不被破坏
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-11-SUMMARY.md` when done
</output>
</content>
