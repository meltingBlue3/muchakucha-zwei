---
phase: 07-recurring-events-tasks
plan: 12
type: execute
wave: 9
depends_on: [07-11]
files_modified:
  - apps/api/src/modules/recurrence/recurrence-date.ts
  - apps/api/src/modules/recurrence/recurrence-date.test.ts
  - apps/api/src/modules/recurrence/dto/recurrence.dto.ts
  - apps/api/src/modules/recurrence/recurrence.service.ts
  - apps/api/src/modules/recurrence/recurrence.controller.ts
  - apps/api/src/modules/recurrence/recurrence.module.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - packages/api-client/openapi.json
  - apps/api/test/recurrence/recurrence-rules-api.int.test.ts
autonomous: true
requirements: [RECR-01, RECR-02]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /households/:householdId/recurrence-rules 按家庭列出规则本身（不是实例），每项含规则 id、类型、模板标题、频率字段、结束条件、时区与下一次发生日期 (D-16)"
    - "nextOccurrenceDate 由规则推算而不是查已生成的行 —— 一条健康的每周规则在提前量窗口外没有任何未来实例行，它的下一次发生仍然必须被正确返回 (D-16)"
    - "规则的 kind 由它拥有的实例关系推导（tasks 计数与 events 计数），不新增任何数据库列、不新增迁移；两侧计数都为 0 的历史规则返回 null 而不是猜一个类型 (D-16, D-17)"
    - "列表按契约排序：未结束的规则在前、按下一次发生日期升序；已结束的规则在后、按模板标题升序；已结束的规则保留在列表里而不是被隐藏 (D-16)"
    - "列表与详情响应都不包含 createdBy —— 界面不复制权限判断，规则数据里不下发创建者身份"
    - "GET /households/:householdId/recurrence-rules/:ruleId 对不存在或跨家庭的 ruleId 返回 404 RECURRENCE_RULE_NOT_FOUND，对非家庭成员返回 404 HOUSEHOLD_NOT_FOUND —— 两者都先于任何角色判断 (SAFE-01)"
    - "POST /households/:householdId/recurrence-rules/:ruleId/end 复用与 deleteSeriesFromOccurrence(this_and_following) 完全相同的事务体：设置 endsOn、清空 count、删除锚点及以后的已生成实例 (D-14)"
    - "结束操作的锚点是规则时区的明天 —— 今天这一次可能已经被完成，结束不吞掉它；执行后 endsOn 等于规则时区的今天 (D-14)"
    - "结束逻辑只有一份实现：deleteSeriesFromOccurrence 与 endRule 共用抽出的 endSeriesAt，两条路径不可能各自漂移 (D-14)"
    - "普通成员只能结束自己创建的规则，管理员与所有者可以结束任意规则；被拒绝时返回 403 FORBIDDEN 而不是 404"
    - "两个新端点都挂 AccessTokenGuard 与 ApiBearerAuth，路径参数各自用 ParseUUIDPipe({ version: '4' }) 校验，绝不使用交叉类型的参数 DTO"
    - "两个新操作与新 schema 由 generate-openapi.ts 的模板字符串与断言块产出并有稳定 operationId；pnpm openapi:check 对提交树零漂移"
    - "本计划不新增任何 Prisma 迁移"
  artifacts:
    - path: "apps/api/src/modules/recurrence/recurrence-date.ts"
      provides: "由规则推算下一次发生日期的纯函数"
      exports: ["nextOccurrenceFor"]
    - path: "apps/api/src/modules/recurrence/dto/recurrence.dto.ts"
      provides: "规则列表项与列表响应的 DTO"
      exports: ["RecurrenceRuleListItemDto", "RecurrenceRuleListResponseDto"]
    - path: "apps/api/src/modules/recurrence/recurrence.controller.ts"
      provides: "规则级子资源控制器（列表 / 详情 / 结束）"
      exports: ["RecurrenceRulesController"]
    - path: "apps/api/src/modules/recurrence/recurrence.service.ts"
      provides: "规则列表与详情读取、共享的锚点结束事务体、规则级结束"
      contains: "endSeriesAt"
    - path: "apps/api/test/recurrence/recurrence-rules-api.int.test.ts"
      provides: "规则列表/详情/结束三条路径的集成回归，含跨家庭 404、成员 403 与锚点边界"
      min_lines: 220
  key_links:
    - from: "apps/api/src/modules/recurrence/recurrence.service.ts"
      to: "apps/api/src/modules/recurrence/recurrence-date.ts"
      via: "listRules 用 currentCalendarDateIn(rule.timezone) + nextOccurrenceFor 推算每条规则的下一次发生；endRule 用同一个 currentCalendarDateIn 求锚点"
      pattern: "currentCalendarDateIn"
    - from: "apps/api/src/modules/recurrence/recurrence.service.ts"
      to: "apps/api/src/modules/recurrence/recurrence.service.ts"
      via: "deleteSeriesFromOccurrence 的 this_and_following 分支与 endRule 都调用同一个私有 endSeriesAt"
      pattern: "endSeriesAt"
---

<objective>
新增一个规则级子资源：按家庭列出重复规则、按 id 读一条规则、以及不依赖「先选中某次发生」的「结束此重复」。

Purpose: 「我一共设了哪些重复」和「怎么让某一条停下来」这两个问题在实例视图里无处可问 —— 用户只能先找到某一次发生、再从它的删除路径里选此后所有，既不可发现，语义上也读作删除。提前量窗口收窄之后更糟：一条健康的每周规则经常一条未来实例都没有，连可选中的锚点都不存在。
Output: `nextOccurrenceFor` 纯函数、两个规则 DTO、`RecurrenceRulesController`（GET 列表 / GET 详情 / POST 结束）、抽出的共享 `endSeriesAt`、同步的手写 OpenAPI 契约与一份集成回归。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-CONTEXT.md
@.planning/phases/07-recurring-events-tasks/07-RESEARCH-ADDENDUM.md
@.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md
@.planning/phases/07-recurring-events-tasks/07-09-SUMMARY.md
@.planning/phases/07-recurring-events-tasks/07-11-SUMMARY.md
</context>

<artifacts_this_plan_produces>
新增符号：`nextOccurrenceFor`（`recurrence-date.ts`）、`RecurrenceRuleListItemDto`、`RecurrenceRuleListResponseDto`（`dto/recurrence.dto.ts`）、`RecurrenceRulesController`（`recurrence.controller.ts`）、`RecurrenceService.listRules`、`RecurrenceService.getRule`、`RecurrenceService.endRule`、私有 `RecurrenceService.endSeriesAt`。
新增 OpenAPI 操作：`listRecurrenceRules`、`getRecurrenceRule`、`endRecurrenceRule`；新增生成客户端方法同名。
新增文件：`apps/api/test/recurrence/recurrence-rules-api.int.test.ts`。
无新增 Prisma 迁移，无新增依赖。
</artifacts_this_plan_produces>

<tasks>

<task type="tracer">
  <name>Task 1: 规则列表与详情端到端（推算下一次发生 → DTO → 控制器 → OpenAPI → 集成断言）</name>
  <files>apps/api/src/modules/recurrence/recurrence-date.ts, apps/api/src/modules/recurrence/recurrence-date.test.ts, apps/api/src/modules/recurrence/dto/recurrence.dto.ts, apps/api/src/modules/recurrence/recurrence.service.ts, apps/api/src/modules/recurrence/recurrence.controller.ts, apps/api/src/modules/recurrence/recurrence.module.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts, packages/api-client/openapi.json, apps/api/test/recurrence/recurrence-rules-api.int.test.ts</files>
  <read_first>
    - `apps/api/src/modules/recurrence/recurrence-date.ts` 第 1-45 行与 141-205 行（`WalkRule`、`WalkOptions`、`walkOccurrences` 的 `from` 语义与两个上限常量的注释）
    - `apps/api/src/modules/recurrence/recurrence.service.ts` 第 1-90 行（`resolveActorRole`、`canMutate`、`resolveRuleForOccurrence` 的 404 文案与顺序）
    - `apps/api/src/modules/recurrence/recurrence.controller.ts` 全文（`uuidParam` 的注释解释了为什么不能用交叉类型参数 DTO）
    - `apps/api/src/modules/recurrence/dto/recurrence.dto.ts` 第 95-107 行（`RecurrenceResponseDto` 的日期字段格式约定）
    - `apps/api/prisma/schema.prisma` 第 186-219 行（`RecurrenceRule` 全部列与 `events` / `tasks` 两个关系）
    - `apps/api/src/openapi/generate-openapi.ts` 第 240-260 行（`RecurrenceResponseDto` 的 models 模板）、第 385-400 行（`LabelListResponseDto` 的列表响应形状）、第 1068-1080 行（`listLabels` 的 client 模板）、第 1364-1376 行（断言块形状）
    - `07-RESEARCH-ADDENDUM.md` §8 全节
    - `07-UI-SPEC.md` 第 450-476 行（列表行要展示什么、排序、已结束规则保留）
  </read_first>
  <precondition>本地 PostgreSQL 测试库可连通且迁移已应用；`pnpm openapi:generate` 能启动 Nest 应用上下文。</precondition>
  <action>
**a) 纯函数。** 在 `recurrence-date.ts` 新增并导出 `nextOccurrenceFor(rule: WalkRule, from: CalendarDate, lookaheadDays = 400): CalendarDate | null`，实现为 `walkOccurrences(rule, { horizon: addDays(from, lookaheadDays), from })[0] ?? null`。写注释说明 400 天是为了覆盖 yearly，以及为什么必须走这个走子而不是查已生成的行（提前量窗口下健康的周规则经常零未来实例行）。在 `recurrence-date.test.ts` 补单测：weekly 规则返回从 `from` 起最近的选中星期几；monthly 锚点为 31 日时返回钳位后的日期；`count` 已用尽的规则返回 null；`endsOn` 早于 `from` 的规则返回 null；yearly 规则在 `from` 之后 300 天时仍能返回（证明 400 天窗口够用）。

**b) DTO。** 在 `dto/recurrence.dto.ts` 新增 `RecurrenceRuleListItemDto`，字段与来源如下：`id`（列）、`kind: 'task' | 'event' | null`、`title`（来自 `templateTitle`，**不是**任何实例的标题 —— 实例可被单独改名）、`freq`、`interval`、`byWeekday`、`startsOn`、`endsOn`（可空）、`count`（可空）、`timezone`、`startTimeLocal`（可空）、`durationMinutes`（可空）、`nextOccurrenceDate`（可空）。所有日期字段一律 `YYYY-MM-DD` 字符串，格式化方式照 `RecurrenceResponseDto` 既有约定。**不要**加入 `createdBy`、`householdId` 或 `materializedThrough`。再新增 `RecurrenceRuleListResponseDto { rules: RecurrenceRuleListItemDto[]; total: number }`，形状照 `LabelListResponseDto`。给 `kind` 写一行注释说明它为什么可空：结束或拆系列可能把一条规则的全部实例行都删光，此时类型无从推导，返回 null 好过猜。

**c) 服务。** 在 `RecurrenceService` 新增 `listRules(actorId, householdId): Promise<RecurrenceRuleListResponseDto>` 与 `getRule(actorId, householdId, ruleId): Promise<RecurrenceRuleListItemDto>`。两者都以既有的 `resolveActorRole` → `null` 时抛 404 `{ code: 'HOUSEHOLD_NOT_FOUND', message: 'Household not found.' }` 开场（读取是家庭内共享的，不做 `canMutate` 限制）。`listRules` 用 `findMany({ where: { householdId }, include: { _count: { select: { tasks: true, events: true } } } })` 取规则；`kind` 由 `_count.tasks > 0 ? 'task' : _count.events > 0 ? 'event' : null` 推导。每条规则的 `nextOccurrenceDate` 用 `nextOccurrenceFor({ freq, interval, byWeekday, startsOn, endsOn, count }, currentCalendarDateIn(rule.timezone))` 推算 —— 注意 `today` 必须按**每条规则自己的时区**求，不能整个列表共用一个。排序在服务端完成：`nextOccurrenceDate` 非空的在前，按该日期升序、同日期按 `title` 升序；`nextOccurrenceDate` 为 null 的在后，按 `title` 升序。`total` 为 `rules.length`。`getRule` 走同一套映射，规则为 null 或 `rule.householdId !== householdId` 时抛 404 `{ code: 'RECURRENCE_RULE_NOT_FOUND', message: 'Recurrence rule not found.' }`（与 `resolveRuleForOccurrence` 现有字面量一致）。

**d) 控制器与模块。** 在 `recurrence.controller.ts` 新增 `@Controller('households/:householdId/recurrence-rules')` 的 `RecurrenceRulesController`，带 `@ApiTags('recurrence')`、`@ApiBearerAuth()`、`@UseGuards(AccessTokenGuard)`，复用文件顶部第 20 行那个模块级 `uuidParam` 常量做每个路径参数的管道。`@Get()` → `operationId: 'listRecurrenceRules'`、`@ApiOkResponse({ type: RecurrenceRuleListResponseDto })`；`@Get(':ruleId')` → `operationId: 'getRecurrenceRule'`、`@ApiOkResponse({ type: RecurrenceRuleListItemDto })`。在 `recurrence.module.ts` 的 `controllers` 数组里追加这个类。

**e) OpenAPI。** 在 `generate-openapi.ts` 的 `modelsSource` 中新增 `RecurrenceRuleListItemDto` 与 `RecurrenceRuleListResponseDto` 两个 interface（字段与可空性与 DTO 逐项一致），把它们加进 `clientSource` 顶部的 `import type { ... }` 列表，并新增 `listRecurrenceRules(accessToken, householdId, signal?)` 与 `getRecurrenceRule(accessToken, householdId, ruleId, signal?)` 两个方法，形状照 `listLabels`。在断言块中追加一条：`/api/v1/households/{householdId}/recurrence-rules` 的 `get.operationId` 必须是 `listRecurrenceRules`、`/api/v1/households/{householdId}/recurrence-rules/{ruleId}` 的 `get.operationId` 必须是 `getRecurrenceRule`、且两个新 schema 都存在，否则 `throw new Error('OpenAPI recurrence rule operations or schemas are missing or unstable.')`。**不要手改 `packages/api-client/src/generated/*`**，改完模板后运行 `pnpm openapi:generate` 与 `pnpm openapi:check`。

**f) 集成测试。** 新建 `apps/api/test/recurrence/recurrence-rules-api.int.test.ts`，fixture helper 从 `materializer.int.test.ts` 第 29-91 行复制。至少覆盖：一条每周规则在提前量窗口外（下一次发生 7 天以后、因此没有任何未来实例行）时列表仍返回正确的 `nextOccurrenceDate`；一条任务规则与一条事件规则同时存在时二者的 `kind` 分别为 `'task'` 与 `'event'` 且出现在同一个列表里（D-20 合并展示）；`title` 等于规则的模板标题而非被单独改名后的实例标题（先改一条实例的标题再断言）；`count` 已用尽的规则 `nextOccurrenceDate` 为 null 且排在未结束规则之后；响应体的每一项都不含 `createdBy` 与 `householdId` 键；非家庭成员请求列表得到 404 `HOUSEHOLD_NOT_FOUND`；用另一个家庭的 ruleId 请求详情得到 404 `RECURRENCE_RULE_NOT_FOUND`；格式非法的 ruleId 得到 400 而不是 500。
  </action>
  <verify>
    <automated>pnpm openapi:check &amp;&amp; cd apps/api &amp;&amp; pnpm typecheck &amp;&amp; pnpm test:quick &amp;&amp; pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function nextOccurrenceFor" apps/api/src/modules/recurrence/recurrence-date.ts` 为 1；`pnpm --filter api test:quick` 中针对它的用例数 ≥ 5。
    - `grep -c "RecurrenceRulesController" apps/api/src/modules/recurrence/recurrence.module.ts` 为 1。
    - `grep -c "createdBy" apps/api/src/modules/recurrence/dto/recurrence.dto.ts` 在本任务前后不变，且 `RecurrenceRuleListItemDto` 的类体内不含 `createdBy` 或 `householdId`。
    - `grep -c "_count" apps/api/src/modules/recurrence/recurrence.service.ts` ≥ 1，且 `git status --porcelain apps/api/prisma/migrations` 无新增目录。
    - `pnpm openapi:check` 通过；`pnpm openapi:generate` 后工作树对 `packages/api-client` 无额外漂移。
    - 上述八条集成断言全部通过，其中「窗口外的每周规则仍返回正确 nextOccurrenceDate」这一条在被删除后会导致用例失败（它是本任务最核心的回归）。
  </acceptance_criteria>
  <done>规则列表与详情可用，下一次发生日期由规则推算而非查行，类型由实例关系计数推导且零迁移，排序与不下发创建者身份两条契约各有断言把守。</done>
</task>

<task type="auto">
  <name>Task 2: 抽出共享的锚点结束事务体，并新增规则级「结束此重复」</name>
  <files>apps/api/src/modules/recurrence/recurrence.service.ts, apps/api/src/modules/recurrence/recurrence.controller.ts, apps/api/src/openapi/generate-openapi.ts, packages/api-client/src/generated/client.ts, packages/api-client/openapi.json, apps/api/test/recurrence/recurrence-rules-api.int.test.ts</files>
  <read_first>
    - `apps/api/src/modules/recurrence/recurrence.service.ts` 第 262-303 行（`deleteSeriesFromOccurrence` 全文，要抽出的就是它 `this_and_following` 分支的事务体）
    - `apps/api/src/modules/recurrence/recurrence.controller.ts` 第 43-57 行（`@Delete` + `@HttpCode(204)` + `@ApiNoContentResponse` 的既有形状）
    - `07-RESEARCH-ADDENDUM.md` §6 全节与 Code Examples 中的 `endRule` 骨架
    - `07-UI-SPEC.md` 第 514-529 行（锚点为明天的理由与权限呈现约定）
    - `.planning/STATE.md` 中 `Count-based source rules clear count atomically when a split date-bounds them` 一条
  </read_first>
  <precondition>本地 PostgreSQL 测试库可连通且迁移已应用。</precondition>
  <action>
**a) 抽公共。** 新增私有方法 `private async endSeriesAt(tx: TransactionClient, ruleId: string, anchor: Date): Promise<void>`，把 `deleteSeriesFromOccurrence` 的 `this_and_following` 分支里「更新规则 + 清理实例」那段搬进去：先 `tx.recurrenceRule.update({ where: { id: ruleId }, data: { endsOn: new Date(\`${formatIsoDate(addDays(parseIsoDate(anchor.toISOString().slice(0, 10)), -1))}T00:00:00.000Z\`), count: null } })`，再对 `tx.task` 与 `tx.event` **各执行一次** `deleteMany({ where: { recurrenceRuleId: ruleId, occurrenceDate: { gte: anchor } } })`。写注释说明为什么两侧都发：一条规则只可能拥有其中一个关系的行，另一侧是零行 no-op，这样这段逻辑就不需要知道 kind，两个调用方也就不可能因为 kind 判别方式不同而漂移。改造 `deleteSeriesFromOccurrence`，让它在权限校验之后直接调 `endSeriesAt(tx, occurrence.rule.id, occurrence.occurrenceDate)`；它对外的行为、错误码与错误文案一字不改（既有的 `/series` 集成用例必须原样通过）。

**b) 规则级结束。** 新增 `async endRule(actorId: string, householdId: string, ruleId: string): Promise<void>`：
- `resolveActorRole` 为 null → 404 `{ code: 'HOUSEHOLD_NOT_FOUND', message: 'Household not found.' }`；
- 在 `this.prisma.$transaction` 内 `findUnique` 取规则，`rule === null || rule.householdId !== householdId` → 404 `{ code: 'RECURRENCE_RULE_NOT_FOUND', message: 'Recurrence rule not found.' }`（先于任何角色判断，SAFE-01）；
- `!this.canMutate(role, rule.createdBy, actorId)` → 403 `{ code: 'FORBIDDEN', message: 'Only the creator, admin, or owner can end this series.' }`；
- 锚点 `const anchor = databaseDate(addDays(currentCalendarDateIn(rule.timezone), 1))` —— 即规则时区的明天。写注释说明这是刻意的：今天这一次可能已经被完成，按今天为锚会把它一并清掉，那与这个操作的名字矛盾。`databaseDate` 若在本文件中不存在，就地写一个与 materializer 中同形的两行私有助手，不要跨模块导入私有实现；
- 调 `endSeriesAt(tx, rule.id, anchor)`。

**c) 控制器与契约。** 在 Task 1 建好的 `RecurrenceRulesController` 上追加 `@Post(':ruleId/end')` + `@HttpCode(204)` + `@ApiOperation({ operationId: 'endRecurrenceRule' })` + `@ApiNoContentResponse()`，两个路径参数都过 `uuidParam`。在 `generate-openapi.ts` 的 `clientSource` 新增 `endRecurrenceRule(accessToken, householdId, ruleId, signal?): Promise<void>`（形状照 `deleteTaskSeries`，但方法为 `'POST'`、body 传 `undefined`），并把断言块里 Task 1 新增的那条扩展为同时检查 `/api/v1/households/{householdId}/recurrence-rules/{ruleId}/end` 的 `post.operationId` 为 `endRecurrenceRule`。重新运行 `pnpm openapi:generate` 与 `pnpm openapi:check`。

**d) 集成测试。** 在 `recurrence-rules-api.int.test.ts` 追加：
- 锚点边界：一条每日规则同时拥有「今天」与「明天」两条实例行时，结束之后今天那条仍在、明天那条消失，且规则的 `ends_on` 等于规则时区的今天；
- 历史保留：结束之前存在的过去实例行在结束之后一行不少；
- `count` 清空：一条带 `count` 的规则被结束后 `count` 为 NULL 且 `ends_on` 非空（保持 endsOn/count 互斥约束）；
- 事件规则同样适用（用一条事件规则重复上面第一条断言）；
- 跨家庭 ruleId → 404 `RECURRENCE_RULE_NOT_FOUND`；非家庭成员 → 404 `HOUSEHOLD_NOT_FOUND`；普通成员对他人创建的规则 → 403 `FORBIDDEN`；同一普通成员对自己创建的规则 → 204；
- 结束后该规则仍出现在 `listRecurrenceRules` 的响应中，且 `nextOccurrenceDate` 为 null（「结束」不是「移出列表」）；
- 幂等性：对已经结束的规则再次调用返回 204 且不再改变任何数据。
  </action>
  <verify>
    <automated>pnpm openapi:check &amp;&amp; cd apps/api &amp;&amp; pnpm typecheck &amp;&amp; pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "endSeriesAt" apps/api/src/modules/recurrence/recurrence.service.ts` 为 3（定义 1 处 + 两个调用方各 1 处）。
    - `deleteSeriesFromOccurrence` 的 `this_and_following` 分支内不再直接出现 `tx.recurrenceRule.update` 或 `deleteMany`。
    - `grep -c "currentCalendarDateIn(rule.timezone)" apps/api/src/modules/recurrence/recurrence.service.ts` ≥ 1，且 `endRule` 中的 `addDays(..., 1)` 存在（锚点为明天）。
    - `pnpm openapi:check` 通过；断言块中出现 `endRecurrenceRule`。
    - 上述十条集成断言全部通过；`apps/api/test/recurrence/recurrence-rules.int.test.ts` 中既有的 `/series` 用例零回归。
    - `git status --porcelain apps/api/prisma/migrations` 无新增目录。
  </acceptance_criteria>
  <done>结束逻辑只剩一份实现并被两条路径共用，规则级结束以明天为锚保住今天这一次，权限与跨家庭边界与既有 /series 路径完全一致，已结束的规则仍留在列表里。</done>
  <reversibility rating="costly">结束操作会永久删除锚点及以后的已生成实例行。若日后判定锚点应当取今天而非明天，代码改一行即可，但已被删除的实例行无法凭代码回滚恢复。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → `/households/:householdId/recurrence-rules/**` | 未受信输入；与 `/series` 路径不同，这里的 ruleId 由客户端直接提供，没有一个已解析的实例可用来交叉验证归属 |
| 认证身份 → 规则写操作 | 普通成员与管理员/所有者的权限边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-32 | Information disclosure | 猜测 ruleId 读取其他家庭的规则（列表标题、频率、时区都是家庭内私有信息） | high | mitigate | 每一次规则读取都先 `resolveActorRole` → 404 `HOUSEHOLD_NOT_FOUND`，再断言 `rule.householdId === householdId` → 404 `RECURRENCE_RULE_NOT_FOUND`；两条跨家庭集成断言把守。这是本计划最重要的检查 —— ruleId 不再有实例可交叉核对 |
| T-07-33 | Elevation of privilege | 普通成员结束他人创建的规则 | high | mitigate | `canMutate(role, rule.createdBy, actorId)` 在任何写入之前执行，返回 403；集成用例覆盖成员对他人规则 403、对自己规则 204 |
| T-07-34 | Denial of Service | 畸形 UUID 直达 Prisma 变成 500 并泄漏内部错误 | medium | mitigate | 两个路径参数各自过 `ParseUUIDPipe({ version: '4' })`；禁止交叉类型参数 DTO（其 design:paramtypes 为 `Object`，校验器根本不会运行）；一条 400 集成断言把守 |
| T-07-35 | Information disclosure | 列表响应下发 `createdBy`，让客户端得以复制权限判断并推断家庭成员身份 | medium | mitigate | DTO 不含 `createdBy` / `householdId`；一条集成断言检查响应键集合 |
| T-07-36 | Denial of Service | 逐条规则调用走子推算下一次发生，大家庭上放大计算量 | medium | mitigate | 每次走子受既有 `RECURRENCE_MAX_WALK_STEPS` 上限约束，`from: today` 使发出切片极短；返回的是规则（每家庭数十条量级）而非实例；排序确定性，无用户可控的窗口参数 |
| T-07-37 | Tampering | 结束逻辑复制成两份后各自漂移（CR-05 的教训） | high | mitigate | 抽出唯一的 `endSeriesAt`，两个调用方共用；验收条件用出现次数为 3 把守，且既有 `/series` 用例零回归 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增任何依赖，仅重跑既有 `pnpm openapi:generate`；无安装步骤故无供应链检查点 |
</threat_model>

<verification>
- `pnpm openapi:check` 通过
- `cd apps/api && pnpm typecheck` 无错误
- `pnpm --filter api test:quick` 与 `pnpm --filter api test:integration` 全绿
- `git status --porcelain apps/api/prisma/migrations` 无输出
- `git diff --stat apps/api/src/modules/recurrence/recurrence-scheduler.ts` 为空（本计划不碰调度器）
</verification>

<success_criteria>
- 规则列表与详情可用、按契约排序、类型由关系计数推导且零迁移
- 下一次发生日期在没有任何未来实例行时仍然正确
- 结束此重复以明天为锚、保住今天、复用唯一一份结束事务体
- 跨家庭 404、成员 403、畸形 UUID 400 三条边界各有集成断言
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-12-SUMMARY.md` when done
</output>
</content>
