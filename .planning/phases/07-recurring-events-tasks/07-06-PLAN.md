---
phase: 07-recurring-events-tasks
plan: 06
type: execute
wave: 5
depends_on: [07-03, 07-04, 07-05]
files_modified:
  - apps/client/src/features/recurrence/recurrence-badge.tsx
  - apps/client/src/features/events/event-card.tsx
  - apps/client/src/features/tasks/task-card.tsx
  - apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx
  - apps/client/app/(protected)/households/[id]/tasks/[taskId]/index.tsx
  - apps/client/app/(protected)/households/[id]/today.tsx
  - apps/client/src/features/tasks/__tests__/task-status-test.tsx
autonomous: true
requirements: [RECR-01, RECR-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "重复实例卡片只增加一个 14px inkMuted 的 Repeat 徽标，宿主卡片的 accessibilityLabel 扩展为『事件：{标题}，重复』/『任务：{标题}，重复』；卡片高度、内边距、圆角、边框均不因徽标而改变，也没有为『是否重复』引入第二套渲染路径 (D-02)"
    - "月历格子不新增任何重复标记 —— 重复实例与普通事件共用同一个圆点，calendar-month.tsx 零改动"
    - "详情页在既有信息块序列中插入一个『重复』块（事件在『结束』之后、地点之前；任务在『截止日期』之后），内容为中文摘要，必要时追加月末钳位说明与时区注记 (D-09, D-10)"
    - "status === 'cancelled' 的任务卡片同时用三种方式表达取消：Ban 图标 + 『已取消』文字 + 标题删除线，卡片整体 opacity 0.6，不依赖颜色单独传达 (D-07)"
    - "已取消任务的状态切换圆圈 disabled 且 accessibilityState.disabled 为 true，可访问名称为『这次重复已取消』"
    - "已取消的任务被排除出今日视图的『今日待办』分区，也不计入逾期分区 (D-07)"
    - "已取消的任务不会因为点击状态切换而被复活成 pending —— 状态循环对 cancelled 是空操作"
    - "已取消的事件从日历与今日视图的列表中消失（服务端列表查询已过滤），但通过链接直达详情页时仍可解析并显示『已取消』徽标与说明『这次重复已取消。』(D-07)"
    - "recurrenceRuleId 为 null 的普通条目在卡片、详情页与今日视图上的呈现与本阶段之前逐字节一致"
  artifacts:
    - path: "apps/client/src/features/recurrence/recurrence-badge.tsx"
      provides: "Repeat 内联徽标，供 EventCard / TaskCard / 详情页复用"
      exports: ["RecurrenceBadge"]
    - path: "apps/client/src/features/tasks/__tests__/task-status-test.tsx"
      provides: "cancelled 任务的分区排除、逾期排除、状态循环空操作、卡片三重表达断言"
  key_links:
    - from: "apps/client/src/features/tasks/task-card.tsx"
      to: "apps/client/src/features/recurrence/recurrence-badge.tsx"
      via: "recurrenceRuleId 非 null 时在徽标行渲染 RecurrenceBadge"
      pattern: "RecurrenceBadge"
    - from: "apps/client/app/(protected)/households/[id]/today.tsx"
      to: "packages/api-client/src/generated/models.ts"
      via: "分区循环读取 task.status 并对 cancelled 提前 continue"
      pattern: "cancelled"
  prohibitions:
    - "MUST NOT 让已取消的一次重复变得无法查看：它从列表消失是刻意的，但通过链接直达时必须仍能解析并明确告诉用户它已被取消，不能表现得像它从未存在过"
---

<objective>
让重复这件事在界面上**看得出来**，同时让「已取消的这一次」在任务侧与事件侧都被正确地表达和排除。

完成之后，用户在日历、任务列表和今日视图里可以一眼分辨哪条安排是重复的，在详情页能读到中文规则摘要，并且取消掉的那一次不会再混进今日待办里制造噪音。

Purpose: D-02 明令不得为「是否重复」引入第二套渲染路径 —— 本计划的全部改动必须收敛成一个徽标、一个详情信息块和一处分区守卫；D-07 的 `cancelled` 取值在客户端有三个消费点，漏掉任意一个都会让取消的家务继续出现在待办里。
Output: `RecurrenceBadge` + 两张卡片 + 两个详情页 + 今日视图的取消排除 + 覆盖取消扇出的组件测试。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-CONTEXT.md
@.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md
@.planning/phases/07-recurring-events-tasks/07-PATTERNS.md
@.planning/phases/07-recurring-events-tasks/07-03-SUMMARY.md
@.planning/phases/07-recurring-events-tasks/07-04-SUMMARY.md
</context>

## Artifacts this plan produces

- `apps/client/src/features/recurrence/recurrence-badge.tsx` → `RecurrenceBadge`
- `apps/client/src/features/tasks/__tests__/task-status-test.tsx`
- `EventCard` / `TaskCard` 的徽标行分支；两个详情页的「重复」信息块；`today.tsx` 的取消守卫

<tasks>

<task type="auto">
  <name>Task 1: RecurrenceBadge + 事件卡片与任务卡片的徽标行</name>
  <reversibility rating="reversible">纯展示层新增，卡片几何不变。</reversibility>
  <precondition>`packages/api-client` 的 `TaskResponseDto` / `EventResponseDto` 已带 `recurrenceRuleId`（07-03 交付），`cd apps/client && pnpm typecheck` 可通过。</precondition>
  <files>apps/client/src/features/recurrence/recurrence-badge.tsx, apps/client/src/features/events/event-card.tsx, apps/client/src/features/tasks/task-card.tsx</files>
  <read_first>
    - `apps/client/src/features/tasks/task-card.tsx` 全文 —— 第 4 行图标的精确子路径导入写法、第 11 行与 57-88 行的徽标 View 结构与 `BADGE_PADDING_V = 2`、第 43 行卡片 `accessibilityLabel`、第 50 与 129-135 行 `completed` 状态的 opacity 与删除线先例
    - `apps/client/src/features/events/event-card.tsx` 全文 —— 第 20 行卡片 `accessibilityLabel`、时间行结构
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Recurring Instance Presentation` → `### RecurrenceBadge` 与 `### 已取消的单次实例（D-07）` 两小节
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Color` 的「Accent reserved for（穷举）」第 4 条 —— 徽标图标描边**不使用** coral，用 `inkMuted`
    - `apps/client/src/ui/primitives.tsx` —— `Inline` / `Text` 的可用变体
  </read_first>
  <action>
**(1) 新建 `recurrence-badge.tsx`。** 导出 `RecurrenceBadge`：`Repeat` 图标（精确子路径导入 `lucide-react-native/icons/repeat`，与 `task-card.tsx:4` 的导入风格一致）、尺寸 14px、`strokeWidth={2}`、`color = theme.colors.inkMuted`。无可见文字，`accessibilityLabel="重复"`。图标描边**不用** coral —— 否则每一张重复卡片都会拉出一块强调色，破坏 60/30/10 分配（UI-SPEC 的 accent 穷举清单里明确把徽标排除在外）。

**(2) `task-card.tsx`。** 在顶部徽标行追加 `{task.recurrenceRuleId !== null && <RecurrenceBadge />}`。徽标行超宽时换行，**不得**挤压状态切换按钮。卡片 `accessibilityLabel` 从 `任务：${task.title}` 扩展为 `任务：${task.title}，重复`（仅重复时追加），让屏幕阅读器一次读到。

同时加**已取消分支**（D-07）：`task.status === 'cancelled'` 时在徽标行显示 `Ban` 图标（14px、`inkMuted`）+ `caption` 文字 `已取消`，背景 `theme.colors.surfaceMuted`、圆角 `theme.borderRadii.sm`、垂直内边距沿用既有的 `BADGE_PADDING_V`；标题加删除线，卡片整体 `opacity: 0.6`（与 `completed` 分支一致的写法）；状态切换圆圈 `disabled` 且 `accessibilityState={{ disabled: true }}`，可访问名称 `这次重复已取消`。取消态用**图标 + 文字 + 删除线**三重表达，不依赖颜色（UI-SPEC `## Color` 的「不依赖颜色的强制项」）。

**(3) `event-card.tsx`。** 在时间行右侧追加 `{event.recurrenceRuleId !== null && <RecurrenceBadge />}`（`Inline gap={1}`）。卡片 `accessibilityLabel` 扩展为 `事件：${event.title}，重复`。

**(4) 硬约束。** 卡片的高度、内边距、圆角、边框**均不得**因徽标而改变；`recurrenceRuleId` 有无只影响徽标是否存在，其余 props 与样式完全相同 —— D-02 禁止为「是否重复」引入第二套渲染路径。`apps/client/src/features/events/calendar-month.tsx` **零改动**：月历格子不新增任何重复标记，重复实例与普通事件共用同一个圆点。

样式全部走 `useTheme<Theme>()`；唯一允许的裸值是与既有代码一致的徽标微内边距常量。
  </action>
  <verify>
    <automated>cd apps/client && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/client && pnpm test` 全绿；`cd apps/client && pnpm typecheck` 零错误。
    - `git diff --exit-code -- apps/client/src/features/events/calendar-month.tsx`（月历文件零改动）。
    - `grep -c 'RecurrenceBadge' apps/client/src/features/tasks/task-card.tsx` ≥ 1 且 `grep -c 'RecurrenceBadge' apps/client/src/features/events/event-card.tsx` ≥ 1。
    - `grep -c '已取消' apps/client/src/features/tasks/task-card.tsx` ≥ 1 且 `grep -c 'textDecorationLine' apps/client/src/features/tasks/task-card.tsx` ≥ 1（三重表达里的文字与删除线两项）。
    - `! grep -REn '#[0-9A-Fa-f]{3,8}|rgba?\(' apps/client/src/features/recurrence/recurrence-badge.tsx`（无裸色值）
    - `! grep -n 'colors.coral' apps/client/src/features/recurrence/recurrence-badge.tsx`（徽标不用强调色）
  </acceptance_criteria>
  <done>重复实例的两种卡片各多出一个 Repeat 徽标且卡片几何不变；已取消任务的卡片以图标+文字+删除线三重表达并禁用状态切换；月历文件零改动。</done>
</task>

<task type="auto">
  <name>Task 2: 事件与任务详情页的「重复」信息块</name>
  <reversibility rating="reversible">纯展示层新增信息块。</reversibility>
  <files>apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx, apps/client/app/(protected)/households/[id]/tasks/[taskId]/index.tsx</files>
  <read_first>
    - `apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx` 全文 —— 第 112-124 行 `tealSoft` 信息胶囊、第 126-151 行 `Stack gap={1}` + `Text variant="label" color="inkMuted"` 标签 + `Text variant="body"` 值的信息块结构、外层 `Stack gap={5}`
    - `apps/client/app/(protected)/households/[id]/tasks/[taskId]/index.tsx` 全文 —— 与事件详情页同构，找到「截止日期」块
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Recurring Instance Presentation` → `### 详情页"重复"信息块` 与 `### 已取消的单次实例（D-07）` 的事件分支
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Information Architecture and Screen Inventory` 表中两个详情页的行 —— 插入位置的精确约束
    - `apps/client/src/features/recurrence/recurrence-summary.tsx`（07-05 产出）—— 复用 `formatRecurrenceSummary` 与 `RecurrenceSummary`
  </read_first>
  <action>
**(1) 事件详情页。** 在「结束」信息块之后、「地点」之前插入一个新的 `Stack gap={1}` 块，仅当 `event.recurrenceRuleId !== null` 时渲染：标签 `Text variant="label" color="inkMuted"` 值为 `重复`；值行用 07-05 的 `formatRecurrenceSummary(event.recurrence, 设备时区)` 得到的中文摘要，以 `Text variant="body"` 渲染；`clampNote` 与 `timeZoneNote` 非空时各追加一行 `variant="caption"` + `color="inkMuted"`。摘要与两条注记的措辞全部来自 07-05 的格式化函数，本页不重新拼接文案。

**(2) 已取消事件分支。** `event.cancelledAt !== null` 时在标题区渲染 `已取消` 徽标（`Ban` 图标 + `caption` 文字 + `surfaceMuted` 背景 + `radius.sm`），并在重复信息块下方渲染一行 `caption` 说明 `这次重复已取消。`。**详情页必须仍然可解析** —— 服务端的 `getById` 刻意放行已取消事件（07-03），界面不能表现得像它从未存在过。

**(3) 任务详情页。** 同构，插入位置在「截止日期」之后。任务的取消态由 `task.status === 'cancelled'` 判定，徽标与说明文案与卡片一致。

**(4) 普通条目零变化。** `recurrenceRuleId === null` 时两个详情页的渲染输出与本阶段之前逐字节一致 —— 不新增空块、不新增分隔线、不改变 `Stack gap`。
  </action>
  <verify>
    <automated>cd apps/client && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/client && pnpm typecheck` 零错误；`cd apps/client && pnpm test` 全绿。
    - `grep -c 'formatRecurrenceSummary\|RecurrenceSummary' "apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx"` ≥ 1 且同样的检查在任务详情页 ≥ 1（两页都复用同一个格式化来源，没有第二份文案实现）。
    - `grep -c '这次重复已取消。' "apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx"` ≥ 1。
    - `! grep -REn '#[0-9A-Fa-f]{3,8}' "apps/client/app/(protected)/households/[id]/events/[eventId]/index.tsx"`（无裸色值）
  </acceptance_criteria>
  <done>两个详情页在指定位置渲染「重复」信息块，摘要与两条注记复用同一个格式化函数；已取消的事件与任务在详情页可解析并被明确标注；普通条目渲染零变化。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 今日视图排除已取消任务（D-07 客户端扇出点）</name>
  <reversibility rating="reversible">分区逻辑的局部守卫。</reversibility>
  <files>apps/client/app/(protected)/households/[id]/today.tsx, apps/client/src/features/tasks/__tests__/task-status-test.tsx</files>
  <read_first>
    - `apps/client/app/(protected)/households/[id]/today.tsx` 全文 —— 第 69-94 行的分区循环（`completed` 提前 continue 那一行旁边就是新守卫的位置）、第 157-178 行的状态循环三元表达式
    - `apps/client/src/features/tasks/task-utils.ts` —— `isOverdue` / `isApproachingDeadline` 的判定，确认取消态是否需要在这里也加守卫
    - `.planning/phases/07-recurring-events-tasks/07-RESEARCH.md` `## Common Pitfalls` Pitfall 2 —— 六处扇出点的完整清单；本任务负责其中今日视图的分区与状态循环两处
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `### 已取消的单次实例（D-07）` 的任务分支
    - `apps/client/src/features/households/__tests__/member-removal-test.tsx` —— 客户端测试写法参考（glob 为 `-test.tsx` 后缀）
  </read_first>
  <behavior>
先写 `apps/client/src/features/tasks/__tests__/task-status-test.tsx`，再改 `today.tsx`。断言：

- 一条 `status: 'cancelled'` 且 `dueDate` 为今天的任务，**不出现**在「今日待办」分区
- 一条 `status: 'cancelled'` 且 `dueDate` 已过期的任务，**不计入**逾期分区
- 一条 `status: 'cancelled'` 且 `dueDate` 为 null 的任务，不出现在任何分区
- 对一条 `cancelled` 任务点击状态切换控件 → 不发出任何状态更新调用，任务不会变成 `pending`（状态循环对 cancelled 是空操作）
- 一条 `status: 'pending'` 的普通任务在同一批数据里仍然正常出现在「今日待办」（回归保护：新守卫没有误伤正常任务）
- `cancelled` 任务卡片同时渲染 `已取消` 文字与标题删除线，且状态切换控件 `accessibilityState.disabled` 为 true、可访问名称为 `这次重复已取消`
  </behavior>
  <action>
**(1) 分区循环。** 在 `today.tsx` 的任务分区循环里，紧邻既有的 `completed` 提前 continue，加一条对 `cancelled` 的提前 continue。少了这一条，被取消的家务会一路落进「今日待办」—— 它 `dueDate` 是今天、状态又不是 completed，现有的每一个分支都会收下它。

**(2) 状态循环。** 现有的状态循环是一个「pending → in_progress → completed → pending」的三元链，末尾的 `else` 分支会把任何非 pending/非 in_progress 的状态映射回 `pending`。这意味着点一下取消掉的任务，它就复活成待办了。改成：`cancelled` 时直接返回当前状态（空操作，不发请求），恢复动作只在任务编辑表单里通过 `恢复这一次` 提供（07-05 已交付）。

**(3) 逾期判定。** 确认 `task-utils.ts` 的 `isOverdue` / `isApproachingDeadline` 在分区循环里只对未被 continue 掉的任务调用；若它们在别处被独立调用，同样加取消守卫。取消掉的一次重复既不是待办也不是逾期。

**(4) 不新增分区、不新增筛选入口。** UI-SPEC 明确本阶段今日视图只做排除，不为「已取消」开一个新分区，也不在任务列表筛选区新增「只看重复」选项。
  </action>
  <verify>
    <automated>cd apps/client && pnpm test -- task-status && cd apps/client && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/client && pnpm test -- task-status` 全绿。
    - `cd apps/client && pnpm test` 全量全绿（今日视图与任务列表的既有测试无回归）。
    - `grep -c "cancelled" "apps/client/app/(protected)/households/[id]/today.tsx"` ≥ 2（分区守卫与状态循环两处都改到了）。
    - `grep -c '这次重复已取消' apps/client/src/features/tasks/__tests__/task-status-test.tsx` ≥ 1。
    - `cd apps/client && pnpm typecheck` 零错误。
  </acceptance_criteria>
  <done>已取消的任务不出现在今日待办、不计入逾期、点状态切换不会复活；正常任务在同一批数据里行为不变；今日视图没有新增分区或筛选入口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端响应 → 卡片/详情渲染 | 客户端按 `recurrenceRuleId` / `status` / `cancelledAt` 分支渲染，这些字段完全由服务端权威决定 |
| 深链 URL → 详情页 | 用户可直达任意 id；归属校验在服务端，客户端只负责如实呈现返回结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Elevation of Privilege | 详情页深链 | high | mitigate | 客户端不做任何本地授权推断；跨家庭 id 由服务端返回 404 `HOUSEHOLD_NOT_FOUND` / `EVENT_NOT_FOUND`，界面沿用既有错误面板呈现，不额外泄漏该 id 是否存在 |
| T-07-15 | Information Disclosure | 已取消事件的详情放行 | low | accept | 放行是刻意的产品行为且仍经过服务端的 household 归属校验；界面只多渲染一行取消说明，不暴露任何新字段 |
| T-07-16 | Repudiation | 已取消任务被状态循环复活 | medium | mitigate | 状态循环对 `cancelled` 为空操作且不发请求；恢复只能通过编辑表单里的显式 `恢复这一次` 动作，用户意图明确可追溯；组件测试直接断言点击不产生调用 |
| T-07-SC | Tampering | npm 安装 | high | mitigate | 本计划零新增包（`Ban` / `Repeat` 图标来自已安装的图标库，走精确子路径导入）。若判定必须新增依赖，停止并走阻塞式人工审批 |
</threat_model>

<verification>
- `cd apps/client && pnpm test` 全绿
- `cd apps/client && pnpm typecheck` 零错误
- `git diff --exit-code -- apps/client/src/features/events/calendar-month.tsx`（月历零改动）
- `apps/client/package.json` 与 `pnpm-lock.yaml` 无变动
</verification>

<success_criteria>
- 重复只表现为一个小徽标和详情页的一行说明，没有第二套渲染路径，月历零改动。
- 已取消的一次重复在任务侧被三重表达并排除出今日待办，在事件侧从列表消失但深链仍可解析。
- 普通一次性条目的卡片、详情页与今日视图行为零变化。
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-06-SUMMARY.md` when done
</output>
