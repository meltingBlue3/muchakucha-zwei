---
phase: 07-recurring-events-tasks
plan: 15
type: execute
wave: 12
depends_on: [07-10, 07-14]
files_modified:
  - e2e/events/recurrence-rules.spec.ts
  - scripts/check-required-tests.ps1
  - .planning/phases/07-recurring-events-tasks/07-VALIDATION.md
autonomous: false
requirements: [RECR-01, RECR-02]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Web 端可以走通生成时机旅程：创建一条每日重复任务后，任务列表里只出现今天这一条，而不是一批近期实例 (D-11/D-12/D-17)"
    - "Web 端可以走通筛选旅程：任务列表的折叠筛选面板里切到『仅看周期性』后只剩周期实例，筛选按钮上的计数徽标 +1，再切回『全部』恢复 (D-15)"
    - "Web 端可以走通管理旅程：家庭首页 → 周期规则 → 列表里看到带类型徽标的规则行与『下一次』日期 → 进入详情 → 两步确认结束此重复 → 回到列表该规则仍在、标为已结束 (D-14/D-16/D-20)"
    - "结束之后今天这一次仍在任务列表里 —— 端到端断言与『明天起不再重复，今天和之前的安排都保留』的文案一致 (D-14)"
    - "两个新屏幕具备 axe 无障碍断言与键盘遍历断言；重复筛选 chip 的 radiogroup 语义、类型徽标的文字表达、已结束规则的非颜色信号各有一条断言 (07-UI-SPEC.md 无障碍契约)"
    - "生成相关文案在运行时不出现任何固定天数：端到端断言页面上不出现『N 天』形态的生成说明 (D-19)"
    - "scripts/check-required-tests.ps1 的必需测试清单包含本次新增的全部测试文件，且其禁用模式（.skip / .todo / IMPLEMENTATION_MISSING）对它们生效"
    - "07-VALIDATION.md 在既有 ✅ 行之下追加本次 addendum 的验证行，既有行的内容与状态标记一字不改；frontmatter 的 status / nyquist_compliant / wave_0_complete 反映包含 addendum 在内的真实状态"
    - "真机验收覆盖规则列表与详情在 Android 上的呈现、两步内联确认的触控目标、以及 200% 字体下的换行表现"
  artifacts:
    - path: "e2e/events/recurrence-rules.spec.ts"
      provides: "addendum 的 Web 端到端旅程（生成时机 / 筛选 / 规则管理 / 结束）与两个新屏幕的无障碍断言"
      min_lines: 180
    - path: ".planning/phases/07-recurring-events-tasks/07-VALIDATION.md"
      provides: "addendum 新增测试覆盖的验证映射行"
  key_links:
    - from: "e2e/events/recurrence-rules.spec.ts"
      to: "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx"
      via: "端到端点击结束此重复的两步确认并断言列表与任务列表的后续状态"
      pattern: "结束此重复"
---

<objective>
用 Web 端到端旅程覆盖 addendum 引入的四条用户路径，把新增测试纳入必需测试清单，补齐验证映射，并在真机上验收新的管理界面。

Purpose: addendum 的两个新屏幕位于 `app/` 之下，客户端 Jest 的 testMatch 够不到它们；生成时机、筛选与结束这三条行为又都是跨层的，只有端到端旅程能证明它们串起来是对的。
Output: 一份端到端 spec、更新后的必需测试清单、追加了 addendum 行的验证映射，以及一次真机验收。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md
@.planning/phases/07-recurring-events-tasks/07-VALIDATION.md
@.planning/phases/07-recurring-events-tasks/07-10-SUMMARY.md
@.planning/phases/07-recurring-events-tasks/07-14-SUMMARY.md
</context>

<artifacts_this_plan_produces>
新增文件：`e2e/events/recurrence-rules.spec.ts`。
更新：`scripts/check-required-tests.ps1` 的 `$requiredTests` 数组、`07-VALIDATION.md` 的 Per-Task Verification Map 与 frontmatter。
不新增依赖，不改动任何源代码。
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: addendum 的 Web 端到端旅程与新屏幕无障碍断言</name>
  <files>e2e/events/recurrence-rules.spec.ts</files>
  <read_first>
    - `e2e/events/recurrence.spec.ts` 第 1-60 行（fixture 形状：API_ORIGIN/WEB_ORIGIN/DATABASE_URL 常量、账号与家庭的建立方式、`toIsoDate` 等助手）
    - `e2e/events/accessibility.spec.ts`（axe 与键盘遍历断言的既有写法）
    - `07-UI-SPEC.md` 第 594-644 行（本次全部新文案，端到端断言按它取字符串）
    - `07-UI-SPEC.md` 第 684-693 行（addendum 无障碍要求）
    - 07-10 与 07-14 的 SUMMARY（实际落地的可访问名称与 testID）
  </read_first>
  <precondition>Playwright 的 Web 端到端栈可运行：API 进程、Expo Web 进程与 PostgreSQL 测试库均已按 `playwright.config.ts` 的既有约定启动。</precondition>
  <action>
新建 `e2e/events/recurrence-rules.spec.ts`，账号/家庭/登录的 fixture 从 `e2e/events/recurrence.spec.ts` 复制（沿用本仓库每 spec 各自复制 fixture 的既有约定）。四个 `test`：

1. **生成时机**：通过 API 创建一条 `freq: 'daily'`、`startsOn` 为今天的重复任务，然后在 Web 上打开任务列表；断言页面上属于该标题的条目恰好 1 个，且页面上不出现匹配 `/\d+\s*天/` 的生成说明文案。
2. **仅看周期性筛选**：同一家庭里再建一条普通一次性任务；展开折叠筛选面板，断言重复筛选组容器的 `role` 为 `radiogroup`；点「仅看周期性」后普通任务从列表消失、周期任务仍在，且筛选按钮上的计数徽标数值比点击前大 1；点回「全部」后两条都在。
3. **规则管理与结束**：从家庭首页点入口卡片进入周期规则列表；断言该规则行同时可见类型徽标文字、频率摘要与「下一次」日期；点进详情，断言 `结束此重复` 可见且此时**尚未**发生任何写入（页面上仍显示原有的下一次发生日期）；点它展开确认行，断言确认提示文案与 `07-UI-SPEC.md` 第 637 行一致；点确认动作；断言回到列表后该规则**仍在列表中**且显示已结束文案；再回到任务列表，断言今天那一条任务**仍然存在**（结束的锚点是明天）。
4. **无障碍**：对规则列表屏与规则详情屏各跑一次 axe 断言（无 serious/critical 违规）；键盘 Tab 能从「结束此重复」直接到达其确认与取消动作；断言已结束规则行同时具备已结束文字与 `aria-disabled` 语义的动作（不只靠透明度）。

全部断言按可访问名称/文本定位，不要依赖样式类名或 DOM 层级。
  </action>
  <verify>
    <automated>pnpm test:e2e:web -- e2e/events/recurrence-rules.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:e2e:web -- e2e/events/recurrence-rules.spec.ts` 四个 test 全绿。
    - spec 中不含 `.skip` / `.todo` / `test.fixme`。
    - 「结束之后今天那一条任务仍然存在」这一条断言存在且在锚点被改成今天时必然失败（它是 D-14 锚点语义的守卫）。
    - spec 中存在对 `/\d+\s*天/` 的否定断言。
    - `pnpm test:e2e:web` 全量运行时，`e2e/events/recurrence.spec.ts` 与 `e2e/events/accessibility.spec.ts` 零回归。
  </acceptance_criteria>
  <done>addendum 的四条用户路径在 Web 上端到端可复现，两个新屏幕有 axe 与键盘断言，结束锚点的语义有一条会失败的守卫。</done>
</task>

<task type="auto">
  <name>Task 2: 必需测试清单与验证映射</name>
  <files>scripts/check-required-tests.ps1, .planning/phases/07-recurring-events-tasks/07-VALIDATION.md</files>
  <read_first>
    - `scripts/check-required-tests.ps1` 第 1-40 行（`$requiredTests` 数组与 `$forbiddenPattern`）
    - `.planning/phases/07-recurring-events-tasks/07-VALIDATION.md` 全文（表头列、状态图例与既有 ✅ 行）
    - 07-09 … 07-14 的 SUMMARY（各自实际落地的测试文件路径与命令）
  </read_first>
  <action>
**a) 必需测试清单。** 在 `scripts/check-required-tests.ps1` 的 `$requiredTests` 数组尾部追加 addendum 新增的全部测试文件（按各 SUMMARY 中记录的实际路径）：`apps/api/test/recurrence/lookahead.int.test.ts`、`apps/api/test/recurrence/recurring-filter.int.test.ts`、`apps/api/test/recurrence/recurrence-rules-api.int.test.ts`、`apps/client/src/features/recurrence/__tests__/recurring-filter-test.tsx`、`apps/client/src/features/recurrence/__tests__/recurrence-rule-row-test.tsx`、`e2e/events/recurrence-rules.spec.ts`。数组的既有条目一行不动，`$forbiddenPattern` 与自检逻辑也不动。

**b) 验证映射。** 在 `07-VALIDATION.md` 的 Per-Task Verification Map 表格**末尾追加**新行，既有 25 行的内容与 ✅ 标记**一字不改**。在追加的第一行之前插入一行小标题式的注释行（例如一行 `<!-- Addendum (D-11 … D-20), planned 2026-08-13 -->`），让两批行在源文件里可分辨。新增行按下表覆盖，Status 列先填 ⬜，由本计划执行时的实测结果改成 ✅：

| Requirement | Behavior | Test Type | Command | File | Owner |
|---|---|---|---|---|---|
| RECR-01 | `currentCalendarDateIn` 跨 UTC+14 / UTC−11 / DST 的日历日期 | unit | `pnpm --filter api test:quick` | `recurrence-date.test.ts` | 07-09 T1 |
| RECR-01 | 每日规则在规则时区当天生成、每周规则 6 天边界含端点 | integration | `pnpm --filter api test:integration` | `lookahead.int.test.ts` | 07-09 T1 |
| RECR-01 | 水位线只增不减；已生成的未来实例一行不删 (D-13) | integration | 同上 | `lookahead.int.test.ts` | 07-09 T2 |
| RECR-01 | `endsOn` 已过的规则退出扫描且不污染家庭 `_min` 水位线 | integration | 同上 | `lookahead.int.test.ts` | 07-09 T2 |
| RECR-01 | 创建即一次标准生成检查：daily 只有今天、weekly 窗口外只有种子行 (D-12/D-17) | integration | 同上 | `lookahead.int.test.ts` | 07-09 T3 |
| RECR-01 | `recurring=true/false/缺省` 三态在任务与事件两侧 + 非成员 404 | integration | 同上 | `recurring-filter.int.test.ts` | 07-11 T1/T2 |
| RECR-01 | 客户端重复筛选判据与生成窗口三态分类器（含抑制条件与零天数文案） | client unit | `cd apps/client && pnpm test` | `__tests__/recurring-filter-test.tsx` | 07-10 T1 |
| RECR-01 | `nextOccurrenceFor` 在 count 用尽 / endsOn 已过 / yearly 300 天后的取值 | unit | `pnpm --filter api test:quick` | `recurrence-date.test.ts` | 07-12 T1 |
| RECR-01 | 规则列表：窗口外的周规则仍有 nextOccurrenceDate；kind 推导；排序；不下发 createdBy | integration | `pnpm --filter api test:integration` | `recurrence-rules-api.int.test.ts` | 07-12 T1 |
| RECR-02 | 结束此重复：锚点为明天、今天保留、count 清空、跨家庭 404 / 成员 403 / 幂等 | integration | 同上 | `recurrence-rules-api.int.test.ts` | 07-12 T2 |
| RECR-02 | 规则级编辑：原子性、模板取自 `template_*`、种子行不变量、继承与权限 | integration | 同上 | `recurrence-rules-api.int.test.ts` | 07-13 T2 |
| RECR-01 | 规则行格式化：null 归一化、永不结束后缀、已结束行、可访问名称、锚点推算 | client unit | `cd apps/client && pnpm test` | `__tests__/recurrence-rule-row-test.tsx` | 07-14 T1 |
| RECR-01/02 | E2E web：生成时机 / 仅看周期性筛选 / 规则管理与结束 / 两个新屏幕 axe | e2e | `pnpm test:e2e:web` | `e2e/events/recurrence-rules.spec.ts` | 07-15 T1 |
| —（回归） | 新增端点与查询参数后手写 OpenAPI 客户端与提交树零漂移 | contract | `pnpm openapi:check` | n/a | 07-11 / 07-12 / 07-13 |
| —（回归） | 必需测试清单覆盖 addendum 的 6 个新测试文件并拒绝禁用标记 | script | `pwsh -File scripts/check-required-tests.ps1` | `scripts/check-required-tests.ps1` | 07-15 T2 |

同时在 Wave 0 Requirements 一节追加 addendum 的三个新测试文件条目（`lookahead.int.test.ts`、`recurring-filter.int.test.ts`、`recurrence-rules-api.int.test.ts` 与两个客户端 `__tests__` 文件），并在 Manual-Only Verifications 一节说明 addendum 的真机验收由本计划 Task 3 的检查点承担、不是 manual-only 测试。frontmatter 的 `status` 在全部命令实测通过后保持 `validated`，`nyquist_compliant` 与 `wave_0_complete` 按实测结果填写。
  </action>
  <verify>
    <automated>pwsh -File scripts/check-required-tests.ps1 &amp;&amp; pwsh -File scripts/check-required-tests.ps1 -SelfTest</automated>
  </verify>
  <acceptance_criteria>
    - `pwsh -File scripts/check-required-tests.ps1` 退出码为 0，且 `$requiredTests` 数组元素数比本任务开始前恰好多 6。
    - `pwsh -File scripts/check-required-tests.ps1 -SelfTest` 通过。
    - `git diff .planning/phases/07-recurring-events-tasks/07-VALIDATION.md` 中，本计划之前就存在的 25 行验证行没有任何一行被修改或删除（diff 里对这些行只应有上下文，不应有 `-` 开头的行）。
    - `07-VALIDATION.md` 中新增验证行数 ≥ 15，且每一行的 Status 列均为 ✅（由实测填写，不得留 ⬜）。
    - `07-VALIDATION.md` 中所有 addendum 行引用的命令都在本计划执行时真实跑过一次并记录结果。
  </acceptance_criteria>
  <done>addendum 的 6 个新测试文件进入必需测试清单，验证映射在不触碰既有行的前提下补齐 15 行以上并全部标为实测通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Android 真机验收 —— 周期筛选、规则管理与两条两步确认路径</name>
  <what-built>
    addendum 的三块用户可见改动已在 Web 上端到端验证通过：
    1. 任务与日历两块屏幕上的「仅看周期性」筛选，以及重写后的生成窗口两个状态（生成落后的整块面板 / 更远日期的一行注记）；
    2. 家庭首页第 7 张「周期规则」入口卡片与周期规则列表（任务与事件规则合并、带类型徽标、显示下一次发生日期、已结束规则保留在列表中）；
    3. 规则详情页的规则级编辑（两步内联确认）与「结束此重复」（两步内联确认，锚点为明天）。
    服务端侧：按频率提前量 + 按规则时区的生成窗口、水位线只增不减、1 小时 tick、三个新的规则级端点。
  </what-built>
  <how-to-verify>
    在 Android 真机（或与既有 Phase 2-4 验收相同的模拟器配置）上：
    1. 新建一条每日重复任务 → 回到任务列表，确认只出现今天这一条，而不是一批近期实例。
    2. 展开任务筛选面板，滚到最下面找到「重复」这一组 → 确认两个选项是单选、触控目标够大、选中后筛选按钮上的计数徽标 +1；确认面板在系统字体放大到 200% 时仍可纵向滚动、计数徽标没有被挤出、chip 没有横向溢出（这是 UI-SPEC 标为 backstop 的一项）。
    3. 家庭首页 → 「周期规则」卡片 → 确认列表里任务规则与事件规则合并展示、每行的类型徽标是图标 + 文字（不是只靠颜色）、能看到频率摘要与「下一次」日期。
    4. 用一条模板标题很长、又是每年 + 截止日期 + 长时区名的规则，确认标题最多两行、超出省略，且没有把「下一次」那一行挤掉（这是 UI-SPEC 标为 backstop 的一项）。
    5. 进入某条规则的详情 → 改动频率 → 点「保存更改」→ 确认**不会立刻写入**，而是就地展开一行确认；点确认后返回列表并看到新状态。
    6. 在另一条规则的详情最下方点「结束此重复」→ 确认展开的确认文案里说明了今天和之前的安排都保留 → 确认后回到列表，该规则仍在列表里并标为已结束；回到任务列表确认今天那一次仍然在。
    7. 打开系统的「减少动态效果」，确认两个新屏幕没有位移动画；打开高对比度/forced-colors，确认类型徽标与已结束状态仍可分辨。
  </how-to-verify>
  <resume-signal>回复 "approved"，或逐条描述哪一步与预期不符（附截图与机型/系统版本）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 端到端测试装置 → 测试数据库 | 测试专用凭据与专用数据库，绝不指向任何非测试环境 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-49 | Tampering | 新增 E2E spec 误连到非测试数据库并破坏真实数据 | medium | mitigate | 沿用 `e2e/events/recurrence.spec.ts` 既有的 `DATABASE_URL` 常量与测试库默认值，不引入新的连接字符串来源 |
| T-07-50 | Repudiation | 验证映射把未实测的行标为通过，掩盖覆盖缺口 | medium | mitigate | 每一行的命令在本计划执行时真实运行一次并记录结果；验收条件禁止留 ⬜ |
| T-07-51 | Denial of Service | 新增必需测试文件被后续 `.skip` 静默禁用 | low | mitigate | `check-required-tests.ps1` 的 `$forbiddenPattern` 对新增条目同样生效；`-SelfTest` 验证守卫本身有效 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增任何依赖，Playwright 与 PowerShell 脚本均已在用；无安装步骤故无供应链检查点 |
</threat_model>

<verification>
- `pnpm test:e2e:web` 全绿（含新 spec 与既有两个 recurrence/accessibility spec）
- `pwsh -File scripts/check-required-tests.ps1` 与 `-SelfTest` 均通过
- `pnpm --filter api test:quick`、`pnpm --filter api test:integration`、`cd apps/client && pnpm test`、`pnpm openapi:check` 全绿（作为 addendum 的相位闸门整体复跑一次）
- `07-VALIDATION.md` 既有 25 行验证行的 diff 中无删除或修改
</verification>

<success_criteria>
- addendum 的四条用户路径在 Web 上端到端可复现且可回归
- 两个新屏幕有 axe 与键盘断言，补齐 `app/**` 屏幕不可被 Jest 触及的缺口
- 6 个新测试文件进入必需测试清单并受禁用标记守卫
- 验证映射补齐且不触碰既有行
- Android 真机验收覆盖两项 UI-SPEC backstop 与两条两步确认路径
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-15-SUMMARY.md` when done
</output>
</content>
