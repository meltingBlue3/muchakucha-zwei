---
phase: 07-recurring-events-tasks
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/api/src/modules/events/dto/create-event.dto.ts
  - apps/api/src/modules/events/dto/update-event.dto.ts
  - apps/api/src/modules/events/events.service.ts
  - apps/api/src/modules/events/events.module.ts
  - apps/api/src/modules/tasks/tasks.service.ts
  - apps/api/src/modules/tasks/dto/create-task.dto.ts
  - apps/api/src/modules/tasks/tasks.controller.ts
  - apps/api/src/openapi/generate-openapi.ts
  - packages/api-client/src/generated/models.ts
  - packages/api-client/src/generated/client.ts
  - apps/api/test/recurrence/recurrence-rules.int.test.ts
autonomous: true
requirements: [RECR-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "创建带 recurrence 的事件后，生成的每个发生项保持模板事件的相对时长不变（endTime − startTime 恒定），并按规则时区还原本地墙钟时间"
    - "事件列表查询过滤掉 cancelledAt 非空的行，但按 id 深链的 getById 仍然返回它 —— 已取消的一次重复仍可解析 (D-07)"
    - "事件与任务的列表响应都带家庭级生成水位线 materializedThrough（家庭无任何有效重复规则时为 null）"
    - "recurrence 字段在 CreateEventDto / UpdateEventDto / CreateTaskDto / UpdateTaskDto 上一律可选且新增，不改变任何既有请求体的必填项 (AGENTS.md C-02，已发布客户端必须能与升级后的服务端共存)"
    - "packages/api-client 里的 recurrence 类型与客户端方法是手工编辑 generate-openapi.ts 的两个模板字符串得到的，不是装饰器自动产出的；pnpm openapi:check 对提交树零漂移"
    - "非家庭成员对带 recurrence 的事件创建请求得到 404 HOUSEHOLD_NOT_FOUND（不是 403）(SAFE-01)"
  artifacts:
    - path: "apps/api/src/modules/events/events.service.ts"
      provides: "事件的 recurrence 创建/更新分支、cancelledAt 读路径过滤、列表水位线"
      contains: "cancelledAt"
    - path: "apps/api/src/openapi/generate-openapi.ts"
      provides: "modelsSource 与 clientSource 两个模板字符串里的 recurrence 类型与字段"
      contains: "RecurrenceDto"
    - path: "packages/api-client/src/generated/models.ts"
      provides: "客户端可用的 RecurrenceDto / RecurrenceResponseDto 类型"
      contains: "RecurrenceResponseDto"
  key_links:
    - from: "apps/api/src/modules/events/events.service.ts"
      to: "apps/api/src/modules/recurrence/recurrence-materializer.service.ts"
      via: "create()/update() 在规则事务提交后调用 materializeRule"
      pattern: "materializeRule"
    - from: "packages/api-client/src/generated/client.ts"
      to: "apps/api/src/openapi/generate-openapi.ts"
      via: "clientSource 模板字符串是这些方法的唯一来源；pnpm openapi:generate 重新写出"
      pattern: "recurrence"
  prohibitions:
    - "MUST NOT 让已取消的一次重复变得无法查看：它从列表消失是刻意的，但通过链接直达时必须仍能解析并明确告诉用户它已被取消，不能返回 404 假装它从未存在过"
---

<objective>
把 07-01 在任务侧证明的重复链路横向展开到事件侧，并把整条 Phase 7 的 API 形状真正交付给客户端 —— 手工编辑 `generate-openapi.ts` 里两个手写模板字符串，重新生成并提交 `packages/api-client`。

完成之后，用户（通过 API）可以创建一个重复事件并在日历范围查询里看到全部发生项；客户端 TypeScript 也第一次能编译出带 `recurrence` 的请求。

Purpose: 事件比任务多了「相对时长不变」与「取消态读路径过滤」两个约束；而 OpenAPI 客户端在本仓库不是从装饰器生成的，必须显式作为一项工作来做，否则后续所有客户端计划都无法编译。
Output: 事件重复支持 + 取消态读路径 + 列表水位线 + 零漂移的生成客户端。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-CONTEXT.md
@.planning/phases/07-recurring-events-tasks/07-RESEARCH.md
@.planning/phases/07-recurring-events-tasks/07-PATTERNS.md
@.planning/phases/07-recurring-events-tasks/07-01-SUMMARY.md
</context>

## Artifacts this plan produces

- `CreateEventDto.recurrence` / `UpdateEventDto.recurrence`
- `EventResponseDto.recurrenceRuleId` / `.occurrenceDate` / `.recurrence` / `.cancelledAt`
- `EventListResponseDto.materializedThrough`、`TaskListResponseDto.materializedThrough`
- `packages/api-client` 新增导出类型：`RecurrenceDto`、`RecurrenceResponseDto`

<tasks>

<task type="auto">
  <name>Task 1: 事件侧重复支持 + 取消态读路径 + 列表水位线</name>
  <reversibility rating="costly">`materializedThrough` 与 `cancelledAt` 进入对外响应形状后，已发布的移动客户端会依赖它；移除需要一次协调的 API 版本变更（AGENTS.md C-02 的兼容窗口）。</reversibility>
  <precondition>07-01 的迁移已应用（`recurrence_rules` 表与 `events.cancelled_at` / `events.recurrence_rule_id` / `events.occurrence_date` 列存在），可用 `pnpm --filter api exec prisma migrate deploy` 确认。</precondition>
  <files>apps/api/src/modules/events/dto/create-event.dto.ts, apps/api/src/modules/events/dto/update-event.dto.ts, apps/api/src/modules/events/events.service.ts, apps/api/src/modules/events/events.module.ts, apps/api/src/modules/tasks/tasks.service.ts, apps/api/src/modules/tasks/dto/create-task.dto.ts, apps/api/src/modules/tasks/tasks.controller.ts</files>
  <read_first>
    - `apps/api/src/modules/events/events.service.ts` 全文 —— `resolveActorRole`/`canMutate`(40-57)、`create()`、`list()` 的 where 构造(121-144)、`getById`、`update`、`delete`，以及 130-141 行那段针对 UTC 时间戳做天数加减的既有代码（本任务不扩展它，重复推算一律走 `recurrence-date.ts`）
    - `apps/api/src/modules/events/dto/create-event.dto.ts` 与 `update-event.dto.ts` 全文 —— 注意事件 DTO 的描述是英文，任务/标签/笔记是中文；新增的 recurrence 字段沿用较新的中文惯例
    - `apps/api/src/modules/tasks/tasks.service.ts` 里 07-01 新增的 recurrence 创建分支 —— 事件侧要复用同样的结构（事务内建规则 + 模板行，提交后再生成）
    - `apps/api/src/modules/recurrence/dto/recurrence.dto.ts` 全文（07-01 产出）
    - `apps/api/src/modules/tasks/tasks.controller.ts` 全文 —— 列表响应装配位置，`materializedThrough` 需要挂到 `TaskListResponseDto`
    - `apps/api/src/modules/events/events.module.ts` 全文
    - `.planning/phases/07-recurring-events-tasks/07-RESEARCH.md` `## Common Pitfalls` Pitfall 3（Event 没有取消列且列表 where 不看它）
    - `.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md` `## Recurring Instance Presentation` 的「日历与生成窗口（D-03）」小节 —— 水位线为何必须进响应
  </read_first>
  <action>
**(1) `create-event.dto.ts` / `update-event.dto.ts`** —— 各加一个可选嵌套字段，与 07-01 在 `create-task.dto.ts` 上的写法逐字一致：`@ApiPropertyOptional({ description: '重复规则；省略即普通一次性事件', type: () => RecurrenceDto })` + `@IsOptional()` + `@ValidateNested()` + `@Type(() => RecurrenceDto)`。两个装饰器缺一不可 —— 全局管道开着 `whitelist` / `forbidNonWhitelisted` / `transform`，少了 `@Type` 嵌套对象会被静默剥掉并照样返回 201。

`EventResponseDto` 增加 `recurrenceRuleId: string | null`、`occurrenceDate: string | null`（输出 `YYYY-MM-DD`）、`cancelledAt: string | null`（输出 ISO 瞬时值）、`recurrence: RecurrenceResponseDto | null`。`EventListResponseDto` 增加 `materializedThrough: string | null`。

**(2) `events.service.ts` 的 `create()`** —— 保持既有 `resolveActorRole` + `HOUSEHOLD_NOT_FOUND` 前导不变（非成员一律 404，不泄漏家庭是否存在）。`input.recurrence === undefined` 时行为逐字节不变。有 recurrence 时：在一个事务里建 `RecurrenceRule` 与模板 `Event`（模板行 `occurrenceDate` = 规则 `startsOn`），事务**提交之后**再 `await materializer.materializeRule(ruleId)`。D-06 互斥的错误信封与 07-01 任务侧一致，只把 `details[].field` 换成 `recurrence.endsOn`。

发生项的实际写入由 07-01 已交付的 `RecurrenceMaterializerService` 事件分支负责（它按模板行归属自行分派到 `events` 表，并保证 `endTime − startTime` 与模板恒定）。**本任务不修改 `recurrence-materializer.service.ts`** —— 事件侧只做服务/DTO/模块接线，并由 Task 3 的集成断言证明事件分支正确。若断言暴露生成器缺陷，把修复记入 SUMMARY 的 deviations 并同步告知 07-02（它在同一波次持有该文件）。`recurrence.module.ts` 已 export 该 service，`events.module.ts` 的 `imports` 加入 `RecurrenceModule`。

**(3) `events.service.ts` 的 `list()`** —— 在 where 子句里加 `cancelledAt: null`。仅加列什么也不做：`list()` 目前只按 `householdId` 与 `startTime` 区间过滤，已取消的发生项会继续出现在日历上并计入 `EventListResponseDto.total`。`getById` **保持返回**已取消事件（深链必须仍能解析，客户端据 `cancelledAt` 渲染「这次重复已取消。」）—— 这一条不对称是刻意的，两侧都要有集成断言。

**(4) 列表水位线** —— `list()` 与 `TasksService.list()` 各自查一次该家庭下 `materializedThrough` 的最小值（`prisma.recurrenceRule.aggregate({ _min: { materializedThrough: true }, where: { householdId } })`），格式化成 `YYYY-MM-DD` 放进响应；家庭下没有任何规则时为 `null`。取**最小值**而不是最大值：界面要回答的是「从哪一天起可能还没补齐」，任何一条规则没补到就算没补到。相应更新 `tasks.controller.ts` / 事件控制器的响应装配。
  </action>
  <verify>
    <automated>pnpm --filter api typecheck && pnpm --filter api test:integration -- events</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter api typecheck` 零错误。
    - `pnpm --filter api test:integration` 全量全绿（既有 events 套件无回归）。
    - `grep -c 'cancelledAt: null' apps/api/src/modules/events/events.service.ts` ≥ 1（列表 where 真的过滤了取消态）。
    - `grep -c 'ValidateNested' apps/api/src/modules/events/dto/create-event.dto.ts` ≥ 1 且 `grep -c 'ValidateNested' apps/api/src/modules/events/dto/update-event.dto.ts` ≥ 1。
    - `grep -c 'materializedThrough' apps/api/src/modules/events/events.service.ts` ≥ 1 且 `grep -c 'materializedThrough' apps/api/src/modules/tasks/tasks.service.ts` ≥ 1。
    - `! grep -REn 'setUTCDate|setUTCMonth' apps/api/src/modules/recurrence/`（重复推算没有退回既有的 UTC 时间戳日期算术）。
  </acceptance_criteria>
  <done>带 `recurrence` 的事件创建返回 201 且规则与发生项都已落库；生成的发生项 `endTime − startTime` 与模板一致；已取消事件从 `list()` 消失但 `getById` 仍返回；两个列表响应都带家庭级 `materializedThrough`。</done>
</task>

<task type="auto">
  <name>Task 2: 手工编辑 OpenAPI 模板字符串并重新生成 api-client</name>
  <reversibility rating="costly">`packages/api-client` 是被追踪且被漂移门禁保护的产物；类型形状一旦发布，客户端与已上架移动端都依赖它。</reversibility>
  <precondition>Task 1 已完成且 `pnpm --filter api build`（`tsc -p tsconfig.build.json`）可通过 —— `openapi:generate` 先编译 API 再运行 `dist/openapi/generate-openapi.js`。</precondition>
  <files>apps/api/src/openapi/generate-openapi.ts, packages/api-client/src/generated/models.ts, packages/api-client/src/generated/client.ts</files>
  <read_first>
    - `apps/api/src/openapi/generate-openapi.ts` 第 1-40 行（`modelsSource` 模板字符串的开头与写法）、第 240-300 行（`CreateTaskDto` / `TaskResponseDto` / `TaskListResponseDto` 的模型块）、第 330-360 行（`clientSource` 模板字符串的开头）、第 755-805 行（`createTask` 客户端方法块，注意模板字符串内部的反引号必须转义为 `` \` ``）、第 1100-1200 行（`document.paths[...]?.operationId` 断言守卫的写法）
    - `scripts/check-openapi-drift.mjs` 全文 —— 它先跑 `pnpm --filter api openapi:generate`，再对 `packages/api-client` 做 `git diff --exit-code HEAD`，并额外拒绝该目录下的未跟踪文件
    - `apps/api/src/modules/recurrence/dto/recurrence.dto.ts`（07-01 产出）与本计划 Task 1 改过的事件 DTO —— 模板字符串里的字段必须与它们逐字对应
    - `tsconfig.base.json` 第 6 行 `exactOptionalPropertyTypes: true` —— 客户端方法里可选参数必须用 `...(x === undefined ? {} : { x })` 条件展开惯例
  </read_first>
  <action>
本仓库的 TypeScript 客户端**不是**从装饰器自动生成的。`packages/api-client/src/generated/models.ts` 与 `client.ts` 是 `generate-openapi.ts` 内部两个巨大的手写模板字符串（`modelsSource` 与 `clientSource`）写出来的。只加 `@ApiProperty` 装饰器不会让客户端多出任何字段 —— 那只影响 `openapi.json` 和文件末尾那组 `operationId` 断言守卫。因此本任务的实质工作就是**编辑这两个模板字符串**。

**(1) 在 `modelsSource` 里新增/修改接口**，紧邻既有的 `CreateTaskDto` 块：
- `RecurrenceDto`：`freq: string; interval?: number; byWeekday?: number[]; startsOn: string; endsOn?: string; count?: number; timezone: string;`（若 07-01 选中 option-a，追加 `startTimeLocal?: string; durationMinutes?: number;`）
- `RecurrenceResponseDto`：`id: string; freq: string; interval: number; byWeekday: number[]; startsOn: string; endsOn?: string | null; count?: number | null; timezone: string; materializedThrough?: string | null;`（同上追加）
- `CreateTaskDto` / `UpdateTaskDto` / `CreateEventDto` / `UpdateEventDto` 各加 `recurrence?: RecurrenceDto;`
- `TaskResponseDto` / `EventResponseDto` 各加 `recurrenceRuleId?: string | null; occurrenceDate?: string | null; recurrence?: RecurrenceResponseDto | null;`，`EventResponseDto` 另加 `cancelledAt?: string | null;`
- `TaskListResponseDto` / `EventListResponseDto` 各加 `materializedThrough?: string | null;`

**(2) `clientSource` 无需新增方法**（本计划没有新增路由；`/series` 子资源由 07-04 交付），但要确认 `createTask` / `updateTask` / `createEvent` / `updateEvent` 的 `body` 形参类型已经指向更新后的 DTO 接口 —— 它们本来就是按名字引用的，所以模型块改完即可。若这些方法块里有逐字段展开，同步补上 `recurrence`。

**(3) 运行 `pnpm openapi:generate`，把 `packages/api-client/src/generated/models.ts` 与 `client.ts` 的产出一起提交。** 然后 `pnpm openapi:check` 必须打印 PASS —— 该脚本会重新生成并对 `packages/api-client` 做 `git diff --exit-code HEAD`，同时拒绝该目录下任何未跟踪文件。

**(4) 客户端编译验证**：`pnpm --filter client typecheck` 必须零错误。这一步是本任务真正的验收信号 —— 如果 `recurrence` 没有出现在生成的 `CreateEventDto` 上，后续所有客户端计划都会在这里失败。
  </action>
  <verify>
    <automated>pnpm openapi:generate && pnpm openapi:check && pnpm --filter client typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm openapi:check` 输出以 `PASS:` 开头（生成结果与提交树一致，无未跟踪文件）。
    - `grep -c 'RecurrenceResponseDto' packages/api-client/src/generated/models.ts` ≥ 1 且 `grep -c 'recurrence?: RecurrenceDto' packages/api-client/src/generated/models.ts` ≥ 4（events/tasks 的 create+update 四处）。
    - `grep -c 'materializedThrough' packages/api-client/src/generated/models.ts` ≥ 2。
    - `pnpm --filter client typecheck` 零错误。
    - `git status --porcelain packages/api-client` 在提交后为空。
  </acceptance_criteria>
  <done>`packages/api-client` 里存在 `RecurrenceDto` / `RecurrenceResponseDto` 类型，四个 create/update DTO 都带可选 `recurrence`，两个列表响应都带 `materializedThrough`，`pnpm openapi:check` PASS 且客户端类型检查通过。</done>
</task>

<task type="auto">
  <name>Task 3: 事件重复与取消态的集成断言</name>
  <reversibility rating="reversible">纯测试新增。</reversibility>
  <files>apps/api/test/recurrence/recurrence-rules.int.test.ts</files>
  <read_first>
    - `apps/api/test/recurrence/recurrence-rules.int.test.ts` 全文（07-01 产出）—— 复用其中已有的 `withDatabase` / `insertActor` / `createHousehold` / `addMemberViaDb` 夹具，新增一个事件版的请求辅助函数
    - `apps/api/test/tasks/tasks.int.test.ts` 第 81-99 行 —— `inject` 请求辅助函数的写法与 `exactOptionalPropertyTypes` 下的条件展开
    - `.planning/phases/07-recurring-events-tasks/07-VALIDATION.md` `## Per-Task Verification Map` —— 本任务负责其中标注 `recurrence-rules.int.test.ts` 的 RECR-01 行
  </read_first>
  <action>
在 `recurrence-rules.int.test.ts` 中追加一个 `describe('recurring events')`，断言：

- `POST .../events` 带 `recurrence: { freq: 'weekly', byWeekday: [2,4], startsOn, count: 6, timezone: 'Asia/Shanghai' }` 返回 201，响应 `recurrenceRuleId` 非 null，`recurrence.freq === 'weekly'`
- `GET .../events?startDate&endDate` 覆盖窗口时返回 6 个发生项，且每个发生项的 `endTime − startTime` 毫秒差与模板事件相同（相对时长不变）
- 每个发生项的 `occurrenceDate` 互不相同，且落在 `byWeekday` 指定的星期几上
- 用原始 SQL 给其中一个发生项写入 `cancelled_at`，随后 `GET .../events` 的 `total` 少 1 且该 id 不在列表里；`GET .../events/:id` 对同一 id 仍返回 200 且 `cancelledAt` 非 null
- `GET .../events` 的响应带 `materializedThrough`，且该家庭在没有任何规则时该字段为 null
- 同时提供 `endsOn` 与 `count` → 400 且 `error.code === 'VALIDATION_FAILED'`，`error.details[0].field === 'recurrence.endsOn'`
- `timezone: 'Not/AZone'` → 400 `VALIDATION_FAILED`（不是 500）
- `count: 5000` → 400（超出上限，T-07-01 在 DTO 边界被拦下）
- 非家庭成员对带 recurrence 的事件创建请求 → 404 且 `error.code === 'HOUSEHOLD_NOT_FOUND'`
- 已加入但角色为 MEMBER 的用户对**他人创建**的重复事件调用更新 → 403 `FORBIDDEN`（沿用既有 `canMutate` 语义，重复不改变权限模型）
  </action>
  <verify>
    <automated>pnpm --filter api test:integration -- recurrence</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter api test:integration -- recurrence` 全绿，且 `recurrence-rules.int.test.ts` 中同时出现 `HOUSEHOLD_NOT_FOUND`、`VALIDATION_FAILED`、`cancelled_at` 三个字面量（权限、校验、取消态三类断言真实存在）。
    - `pnpm --filter api test:integration` 全量全绿。
    - `grep -c "'Not/AZone'\|Not/AZone" apps/api/test/recurrence/recurrence-rules.int.test.ts` ≥ 1（非法时区被断言为 400 而不是 500）。
  </acceptance_criteria>
  <done>事件重复创建、相对时长恒定、取消态列表过滤与深链可解析、水位线、四类输入校验拒绝、两条鉴权边界全部有通过的自动化断言。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST/PUT /api/v1/households/:householdId/events` | 未受信的 `recurrence` 对象跨入事件写路径 |
| API → `packages/api-client`（被追踪产物） | 手写模板字符串是生成客户端的唯一来源；污染它等于污染所有客户端 |
| 已取消发生项 → 读路径 | 列表必须过滤、深链必须放行，两条路径的判定不能互相污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Denial of Service | `CreateEventDto.recurrence` | high | mitigate | 复用 07-01 的 `RecurrenceDto` 上限（`count` ≤ 1000、`interval` ≤ 52）与生成器硬截断；集成测试断言 `count: 5000` 被 400 拒绝 |
| T-07-03 | Elevation of Privilege | `EventsService.create` / `update` 的 recurrence 分支 | high | mitigate | 沿用既有 `resolveActorRole` + `canMutate`；非成员 404 `HOUSEHOLD_NOT_FOUND`、越权成员 403 `FORBIDDEN`，两条边界都有集成断言 |
| T-07-05 | Denial of Service | `timezone` → `Intl.DateTimeFormat` | medium | mitigate | DTO 层 `IsIanaTimeZone` 拦截；集成测试直接断言非法时区得到 400 而非 500 |
| T-07-08 | Information Disclosure | `getById` 对已取消事件放行 | low | accept | 放行是刻意的产品行为（D-07 + UI-SPEC 深链可解析），且仍然经过既有的 household 归属校验；不额外收紧 |
| T-07-09 | Tampering | `packages/api-client` 手写模板 | medium | mitigate | `pnpm openapi:check` 重新生成并对提交树做 `git diff --exit-code`，同时拒绝未跟踪文件；本计划把该命令作为验收门 |
| T-07-SC | Tampering | npm 安装 | high | mitigate | 本计划零新增包。验收门要求 `git diff --stat -- apps/api/package.json apps/client/package.json packages/api-client/package.json pnpm-lock.yaml` 为空；若判定必须新增依赖，停止并走阻塞式人工审批 |
</threat_model>

<verification>
- `pnpm --filter api typecheck` 零错误
- `pnpm --filter client typecheck` 零错误
- `pnpm --filter api test:integration` 全量全绿
- `pnpm openapi:check` 输出 PASS
- 依赖清单未变动

<human-check>
无 —— 本计划全部行为均有自动化覆盖。
</human-check>
</verification>

<success_criteria>
- 事件与任务两侧的重复创建行为对称，且事件的相对时长恒定。
- 已取消事件从列表消失、通过链接仍可解析并可被标注为已取消。
- 客户端包里存在 recurrence 类型，`pnpm --filter client typecheck` 通过，`pnpm openapi:check` 零漂移。
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-03-SUMMARY.md` when done
</output>
