---
phase: 07-recurring-events-tasks
plan: 14
type: execute
wave: 11
depends_on: [07-13]
files_modified:
  - apps/client/src/features/recurrence/recurrence-kind-badge.tsx
  - apps/client/src/features/recurrence/recurrence-rule-row.tsx
  - apps/client/src/features/recurrence/__tests__/recurrence-rule-row-test.tsx
  - apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx
  - apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx
  - apps/client/app/(protected)/households/[id]/index.tsx
autonomous: true
requirements: [RECR-01, RECR-02]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "家庭首页在既有 6 张快捷卡片之后新增第 7 张「周期规则」卡片，紧跟「标签管理」之后；视觉模板逐项沿用标签卡片，图标为 Repeat（与重复徽标同形）且为 teal，不新增任何卡片样式 (D-16)"
    - "规则列表把任务规则与事件规则合并成一个列表，不分 tab，每行用一个图标 + 文字的类型徽标区分；徽标的语义由文字承载，不靠颜色 (D-20)"
    - "列表行显示规则的模板标题（不是任何一条实例的标题）、中文频率摘要、以及服务端推算的下一次发生日期；客户端不推算任何未来发生日期"
    - "频率摘要经 recurrenceInputFromResponse 归一化后再交给 formatRecurrenceSummary —— 直接把响应对象喂进去会渲染出带 null 的文案"
    - "规则既无截止日期也无次数时，摘要行尾追加『，永不结束』；有结束条件时该词不出现"
    - "服务端返回的下一次发生为空时，该行改为『这个重复已经结束』且卡片整体 opacity 0.6；不渲染空白、null 或占位横线。opacity 不是唯一信号，文字与 accessibilityState 同时表达"
    - "已结束的规则保留在列表里，排在未结束规则之后 —— 结束不是移除，让它凭空消失会让用户以为数据被清掉了"
    - "两个新屏幕的守卫分支顺序与 labels/index.tsx 逐项一致：accessChanged → AccessChangedPanel，householdId 为空 → 这个页面暂时无法访问。，token 为 null → 登录已过期。；顺序不得调换"
    - "两个新屏幕用 useFocusEffect 在重新获得焦点时重新取数，从详情页返回列表后列表立刻反映新状态；重取期间保留旧列表，不闪空态"
    - "详情页把拆分锚点（规则时区的明天）作为 RecurrencePicker 的 startDate 传入，而不是规则原始的开始日期 —— 该组件内有一个把 value.startsOn 强制同步到 startDate 的 effect，传错会让选择器与表单互相打架"
    - "详情页不复用 SeriesScopeSheet，也不给它新增第四种 mode：规则级编辑的范围由构造决定只能是此后所有，改为常驻可见的范围说明 + 一次两步内联确认"
    - "保存与结束两条路径在用户点下确认之前都不发出任何写请求；提交中确认行不自动收起，选择器与两个动作全部 disabled"
    - "结束此重复路径上的全部文案（含可访问名称、成功与失败文案）不出现 07-UI-SPEC.md 第 644 行列出的三个禁用词，用户必须能只凭文案分辨单次取消与整条规则结束"
    - "写入失败时保留用户已做的选择器改动，用 Banner 显式告知没有发生任何改变，不做乐观更新；成功后返回列表并重新拉取权威状态"
    - "已结束的规则在详情页所有写入入口 disabled 并给出原因，摘要与历史信息保持只读可见，不隐藏、不跳转"
    - "列表行的格式化与状态判断全部位于 src/features/recurrence/（Jest testMatch 可达），app/ 下两个新屏幕只做取数与编排"
    - "不新增任何依赖：三个新图标全部来自已在用的 lucide-react-native 并走精确子路径导入；apps/client/package.json 的预期 diff 为空"
    - statement: "长模板标题 + 长摘要 + 长 IANA 时区名叠加时，列表行标题最多两行、超出省略但 accessibilityLabel 保留全文，且不挤压『下一次』那一行；200% 字体 / 320px 等效宽度下两个新屏幕无水平滚动"
      verification: backstop
  artifacts:
    - path: "apps/client/src/features/recurrence/recurrence-kind-badge.tsx"
      provides: "任务/事件类型徽标（图标 + 文字，均 inkMuted）"
      exports: ["RecurrenceKindBadge"]
    - path: "apps/client/src/features/recurrence/recurrence-rule-row.tsx"
      provides: "规则行的纯格式化函数、行展示组件与规则时区的明天推算"
      exports: ["formatRuleRow", "RecurrenceRuleRow", "nextDayIsoIn"]
    - path: "apps/client/src/features/recurrence/__tests__/recurrence-rule-row-test.tsx"
      provides: "null 归一化、永不结束后缀、已结束行、可访问名称与锚点推算的断言"
      min_lines: 130
    - path: "apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx"
      provides: "周期规则列表屏"
    - path: "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx"
      provides: "规则详情屏（编辑频率/星期/结束条件 + 结束此重复）"
  key_links:
    - from: "apps/client/src/features/recurrence/recurrence-rule-row.tsx"
      to: "apps/client/src/features/recurrence/recurrence-picker.tsx"
      via: "formatRuleRow 必须先用 recurrenceInputFromResponse 归一化响应，再调 formatRecurrenceSummary"
      pattern: "recurrenceInputFromResponse"
    - from: "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx"
      to: "apps/client/src/features/recurrence/recurrence-rule-row.tsx"
      via: "详情屏用 nextDayIsoIn(rule.timezone) 得到拆分锚点并作为 RecurrencePicker 的 startDate"
      pattern: "nextDayIsoIn"
---

<objective>
新增一个周期规则管理页面族：从家庭首页进入的规则列表，以及可以改动频率/星期/结束条件、并执行「结束此重复」的规则详情。

Purpose: 「我一共设了哪些重复」与「怎么让某一条停下来」这两个问题在实例视图里无处可问。列表把任务与事件的规则合并展示，详情把规则级编辑与结束这两个只可能是「此后所有」的操作放在一个由构造决定范围的界面里，因此不需要范围选择弹层。
Output: 两个新组件与一个纯格式化模块（Jest 可达）、两个新路由、家庭首页第 7 张入口卡片。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-recurring-events-tasks/07-UI-SPEC.md
@.planning/phases/07-recurring-events-tasks/07-CONTEXT.md
@.planning/phases/07-recurring-events-tasks/07-12-SUMMARY.md
@.planning/phases/07-recurring-events-tasks/07-13-SUMMARY.md
</context>

<artifacts_this_plan_produces>
新增文件：`recurrence-kind-badge.tsx`、`recurrence-rule-row.tsx`、`__tests__/recurrence-rule-row-test.tsx`（均在 `apps/client/src/features/recurrence/`）、`app/(protected)/households/[id]/recurrence-rules/index.tsx`、`app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx`。
新增符号：`RecurrenceKindBadge`、`RecurrenceRuleRow`、`formatRuleRow`、`FormattedRuleRow`、`nextDayIsoIn`。
新增路由两条；家庭首页新增第 7 张快捷卡片与其 `handleOpenRecurrenceRules` 处理函数。
不新增依赖，不改动任何已发布的 recurrence 组件。
</artifacts_this_plan_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 可测的规则行格式化与类型徽标</name>
  <files>apps/client/src/features/recurrence/recurrence-kind-badge.tsx, apps/client/src/features/recurrence/recurrence-rule-row.tsx, apps/client/src/features/recurrence/__tests__/recurrence-rule-row-test.tsx</files>
  <read_first>
    - `apps/client/src/features/recurrence/recurrence-summary.tsx` 全文（`formatRecurrenceSummary` 的入参形状与它对 `undefined` 的判断）
    - `apps/client/src/features/recurrence/recurrence-picker.tsx` 第 37-58 行（`recurrenceInputFromResponse` 如何剥掉 null）
    - `apps/client/src/features/recurrence/recurrence-badge.tsx`（同目录的组件写法、主题用法与图标导入形式）
    - `07-UI-SPEC.md` 第 69-83 行（本次新增组件的契约与空值陷阱）、第 454-476 行（行内结构与各元素规格）、第 603-615 行（列表全部文案）
    - `packages/api-client/src/generated/models.ts` 中 07-12 新增的 `RecurrenceRuleListItemDto`
  </read_first>
  <behavior>
    - 传入 `endsOn: null, count: null` 的响应形状时，摘要中不出现 `null` 字样，且行尾追加「永不结束」后缀
    - 传入 `endsOn: '2027-08-12'` 时摘要含该日期且**不**追加「永不结束」后缀
    - 传入 `count: 10` 时摘要含次数且**不**追加「永不结束」后缀
    - `nextOccurrenceDate` 非空时 `nextLine` 为「下一次 」加该日期，`ended` 为 false
    - `nextOccurrenceDate` 为 null 时 `nextLine` 为已结束文案，`ended` 为 true
    - `kind: 'task'` → `kindLabel` 为任务、`accessibilityLabel` 以该词开头；`kind: 'event'` → 事件；`kind: null` → 无 kindLabel 且可访问名称退化为不带类型前缀的形式
    - `formatRuleRow` 在规则时区与设备时区不同时给出时区注记，相同时为 null；每月且开始日 ≥ 29 时给出月末钳位说明
    - `nextDayIsoIn('Asia/Shanghai', new Date('2026-08-12T20:00:00Z'))` 返回 `2026-08-14`（上海已是 13 日，明天为 14 日）；`nextDayIsoIn('Pacific/Midway', new Date('2026-08-12T05:00:00Z'))` 返回 `2026-08-12`（Midway 还是 11 日）；跨月末与跨年边界各一条
  </behavior>
  <action>
**a) `recurrence-kind-badge.tsx`。** 导出 `RecurrenceKindBadge({ kind }: { kind: 'task' | 'event' })`：`ListTodo`（`lucide-react-native/icons/list-todo`）或 `Calendar`（`lucide-react-native/icons/calendar`）图标 14px `inkMuted` + `<Text variant="caption" color="inkMuted">` 的类型文字，外层 `View` 背景 `surfaceMuted`、`borderRadius` 用 `borderRadii.sm`、水平内边距 `spacing[2]`，横向排列 `gap: spacing[1]`。图标必须走精确子路径导入形式（Jest 的 moduleNameMapper 依赖它）。全部样式走 `useTheme<Theme>()`，不得出现裸色值、裸间距、裸圆角或裸字号。文字取自 `07-UI-SPEC.md` 第 608 行。

**b) `recurrence-rule-row.tsx`。** 导出：

- `export function nextDayIsoIn(timeZone: string, now: Date = new Date()): string` —— 先用 `new Intl.DateTimeFormat('en-CA', { timeZone, year: 'numeric', month: '2-digit', day: '2-digit' }).format(now)` 得到该时区的今天，再在 `YYYY-MM-DD` 上做一次纯粹的加一天（用 `Date.UTC` 构造再读回，避免设备本地时区参与）。它是详情屏拆分锚点的唯一来源。
- `export interface FormattedRuleRow { title: string; summary: string; clampNote: string | null; timeZoneNote: string | null; nextLine: string; ended: boolean; kindLabel: string | null; accessibilityLabel: string }`
- `export function formatRuleRow(rule: RecurrenceRuleListItemDto, deviceTimeZone: string): FormattedRuleRow` —— **必须**先 `recurrenceInputFromResponse(rule)`（从 `recurrence-picker.tsx` 引入，注意它不在 `recurrence-summary.tsx` 里），归一化结果为 null 时视为异常输入直接返回一个只含标题的降级结果；否则交给 `formatRecurrenceSummary(normalized, deviceTimeZone)`。当 `rule.endsOn` 与 `rule.count` **都**为空时在 `summary` 尾部追加 `07-UI-SPEC.md` 第 610 行的后缀。`nextLine` 按第 609 / 611 行取值。`accessibilityLabel` 按第 612 行的模板拼装；`kind` 为 null 时省略类型前缀词，其余部分不变。
- `export function RecurrenceRuleRow({ rule, deviceTimeZone, onPress }: ...)` —— 纯展示：`Pressable` 卡片，几何逐项沿用 `labels/index.tsx` 第 303-312 行的行卡片（`surface` 背景、`borderRadii.md`、`padding: spacing[4]`、1px `border`），`accessibilityRole="button"`、`accessibilityLabel` 用 `formatRuleRow` 的结果、`accessibilityState={{ disabled: false }}`，`ended` 为 true 时整卡 `opacity: 0.6`。内部 `Stack gap={1}`：第一行是 `RecurrenceKindBadge`（`kindLabel` 为 null 时不渲染徽标）+ `<Text variant="body" numberOfLines={2}>` 标题；第二行 `caption`/`inkMuted` 的摘要；第三行 `caption` 的 `nextLine`（`ended` 时用 `inkMuted`）。组件自身不发任何请求、不推算任何日期。

**c) 测试。** 新建 `__tests__/recurrence-rule-row-test.tsx` 覆盖 `<behavior>` 的全部九条，其中「不出现 null」那条用正则 `/null/` 对 `summary` 做断言。再加一条静态断言：`formatRuleRow` 对同一条规则的输出与详情页「重复」信息块使用的摘要来源一致（即两处都来自 `formatRecurrenceSummary`，测试里断言 `formatRuleRow(...).summary` 以 `formatRecurrenceSummary(recurrenceInputFromResponse(rule)!, tz).summary` 为前缀）。
  </action>
  <verify>
    <automated>cd apps/client &amp;&amp; pnpm typecheck &amp;&amp; pnpm test -- --runInBand --testPathPattern recurrence-rule-row</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recurrenceInputFromResponse" apps/client/src/features/recurrence/recurrence-rule-row.tsx` ≥ 1，且该文件中不存在直接把 `rule` 传给 `formatRecurrenceSummary` 的调用。
    - `grep -cE "lucide-react-native/icons/(list-todo|calendar)" apps/client/src/features/recurrence/recurrence-kind-badge.tsx` 为 2。
    - `cd apps/client && pnpm test -- --runInBand --testPathPattern recurrence-rule-row` 全绿，用例数 ≥ 10。
    - 针对 `endsOn: null, count: null` 的用例断言 `summary` 不匹配 `/null/` 且以「永不结束」后缀结尾。
    - `git diff --stat apps/client/package.json` 为空。
    - `cd apps/client && pnpm typecheck` 无错误。
  </acceptance_criteria>
  <done>规则行的全部格式化、类型徽标与锚点推算落在 Jest 可达的模块里，null 陷阱与永不结束后缀各有可失败的断言。</done>
</task>

<task type="auto">
  <name>Task 2: 周期规则列表屏与家庭首页入口卡片</name>
  <files>apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx, apps/client/app/(protected)/households/[id]/index.tsx</files>
  <read_first>
    - `apps/client/app/(protected)/households/[id]/labels/index.tsx` 全文（骨架原型：守卫顺序、`useFocusEffect`、`AppShell`/`HouseholdHeader`/`HouseholdSwitcher` 三件套、加载与错误块的形状）
    - `apps/client/app/(protected)/households/[id]/index.tsx` 第 1-20 行（图标导入形式）、第 60-80 行（`handleOpen*` 处理函数）、第 200-226 行（标签卡片模板，逐项照抄）
    - `07-UI-SPEC.md` 第 425-476 行（路由、骨架、入口卡片、列表屏规格与排序）、第 594-615 行（入口与列表的全部文案）、第 663 行（加载/失败/聚焦重取行为）
    - Task 1 产出的 `recurrence-rule-row.tsx`
  </read_first>
  <action>
**a) 列表屏。** 新建 `app/(protected)/households/[id]/recurrence-rules/index.tsx`，骨架逐项照抄 `labels/index.tsx`：
- `useLocalSearchParams<{ id: string }>()` + `const householdId = id ?? currentHouseholdId;`
- 三个守卫分支按**固定顺序**：`viewState === 'accessChanged'` → `AccessChangedPanel`；`householdId` 为空 → `这个页面暂时无法访问。`；取 token 为 null → 错误态 `登录已过期。`。顺序不得调换 —— Phase 2 的访问态边界已在 `labels/index.tsx` 里验证过，重排会让「被移出家庭」退化成「页面无法访问」。
- `useFocusEffect(useCallback(() => { void fetchRules(); }, [fetchRules]))` 重新取数；重取时**不清空**已有列表（把 `setLoading(true)` 只用于首次加载，或在失败分支保留旧数据），避免闪空态。
- `AppShell` 带 `accessibilityLabel`、`title`、`showProfile`、`refreshing`、`onRefresh`；`HouseholdHeader` + `HouseholdSwitcher` 与 `handleSwitch`（`router.replace` 回本路由）。
- 取数用 `sessionApiClient.listRecurrenceRules(token, householdId)`（07-12 生成的方法）。加载中居中 `ActivityIndicator`（照 `labels/index.tsx` 第 280-284 行），失败渲染 `destructiveSoft` 错误块 + `重试` 动作，文案取自 `07-UI-SPEC.md` 第 615 行。
- `Heading` 与 `AppShell` 的 `title` 用第 607 行的屏幕标题。空列表文案取自第 613 行。
- 列表**不做客户端排序** —— 服务端已按契约排好（未结束在前按下一次发生升序，已结束在后按标题升序），照原顺序渲染即可。逐条渲染 Task 1 的 `RecurrenceRuleRow`，`onPress` 走 `router.push(\`/households/${encodeURIComponent(householdId)}/recurrence-rules/${encodeURIComponent(rule.id)}\`)`。`deviceTimeZone` 用 `Intl.DateTimeFormat().resolvedOptions().timeZone`，与既有摘要组件取法一致。
- 屏幕本身不含任何格式化或状态判断逻辑（全部来自 Task 1 的模块）。

**b) 首页入口卡片。** 在 `app/(protected)/households/[id]/index.tsx`：新增 `Repeat` 图标导入（`lucide-react-native/icons/repeat`，精确子路径形式）与 `handleOpenRecurrenceRules` 处理函数（照 `handleOpenLabels` 的写法 push 到新路由）。在「标签管理」卡片之后、「家庭笔记」卡片之前插入第 7 张 `Pressable`，样式对象逐项复制标签卡片（`flexDirection`、`alignItems`、`gap: spacing[3]`、`backgroundColor: surface`、`borderRadii.md`、`padding: spacing[4]`、1px `border`、按下 `opacity: 0.8`），图标为 `<Repeat size={24} color={activeTheme.colors.teal} strokeWidth={1.5} />`（**teal 不是 coral** —— 首页已有三张 coral 图标卡片）。可访问名称、标题、副标题、尾部文字取自 `07-UI-SPEC.md` 第 598-601 行。不新增任何卡片样式，其余 6 张卡片一字不改。
  </action>
  <verify>
    <automated>cd apps/client &amp;&amp; pnpm typecheck &amp;&amp; pnpm test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx` 存在，且其中 `AccessChangedPanel`、`这个页面暂时无法访问。`、`登录已过期。` 三处在文件中的出现顺序与 `labels/index.tsx` 中的相对顺序一致。
    - `grep -c "useFocusEffect" "apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx"` 为 1。
    - `grep -c "formatRecurrenceSummary\|recurrenceInputFromResponse\|\.sort(" "apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx"` 为 0（屏幕不含格式化与排序逻辑）。
    - `grep -c "lucide-react-native/icons/repeat" "apps/client/app/(protected)/households/[id]/index.tsx"` 为 1，且新卡片的图标颜色引用 `colors.teal`。
    - `apps/client/app/(protected)/households/[id]/index.tsx` 中 `Pressable` 的数量比本任务开始前恰好多 1。
    - `git diff --stat apps/client/package.json` 为空；`cd apps/client && pnpm typecheck` 与 `pnpm test -- --runInBand` 均无错误。
  </acceptance_criteria>
  <done>从家庭首页可以进入周期规则列表，任务与事件的规则合并展示、按服务端顺序渲染，已结束的规则仍在列表中，屏幕只做取数与编排。</done>
</task>

<task type="auto">
  <name>Task 3: 规则详情屏 —— 规则级编辑与结束此重复</name>
  <files>apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx</files>
  <read_first>
    - Task 2 产出的列表屏（骨架、守卫顺序、取数写法直接沿用）
    - `apps/client/src/features/recurrence/recurrence-picker.tsx` 第 135-180 行（props 契约与那个把 `value.startsOn` 同步到 `startDate` 的 effect）
    - `apps/client/app/(protected)/households/[id]/labels/index.tsx` 第 385-409 行（两步内联确认的形状）
    - `apps/client/src/ui/primitives.tsx` 中的 `Banner`（第 342 行）与 `Spinner`（第 107 行）
    - `07-UI-SPEC.md` 第 477-529 行（详情屏结构、`startDate` 契约、为什么不复用范围弹层、两步确认与结束路径的全部规格）、第 617-644 行（详情与结束路径的全部文案与禁用词）、第 665-668 行与第 684-693 行（行为与无障碍要求）
  </read_first>
  <action>
新建 `app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx`，骨架与三个守卫分支照 Task 2 的列表屏。取数用 `sessionApiClient.getRecurrenceRule(token, householdId, ruleId)`；404 与加载失败分别用 `07-UI-SPEC.md` 第 628 / 627 行的文案。

屏幕结构自上而下：`HouseholdHeader` → `Heading`（规则的模板标题，**不得截断**）→ 一行「类型徽标 + `nextLine`」（复用 Task 1 的 `RecurrenceKindBadge` 与 `formatRuleRow` 的结果）→「重复」信息块（`label`/`inkMuted` 的标签 + `body` 的摘要 + 可选的月末钳位说明 + 可选的时区注记，结构与文案与已发布的实例详情页逐字一致，直接用 `formatRuleRow` 的 `summary`/`clampNote`/`timeZoneNote`）→ 间距 `spacing[5]` → `<RecurrencePicker>` → 常驻可见的范围说明（第 622 行文案，`caption`/`inkMuted`）→ 主按钮「保存更改」→ 间距 `spacing[6]` → 屏幕最下方的 destructive 文本动作「结束此重复」。

**`RecurrencePicker` 的接线（易错，强制）：** `value` 初始化为 `recurrenceInputFromResponse(rule)`；`startDate` 必须传 `nextDayIsoIn(rule.timezone)`（Task 1 的函数，即规则时区的明天），**不得**传规则原始的 `startsOn`。该组件内有一个把 `value.startsOn` 强制同步到 `startDate` 的 effect，传错会让选择器每次渲染都改写用户刚做的选择。用 `useMemo` 固定这个锚点，避免每次渲染重新计算导致的抖动。

**保存路径（两步内联确认）：** 点「保存更改」**不直接写入**，就地展开一行确认（形状沿用 `labels/index.tsx` 的两步内联确认，但用中性色而不是 destructive）：提示文案取第 624 行，两个动作取第 625 行。只有点下确认动作才调用 `sessionApiClient.updateRecurrenceRule(token, householdId, ruleId, { recurrence: 当前选择器的值 })`。进行中：确认动作的标签换成第 626 行的文案，`RecurrencePicker` 与两个动作全部 `disabled`，确认行**不自动收起**直到服务端返回。成功后返回列表（`router.back()` 或 `router.replace` 到列表路由）并让列表的 `useFocusEffect` 重新拉取权威状态；**不做任何乐观更新**，也不播报生成的实例数量。失败时在屏幕顶部插入 `Banner`：403 用第 630 行文案，其余用第 629 行文案；用户已做的选择器改动**全部保留**，确认行恢复可用。

**结束路径（两步内联确认，destructive）：** 触发是屏幕最下方的文本动作，标签取第 636 行，`bodySm`/`destructive`，触控目标 ≥48×48px（用 `hitSlop` 补足）。点下后就地展开确认行：提示取第 637 行、两个动作取第 638 行、进行中标签取第 639 行。只有确认才调用 `sessionApiClient.endRecurrenceRule(token, householdId, ruleId)`。成功后返回列表并重新拉取权威状态 —— 该规则**仍在列表中**并标为已结束，不得让它从列表消失。失败时 `Banner`：403 用第 643 行文案，其余用第 642 行文案。⚠ 这条路径上的标题、说明、按钮、可访问名称、成功与失败文案里**不得**出现 `07-UI-SPEC.md` 第 644 行列出的三个禁用词 —— 用户在已发布的「单次取消」与本操作之间必须能只凭文案分辨；确认文案里必须写明已发生过的安排全部保留。

**已结束的规则（`formatRuleRow(...).ended === true`）：** `RecurrencePicker`、「保存更改」与结束动作全部 `disabled` 且带 `accessibilityState={{ disabled: true }}`，结束动作下方一行 `caption` 说明取第 641 行；摘要与历史信息保持只读可见，不隐藏也不跳转。

**权限：** 界面**不得**预先隐藏任何动作（客户端不复制权限判断，规则数据里也没有创建者身份），一律在服务端拒绝后用 `Banner` 呈现原因。

**不复用 `SeriesScopeSheet`，也不给它新增第四种 mode** —— 规则级编辑的范围由构造决定只能是此后所有，弹出一个只有一个可选项的弹层是纯噪音。已发布的三种 mode 保持原样。

**无障碍：** 两处确认提示都用 `accessibilityLiveRegion="polite"` 播报；确认与取消在 DOM 顺序上紧随各自的触发动作，键盘 Tab 可直接到达。全部样式走 `useTheme<Theme>()`，无裸色值/裸间距/裸圆角/裸字号/裸阴影。
  </action>
  <verify>
    <automated>cd apps/client &amp;&amp; pnpm typecheck &amp;&amp; pnpm test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nextDayIsoIn(" "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx"` ≥ 1，且该文件中 `startDate=` 的取值表达式包含 `nextDayIsoIn`，不包含 `startsOn`。
    - `grep -c "SeriesScopeSheet" "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx"` 为 0；`git diff --stat apps/client/src/features/recurrence/series-scope-sheet.tsx` 为空。
    - `grep -cE "删除|移除|清空" "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx" "apps/client/app/(protected)/households/[id]/recurrence-rules/index.tsx" apps/client/src/features/recurrence/recurrence-rule-row.tsx apps/client/src/features/recurrence/recurrence-kind-badge.tsx` 全部为 0。
    - `updateRecurrenceRule` 与 `endRecurrenceRule` 两处调用各自只出现在确认动作的 `onPress` 回调内，触发动作的 `onPress` 只切换确认行的可见状态（人工检查 + `grep -c "sessionApiClient\." "…/[ruleId]/index.tsx"` 为 3：详情取数 + 两个写入）。
    - `grep -c "accessibilityLiveRegion=\"polite\"" "apps/client/app/(protected)/households/[id]/recurrence-rules/[ruleId]/index.tsx"` ≥ 2。
    - `git diff --stat apps/client/package.json` 为空；`cd apps/client && pnpm typecheck` 与 `pnpm test -- --runInBand` 均无错误。
  </acceptance_criteria>
  <done>规则详情屏可以改动频率/星期/结束条件并结束整条重复，两条写入路径都在确认前零请求，选择器接的是拆分锚点而不是原始开始日期，结束路径的文案与单次取消完全可分辨。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端屏幕 → 规则写接口 | 客户端不做权限判断，一切授权由服务端裁决；界面只负责不误导用户 |
| 路由参数 `ruleId` → API 调用 | 来自深链，可能指向其他家庭的规则或已被移除的规则 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-44 | Elevation of privilege | 客户端复制权限判断并预先隐藏动作，与服务端裁决产生分歧 | medium | mitigate | 界面不隐藏任何动作，规则数据里也不下发创建者身份；403 一律用 `Banner` 呈现原因文案 |
| T-07-45 | Tampering | 用户误把「结束整条规则」当成「取消这一次」，造成不可逆的实例清理 | high | mitigate | 结束路径全部文案禁用三个词（验收条件用 grep 把守）；两步内联确认；确认文案写明已发生过的安排全部保留；锚点为明天，今天这一次不受影响 |
| T-07-46 | Information disclosure | 深链到其他家庭的 ruleId 时泄漏规则存在性 | medium | mitigate | 详情取数完全依赖服务端的 404 `RECURRENCE_RULE_NOT_FOUND`，客户端不做任何本地存在性推断，一律渲染同一条「不存在或已被移除」文案 |
| T-07-47 | Tampering | 乐观更新让界面显示一个服务端并未发生的状态 | medium | mitigate | 两条写入路径都不做乐观更新；成功后返回列表并由 `useFocusEffect` 重新拉取权威状态；失败时显式告知没有发生任何改变 |
| T-07-48 | Denial of Service | 确认前就发出写请求，导致误触即写 | high | mitigate | 两步内联确认；验收条件断言屏幕内 `sessionApiClient.` 调用恰好 3 处且两处写入都在确认回调内 |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增任何依赖，三个新图标均来自已在用的 `lucide-react-native` 并走精确子路径导入；`apps/client/package.json` 预期 diff 为空（07-UI-SPEC.md Registry Safety 的新依赖禁令），无安装步骤故无供应链检查点 |
</threat_model>

<verification>
- `cd apps/client && pnpm typecheck` 无错误
- `cd apps/client && pnpm test -- --runInBand` 全绿
- `git diff --stat apps/client/package.json` 为空
- `git diff --stat apps/client/src/features/recurrence/recurrence-picker.tsx apps/client/src/features/recurrence/series-scope-sheet.tsx apps/client/src/features/recurrence/recurrence-badge.tsx apps/client/src/features/events/event-card.tsx apps/client/src/features/tasks/task-card.tsx` 全部为空（已发布契约零回归）
- `grep -rcE "删除|移除|清空" "apps/client/app/(protected)/households/[id]/recurrence-rules/"` 全部为 0
</verification>

<success_criteria>
- 家庭首页 → 规则列表 → 规则详情 → 结束/保存 的完整路径可走通
- 任务与事件规则合并一个列表，类型由图标 + 文字徽标区分
- 下一次发生日期完全来自服务端，客户端零日期推算
- 两条写入路径确认前零请求、失败不丢用户输入、成功不做乐观更新
- 零新依赖，已发布的 recurrence 组件零改动
</success_criteria>

<output>
Create `.planning/phases/07-recurring-events-tasks/07-14-SUMMARY.md` when done
</output>
</content>
